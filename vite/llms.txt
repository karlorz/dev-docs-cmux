### Example

Source: https://github.com/vitejs/vite/blob/main/docs/config/preview-options.md

Example configuration for setting server and preview ports.

```js
export default defineConfig({
  server: {
    port: 3030,
  },
  preview: {
    port: 8080,
  },
})
```

--------------------------------

### Install dependencies

Source: https://github.com/vitejs/vite/blob/main/packages/create-vite/template-qwik/README.md

Command to install project dependencies using npm, pnpm, or yarn.

```bash
$ npm install # or pnpm install or yarn install
```

--------------------------------

### CSS Styling Example

Source: https://github.com/vitejs/vite/blob/main/playground/worker/index.html

Example of basic CSS styling for paragraphs and a class.

```css
p { background: rgba(0, 0, 0, 0.1); }
.classname { color: green; }
```

--------------------------------

### Install a Vite plugin

Source: https://github.com/vitejs/vite/blob/main/docs/guide/using-plugins.md

Install the official legacy browser support plugin as a development dependency.

```bash
$ npm add -D @vitejs/plugin-legacy
```

--------------------------------

### Preview the Production Build

Source: https://github.com/vitejs/vite/blob/main/docs/guide/static-deploy.md

Starts a local static server to verify the production build before deployment.

```bash
$ npm run preview
```

--------------------------------

### Start the Vite development server

Source: https://github.com/vitejs/vite/blob/main/docs/guide/index.md

Launch the Vite dev server using the appropriate command for your environment.

```npm
$ npx vite
```

```Yarn
$ yarn vite
```

```pnpm
$ pnpm vite
```

```Bun
$ bunx vite
```

```Deno
$ deno run -A npm:vite
```

--------------------------------

### Install CSS pre-processors via npm

Source: https://github.com/vitejs/vite/blob/main/docs/guide/features.md

Install the corresponding pre-processor package to enable `.scss`, `.sass`, `.less`, or `.styl` files. No extra Vite plugins are required.

```bash
# .scss and .sass
npm add -D sass-embedded # or sass

# .less
npm add -D less

# .styl and .stylus
npm add -D stylus
```

--------------------------------

### Start preview server in TypeScript

Source: https://github.com/vitejs/vite/blob/main/docs/guide/api-javascript.md

Programmatically launch a preview server, print its URLs, and bind CLI shortcut listeners.

```ts
import { preview } from 'vite'

const previewServer = await preview({
  // any valid user config options, plus `mode` and `configFile`
  preview: {
    port: 8080,
    open: true,
  },
})

previewServer.printUrls()
previewServer.bindCLIShortcuts({ print: true })
```

--------------------------------

### Install SWC dependencies for decorator lowering

Source: https://github.com/vitejs/vite/blob/main/docs/guide/migration.md

Install the required SWC plugins to handle native decorators as a workaround.

```npm
$ npm install -D @rollup/plugin-swc @swc/core
```

```Yarn
$ yarn add -D @rollup/plugin-swc @swc/core
```

```pnpm
$ pnpm add -D @rollup/plugin-swc @swc/core
```

```Bun
$ bun add -D @rollup/plugin-swc @swc/core
```

```Deno
$ deno add -D npm:@rollup/plugin-swc npm:@swc/core
```

--------------------------------

### preview(inlineConfig?: InlineConfig)

Source: https://github.com/vitejs/vite/blob/main/docs/guide/api-javascript.md

Starts a local preview server using the provided inline configuration to serve the built production output.

```APIDOC
### Function: `preview`

### Type Signature
```ts
async function preview(inlineConfig?: InlineConfig): Promise<PreviewServer>
```

### Parameters
- **inlineConfig** (`InlineConfig`, optional) - Configuration options for the preview server.

### Returns
- `Promise<PreviewServer>` - A promise that resolves to the `PreviewServer` instance.

### Example
```ts
import { preview } from 'vite'

const previewServer = await preview({
  // any valid user config options, plus `mode` and `configFile`
  preview: {
    port: 8080,
    open: true,
  },
})

previewServer.printUrls()
previewServer.bindCLIShortcuts({ print: true })
```
```

--------------------------------

### Install Vite CLI

Source: https://github.com/vitejs/vite/blob/main/docs/guide/index.md

Install Vite as a development dependency using your preferred package manager.

```npm
$ npm install -D vite
```

```Yarn
$ yarn add -D vite
```

```pnpm
$ pnpm add -D vite
```

```Bun
$ bun add -D vite
```

```Deno
$ deno add -D npm:vite
```

--------------------------------

### Start dev server with Vite CLI

Source: https://github.com/vitejs/vite/blob/main/docs/guide/cli.md

Starts the Vite development server in the specified root directory or current working directory. Aliases include `vite dev` and `vite serve`.

```bash
vite [root]
```

--------------------------------

### Scaffold a new Vite project

Source: https://github.com/vitejs/vite/blob/main/docs/guide/index.md

Use these commands to start the interactive scaffolding process for a new project.

```bash
$ npm create vite@latest
```

```bash
$ yarn create vite
```

```bash
$ pnpm create vite
```

```bash
$ bun create vite
```

```bash
$ deno init --npm vite
```

--------------------------------

### Install Babel dependencies for decorator lowering

Source: https://github.com/vitejs/vite/blob/main/docs/guide/migration.md

Install the required Babel plugins to handle native decorators as a workaround.

```npm
$ npm install -D @rolldown/plugin-babel @babel/plugin-proposal-decorators
```

```Yarn
$ yarn add -D @rolldown/plugin-babel @babel/plugin-proposal-decorators
```

```pnpm
$ pnpm add -D @rolldown/plugin-babel @babel/plugin-proposal-decorators
```

```Bun
$ bun add -D @rolldown/plugin-babel @babel/plugin-proposal-decorators
```

```Deno
$ deno add -D npm:@rolldown/plugin-babel npm:@babel/plugin-proposal-decorators
```

--------------------------------

### Install esbuild for CSS minification

Source: https://github.com/vitejs/vite/blob/main/docs/config/build-options.md

Required when setting `build.cssMinify` to `'esbuild'`. Installs esbuild as a development dependency.

```sh
npm add -D esbuild
```

--------------------------------

### Custom Environment Instance Example

Source: https://github.com/vitejs/vite/blob/main/docs/guide/api-environment.md

An example demonstrating how to use `customEnvironment` from `vite-environment-provider` to define a custom SSR environment with specific build output directories.

```js
import { customEnvironment } from 'vite-environment-provider'

export default {
  build: {
    outDir: '/dist/client',
  },
  environments: {
    ssr: customEnvironment({
      build: {
        outDir: '/dist/ssr',
      },
    }),
  },
}
```

--------------------------------

### Accessing Environments from the Server

Source: https://github.com/vitejs/vite/blob/main/docs/guide/api-environment-instances.md

Example demonstrating how to access client and SSR environments from a Vite dev server instance.

```js
// create the server, or get it from the configureServer hook
const server = await createServer(/* options */)

const clientEnvironment = server.environments.client
clientEnvironment.transformRequest(url)
console.log(server.environments.ssr.moduleGraph)
```

--------------------------------

### ModuleRunner Example Usage

Source: https://github.com/vitejs/vite/blob/main/docs/guide/api-environment-runtimes.md

Demonstrates how to instantiate a ModuleRunner with a custom transport and the default ESModulesEvaluator to import an entry point.

```js
import {
  ModuleRunner,
  ESModulesEvaluator,
  createNodeImportMeta,
} from 'vite/module-runner'
import { transport } from './rpc-implementation.js'

const moduleRunner = new ModuleRunner(
  {
    transport,
    createImportMeta: createNodeImportMeta, // if the module runner runs in Node.js
  },
  new ESModulesEvaluator(),
)

await moduleRunner.import('/src/entry-point.js')
```

--------------------------------

### Example .env file

Source: https://github.com/vitejs/vite/blob/main/docs/guide/env-and-mode.md

An example `.env` file showing a `VITE_` prefixed variable and a non-prefixed variable.

```env
VITE_SOME_KEY=123
DB_PASSWORD=foobar
```

--------------------------------

### Example License Metadata Output in JSON

Source: https://github.com/vitejs/vite/blob/main/docs/config/build-options.md

Generated when setting build.license fileName to a path ending with .json. Contains structured metadata for each bundled dependency.

```json
[
  {
    "name": "dep-1",
    "version": "1.2.3",
    "identifier": "CC0-1.0",
    "text": "CC0 1.0 Universal\n\n..."
  },
  {
    "name": "dep-2",
    "version": "4.5.6",
    "identifier": "MIT",
    "text": "MIT License\n\n..."
  }
]
```

--------------------------------

### configurePreviewServer

Source: https://github.com/vitejs/vite/blob/main/docs/guide/api-plugin.md

Configures the preview server before other middlewares are installed. Allows injecting custom middlewares or returning a post-hook function to run after internal middlewares.

```APIDOC
## configurePreviewServer

### Description
Configures the preview server before other middlewares are installed. Returning a function allows injecting middlewares after internal middlewares are set up.

### Type Signature
```ts
(server: PreviewServer) => (() => void) | void | Promise<(() => void) | void>
```

### Hook Attributes
- **Kind**: `async`, `sequential`
- **Scope**: Global

### Parameters
- **server** (`PreviewServer`) - The Vite preview server instance.

### Return Value
- `(() => void) | void | Promise<(() => void) | void>` - Optional post-hook callback invoked after internal middlewares are installed, or a Promise resolving to it.

### Example
```js
const myPlugin = () => ({
  name: 'configure-preview-server',
  configurePreviewServer(server) {
    return () => {
      server.middlewares.use((req, res, next) => {
        // custom handle request...
      })
    }
  },
})
```
```

--------------------------------

### Implement a global module counter plugin

Source: https://github.com/vitejs/vite/blob/main/docs/changes/shared-plugins-during-build.md

A plugin example that tracks transformed modules globally across all environments.

```js
function CountTransformedModulesPlugin() {
  let transformedModules
  return {
    name: 'count-transformed-modules',
    buildStart() {
      transformedModules = 0
    },
    transform(id) {
      transformedModules++
    },
    buildEnd() {
      console.log(transformedModules)
    },
  }
}
```

--------------------------------

### Configure Dev Server URL Pathname to Open on Start

Source: https://github.com/vitejs/vite/blob/main/docs/config/server-options.md

Specifies a custom URL pathname to open automatically in the browser when the development server starts. You can configure browser selection via the BROWSER environment variable.

```javascript
export default defineConfig({
  server: {
    open: '/docs/index.html',
  },
})
```

--------------------------------

### Importing Asset as String

Source: https://github.com/vitejs/vite/blob/main/docs/guide/assets.md

Example of importing an asset as a raw string using the `?raw` suffix.

```js
import 'vite/client'
// ---cut---
import shaderString from './shader.glsl?raw'
```

--------------------------------

### Implement an environment-aware module counter plugin

Source: https://github.com/vitejs/vite/blob/main/docs/changes/shared-plugins-during-build.md

A plugin example that uses a Map to track transformed modules separately for each environment.

```ts
function PerEnvironmentCountTransformedModulesPlugin() {
  const state = new Map<Environment, { count: number }>()
  return {
    name: 'count-transformed-modules',
    perEnvironmentStartEndDuringDev: true,
    buildStart() {
      state.set(this.environment, { count: 0 })
    },
    transform(id) {
      state.get(this.environment).count++
    },
    buildEnd() {
      console.log(this.environment.name, state.get(this.environment).count)
    },
  }
}
```

--------------------------------

### WASM Import API Example

Source: https://github.com/vitejs/vite/blob/main/docs/blog/announcing-vite3.md

The WebAssembly import API has been revised to avoid collisions with future standards and to make it more flexible.

```js
import init from './example.wasm?init'

init().then((instance) => {
  instance.exports.test()
})
```

--------------------------------

### Install Cloudflare Vite Plugin

Source: https://github.com/vitejs/vite/blob/main/docs/guide/static-deploy.md

Install the required development dependency for Cloudflare Workers integration.

```bash
$ npm install --save-dev @cloudflare/vite-plugin
```

--------------------------------

### WebAssembly Loading and Interaction Examples

Source: https://github.com/vitejs/vite/blob/main/playground/wasm/index.html

Demonstrates various Wasm loading techniques including ?init, ?url, and ?worker, as well as handling shared globals and string builtins.

```javascript
import light from './light.wasm?init' import heavy from './heavy.wasm?init' import myWorker from './worker?worker' import { add } from './add.wasm' import { exported\_func } from './light-with-imports.wasm' import { getResult } from './imports.js' import { answer } from './global.wasm' import { getAnswerPlusOne, readCounter } from './global-consumer.wasm' import { bumpCounter } from './global.wasm' import { helloLength } from './string-constants.wasm' const w = new myWorker() w.addEventListener('message', (ev) => { text('.worker-wasm .result', ev.data.result) }) text('.direct-wasm .result', add(1, 2)) exported\_func() text('.direct-wasm-with-imports .result', getResult()) text('.direct-wasm-global .result', `\${answer} ${typeof answer}\`) text('.direct-wasm-global-import .result', getAnswerPlusOne()) // The mutable global is a single live cell shared between the two wasm // modules: mutating it from one is observed by the other (7 + 2 = 9). bumpCounter() bumpCounter() text('.direct-wasm-mutable-global-import .result', readCounter()) // 'hello'.length, where 'hello' is an engine-provided imported string constant // and \`length\` is a js-string builtin. text('.direct-wasm-string-builtins .result', helloLength()) async function testWasm(init, resultElement) { const { exported\_func } = await init({ imports: { imported\_func: (res) => (resultElement.textContent = res), }, }).then((i) => i.exports) exported\_func() } function text(el, text) { document.querySelector(el).textContent = text } document .querySelector('.inline-wasm .run') .addEventListener('click', async () => testWasm(light, document.querySelector('.inline-wasm .result')), ) document .querySelector('.output-wasm .run') .addEventListener('click', async () => testWasm(heavy, document.querySelector('.output-wasm .result')), ) document .querySelector('.init-returns-instance .run') .addEventListener('click', async () => { const res = await light({ imports: { imported\_func: (res) => (resultElement.textContent = res), }, }) text( '.init-returns-instance .result', res instanceof WebAssembly.Instance, ) }) import lightUrl from './light.wasm?url' text('.url', lightUrl)
```

--------------------------------

### Build for production

Source: https://github.com/vitejs/vite/blob/main/packages/create-vite/template-qwik/README.md

Command to build the application for production.

```bash
npm run build
```

--------------------------------

### Build the Application

Source: https://github.com/vitejs/vite/blob/main/docs/guide/static-deploy.md

Executes the production build process, which outputs files to the dist directory by default.

```bash
$ npm run build
```

--------------------------------

### Preview production build with Vite CLI

Source: https://github.com/vitejs/vite/blob/main/docs/guide/cli.md

Starts a local server to preview the build directory (default: dist). Run `vite build` prior to running this command to ensure output files are up to date.

```bash
vite preview [root]
```

--------------------------------

### Run development server

Source: https://github.com/vitejs/vite/blob/main/packages/create-vite/template-qwik/README.md

Command to run the application in development mode.

```bash
npm run dev
```

--------------------------------

### Install unreleased Vite commits

Source: https://github.com/vitejs/vite/blob/main/docs/guide/index.md

Commands to install a specific Vite commit using pkg.pr.new for testing latest features.

```bash
$ npm install -D https://pkg.pr.new/vite@SHA
```

```bash
$ yarn add -D https://pkg.pr.new/vite@SHA
```

```bash
$ pnpm add -D https://pkg.pr.new/vite@SHA
```

```bash
$ bun add -D https://pkg.pr.new/vite@SHA
```

--------------------------------

### Install Terser for Minification

Source: https://github.com/vitejs/vite/blob/main/packages/plugin-legacy/README.md

Install terser as a development dependency if using Vite versions older than 8.1.4 or when build.minify is explicitly set to 'terser'.

```shell
npm add -D terser
```

--------------------------------

### External Svelte Store Example

Source: https://github.com/vitejs/vite/blob/main/packages/create-vite/template-svelte/README.md

An example of a simple external Svelte store that can be used to retain state across HMR updates.

```js
// store.js
// An extremely simple external store
import { writable } from 'svelte/store'
export default writable(0)
```

--------------------------------

### Install alternative minifiers with npm

Source: https://github.com/vitejs/vite/blob/main/docs/config/build-options.md

Install esbuild or Terser as dev dependencies when configuring build.minify to 'esbuild' or 'terser' instead of the default Oxc minifier.

```sh
npm add -D esbuild
npm add -D terser
```

--------------------------------

### Create index.html entry point

Source: https://github.com/vitejs/vite/blob/main/docs/guide/index.md

Define the basic HTML entry point for the Vite application.

```html
<p>Hello Vite!</p>
```

--------------------------------

### Dependency Notation and Example

Source: https://github.com/vitejs/vite/blob/main/playground/ssr/src/forked-deadlock/README.md

Defines the notation for module dependencies and waiting states, followed by an example illustrating a forked deadlock scenario.

```text
A -> B means: B is imported by A and B has A in its stack
A ... B means: A is waiting for B to ssrLoadModule()

H -> X ... Y
H -> X -> Y ... B
H -> A ... B
H -> A -> B ... X
```

--------------------------------

### Sample Generated License File in Markdown

Source: https://github.com/vitejs/vite/blob/main/docs/guide/features.md

Displays the typical markdown format generated by `build.license` listing bundled dependencies and license details.

```md
# Licenses

The app bundles dependencies which contain the following licenses:

## dep-1 - 1.2.3 (CC0-1.0)

CC0 1.0 Universal

...

## dep-2 - 4.5.6 (MIT)

MIT License

...
```

--------------------------------

### Configure build and preview scripts in package.json

Source: https://github.com/vitejs/vite/blob/main/docs/guide/static-deploy.md

Define the standard build and preview commands within the scripts section of your project's package.json file.

```json
{
  "scripts": {
    "build": "vite build",
    "preview": "vite preview"
  }
}
```

--------------------------------

### SSR Dependencies Example

Source: https://github.com/vitejs/vite/blob/main/playground/ssr-deps/index.html

Example demonstrating hydration scripts and dynamic module imports, including a specific case to disable pre-transform optimization to trigger a race condition for testing purposes.

```javascript
// hydration scripts import '@vitejs/test-css-lib' // Using dynamic import, so the module is transformed when browser actually // requests it. This essentially disables pre-transform optimization that's // crucial to trigger a race condition, covered by the test case introduced // in https://github.com/vitejs/vite/pull/11973 import('virtual:isomorphic-module').then(({ default: message }) => { document.querySelector('.isomorphic-module-browser').textContent = message })
```

--------------------------------

### Implement SSR middleware with the Environment API

Source: https://github.com/vitejs/vite/blob/main/docs/guide/api-environment-frameworks.md

Demonstrates setting up a Vite server in middleware mode with a custom environment. This example shows the full SSR request lifecycle including HTML transformation and module importing.

```js
import fs from 'node:fs'
import path from 'node:path'
import { createServer } from 'vite'

const viteServer = await createServer({
  server: { middlewareMode: true },
  appType: 'custom',
  environments: {
    server: {
      // by default, modules are run in the same process as the vite server
    },
  },
})

// You might need to cast this to RunnableDevEnvironment in TypeScript or
// use isRunnableDevEnvironment to guard the access to the runner
const serverEnvironment = viteServer.environments.server

app.use('*', async (req, res, next) => {
  const url = req.originalUrl

  // 1. Read index.html
  const indexHtmlPath = path.resolve(import.meta.dirname, 'index.html')
  let template = fs.readFileSync(indexHtmlPath, 'utf-8')

  // 2. Apply Vite HTML transforms. This injects the Vite HMR client,
  //    and also applies HTML transforms from Vite plugins, e.g. global
  //    preambles from @vitejs/plugin-react
  template = await viteServer.transformIndexHtml(url, template)

  // 3. Load the server entry. import(url) automatically transforms
  //    ESM source code to be usable in Node.js! There is no bundling
  //    required, and provides full HMR support.
  const { render } = await serverEnvironment.runner.import(
    '/src/entry-server.js',
  )

  // 4. render the app HTML. This assumes entry-server.js's exported
  //     `render` function calls appropriate framework SSR APIs,
  //    e.g. ReactDOMServer.renderToString()
  const appHtml = await render(url)

  // 5. Inject the app-rendered HTML into the template.
  const html = template.replace(`<!--ssr-outlet-->`, appHtml)

  // 6. Send the rendered HTML back.
  res.status(200).set({ 'Content-Type': 'text/html' }).end(html)
})
```

--------------------------------

### Inline CSS

Source: https://github.com/vitejs/vite/blob/main/playground/css-sourcemap/index.html

An example of inline CSS styling.

```css
.inline { color: red; }
```

--------------------------------

### Scaffold a project with tiged

Source: https://github.com/vitejs/vite/blob/main/docs/guide/index.md

Use tiged to clone a GitHub repository template and install dependencies.

```bash
npx tiged user/project my-project
cd my-project

npm install
npm run dev
```

--------------------------------

### Initialize a Vite app

Source: https://github.com/vitejs/vite/blob/main/docs/blog/announcing-vite2.md

Command to quickly set up a new Vite project, requiring Node.js >=12.

```bash
npm init @vitejs/app
```

--------------------------------

### Build and link Vite locally

Source: https://github.com/vitejs/vite/blob/main/docs/guide/index.md

Steps to clone, build, and link the Vite repository for local development.

```bash
git clone https://github.com/vitejs/vite.git
cd vite
pnpm install
cd packages/vite
pnpm run build
pnpm link # use your preferred package manager for this step
```

--------------------------------

### Pre-bundle dependencies with Vite CLI

Source: https://github.com/vitejs/vite/blob/main/docs/guide/cli.md

Runs manual dependency pre-bundling. Note that this command is deprecated as pre-bundling is handled automatically.

```bash
vite optimize [root]
```

--------------------------------

### Classic Worker Instantiation

Source: https://github.com/vitejs/vite/blob/main/playground/worker/index.html

Instantiating a classic worker, for the .classic-worker test case.

```javascript
new Worker(new URL('./classic-worker.js', import.meta.url))
```

--------------------------------

### HTML Constant Replacement Syntax

Source: https://github.com/vitejs/vite/blob/main/docs/guide/env-and-mode.md

Example of using import.meta.env properties in HTML files.

```html
<h1>Vite is running in %MODE%</h1>
<p>Using data from %VITE_API_URL%</p>
```

--------------------------------

### Configure Render Build Settings

Source: https://github.com/vitejs/vite/blob/main/docs/guide/static-deploy.md

Required build command and publish directory settings for Render static site deployment.

```text
npm install && npm run build
```

```text
dist
```

--------------------------------

### Importing Asset as URL

Source: https://github.com/vitejs/vite/blob/main/docs/guide/assets.md

Example of importing an image asset, which returns its public URL.

```js
import 'vite/client'
// ---cut---
import imgUrl from './img.png'
document.getElementById('hero-img').src = imgUrl
```

--------------------------------

### Eager Glob Import

Source: https://github.com/vitejs/vite/blob/main/playground/glob-import/root/index.html

This example shows how to use the `eager: true` option with `import.meta.glob` to immediately load all matching modules. The result is a map of module paths to their already-resolved module objects.

```javascript
const eagerModules = import.meta.glob('/dir/**', { eager: true })
document.querySelector('.result-eager').textContent = JSON.stringify(
  eagerModules,
  null,
  2,
)
```

--------------------------------

### File System Serving Test Script with Path Matrix

Source: https://github.com/vitejs/vite/blob/main/playground/fs-serve/root/src/index.html

This script initializes imports, defines utility functions for URL segment joining and text display, and sets up a comprehensive matrix of file paths to test various file system serving scenarios, including safe and unsafe access, special characters, and `.env` file handling.

```javascript
import '../../entry'
import json, { msg } from '../../safe.json'
import './code.js'
// Check virtual svg module still works
import fooSvg from 'virtual:foo.svg'

text('.full', JSON.stringify(json))
text('.named', msg)
text('.virtual-svg', fooSvg)

function text(sel, text) {
  document.querySelector(sel).textContent = text
}

// matrix tests
const base = typeof BASE !== 'undefined' ? BASE : ''
const fsBase = joinUrlSegments('/@fs/', ROOT)
const dotEnvWindows83ShortName = DOTENV83SHORTNAME

function joinUrlSegments(a, b) {
  if (!a || !b) {
    return a || b || ''
  }
  if (a.endsWith('/')) {
    a = a.substring(0, a.length - 1)
  }
  if (b[0] !== '/') {
    b = '/' + b
  }
  return a + b
}

const paths = [
  // inside allowed dir
  { testId: 'safe', path: '/root/src/safe.txt' },
  // inside allowed dir with query
  { testId: 'safe-query', path: '/root/src/safe.txt?query' },
  // inside allowed dir
  { testId: 'safe-subdir', path: '/root/src/subdir/safe.txt' },
  // inside allowed dir with special characters
  { testId: 'safe-subdir-special-characters', path: '/root/src/special%20characters%20%C3%A5%C3%A4%C3%B6/safe.txt', },
  // not imported before but inside allowed dir with special characters
  { testId: 'safe-subdir-special-characters2', path: '/root/src/special%20characters%20%C3%A5%C3%A4%C3%B6/safe.json', },
  // imported before
  { testId: 'safe-imported', path: '/safe.json' },
  // imported before with query
  { testId: 'safe-imported-query', path: '/safe.json?query' },
  // outside of allowed dir
  { testId: 'unsafe', path: '/root/unsafe.txt' },
  // outside of allowed dir and root
  { testId: 'unsafe-json', path: '/unsafe.json' },
  // outside of allowed dir
  { testId: 'unsafe-html', path: '/root/unsafe.html' },
  // outside of allowed dir and root
  { testId: 'unsafe-html-outside-root', path: '/unsafe.html' },
  // outside of allowed dir with special characters #8498
  { testId: 'unsafe-8498', path: '/root/src/%2e%2e%2funsafe%2etxt' },
  // outside of allowed dir with special characters 2 #8498
  { testId: 'unsafe-8498-2', path: '/root/src/%252e%252e%252funsafe%252etxt', },
  // outside of allowed dir with import inline
  { testId: 'unsafe-import-inline', path: '/root/unsafe.txt?import&inline', },
  // outside of allowed dir with raw query import
  { testId: 'unsafe-raw-query-import', path: '/root/unsafe.txt?raw?import', },
  { testId: 'unsafe-raw-import-raw-outside-root', path: '/unsafe.json?import&raw', },
  { testId: 'unsafe-raw-import-raw-outside-root1', path: '/unsafe.json?import&raw??', },
  { testId: 'unsafe-raw-import-raw-outside-root2', path: '/unsafe.json?import&raw?&', },
  // outside of allowed dir with ?url
  { testId: 'unsafe-url', path: '/root/unsafe.txt?import&url' },
  // outside of allowed dir with .svg query import
  { testId: 'unsafe-query-dot-svg-import', path: '/root/unsafe.txt?.svg?import', },
  // outside of allowed dir, svg
  { testId: 'unsafe-svg', path: '/root/unsafe.svg?import', },
  // outside of allowed dir, inline faux wasm?init
  { testId: 'unsafe-import-inline-wasm-init', path: '/root/unsafe.txt?import&?inline=1.wasm?init', },
  // outside of allowed dir, relative path after query
  { testId: 'unsafe-relative-path-after-query', path: '/root/src/?/../../unsafe.txt?import&raw', },
  // .env, denied by default
  { testId: 'unsafe-dotenv', path: '/root/src/.env', },
  // .env, for case insensitive file systems
  { testId: 'unsafe-dotenv-casing', path: '/root/src/.EnV', },
  // .env, with raw query
  { testId: 'unsafe-dotenv-raw', path: '/root/src/.env?raw', },
  // .env, with url query
  { testId: 'unsafe-dotenv-url', path: '/root/src/.env?url' },
  // .env with inline query
  { testId: 'unsafe-dotenv-inline', path: '/root/src/.env?inline' },
  // .env with .svg?.wasm?init
  { testId: 'unsafe-dotenv-query-dot-svg-wasm-init', path: '/root/src/.env?.svg?.wasm?init', },
  // .env with ?import&raw
  { testId: 'unsafe-dotenv-import-raw', path: '/root/src/.env?import&raw' },
  // .env with NTFS ADS suffix
  { testId: 'unsafe-dotenv-ntfs-ads', path: '/root/src/.env::$DATA' },
  // .env with 8.3 short name
  { testId: 'unsafe-dotenv-83-short-name', path: dotEnvWindows83ShortName === undefined ? false : `/root/src/${dotEnvWindows83ShortName}`, },
]

const variants = {
  '': (path) => path === false ? path : path.startsWith('/root/') ? joinUrlSegments(base, path.replace(/^\\/root/, '')) : false,
  '-fs': (path) => path === false ? path : joinUrlSegments(base, fsBase + path),
}

for (const { testId, path } of paths) {
  for (const [variantName, transformPath] of Object.entries(variants)) {
    const baseSelector = `.fetch${variantName}-${testId}`
    const resolvedPath = transformPath(path)
    if (resolvedPath === false) {
      text(`${baseSelector}-path`, '--')
      document.querySelector(
        `a[href="#${baseSelector.slice(1)}-content"]`,
      ).textContent = ''
      text(`${baseSelector}-content`, '--')
      continue
    }
    text(`${baseSelector}-path`, resolvedPath)
    fetch(resolvedPath)
      .then((r) => {
        text(`${baseSelector}-status`, r.status)
        return r.text()
      })
      .then((data) => {
        text(`${
```

--------------------------------

### Importing an Inline Worker

Source: https://github.com/vitejs/vite/blob/main/playground/worker/index.html

Importing an inline worker module for the .pong-inline test case.

```javascript
import InlineWorker from '../my-worker?worker&inline'
```

--------------------------------

### Configure Preview Port

Source: https://github.com/vitejs/vite/blob/main/docs/guide/static-deploy.md

Sets a custom port for the local preview server via the package.json scripts.

```json
{
  "scripts": {
    "preview": "vite preview --port 8080"
  }
}
```

--------------------------------

### Generate HTML tags for specific entry points

Source: https://github.com/vitejs/vite/blob/main/docs/guide/backend-integration.md

Examples of generated HTML tags for different entry points in production. Follow the recommended order of stylesheets, scripts, and preloads for optimal performance.

```html
<link rel="stylesheet" href="assets/foo-5UjPuW-k.css" />
<link rel="stylesheet" href="assets/shared-ChJ_j-JJ.css" />
<script type="module" src="assets/foo-BRBmoGS9.js"></script>
<!-- optional -->
<link rel="modulepreload" href="assets/shared-B7PI925R.js" />
```

```html
<link rel="stylesheet" href="assets/shared-ChJ_j-JJ.css" />
<script type="module" src="assets/bar-gkvgaI9m.js"></script>
<!-- optional -->
<link rel="modulepreload" href="assets/shared-B7PI925R.js" />
```

--------------------------------

### Conditional Exports in package.json

Source: https://github.com/vitejs/vite/blob/main/docs/config/shared-options.md

Example of an exports field in package.json used to define entry points for different module systems.

```json
{
  "exports": {
    ".": {
      "import": "./index.mjs",
      "require": "./index.js"
    }
  }
}
```

--------------------------------

### Configure Multi-Page App Entry Points in Vite

Source: https://github.com/vitejs/vite/blob/main/docs/guide/build.md

Defines multiple HTML files as entry points for multi-page builds. If specifying a custom root directory, include the root entry path in the `resolve` arguments.

```javascript
import { resolve } from 'node:path'
import { defineConfig } from 'vite'

export default defineConfig({
  input: {
    main: resolve(import.meta.dirname, 'index.html'),
    nested: resolve(import.meta.dirname, 'nested/index.html'),
  },
})
```

--------------------------------

### Instantiate per-environment plugins with helper in Vite (JavaScript)

Source: https://github.com/vitejs/vite/blob/main/docs/guide/api-environment-plugins.md

Use `perEnvironmentPlugin` as a shorthand helper to instantiate unique plugin instances per environment when no other hooks are required.

```javascript
import { nonShareablePlugin } from 'non-shareable-plugin'

export default defineConfig({
  plugins: [
    perEnvironmentPlugin('per-environment-plugin', (environment) =>
      nonShareablePlugin({ outputName: environment.name }),
    ),
  ],
})
```

--------------------------------

### Self-Referencing URL Worker

Source: https://github.com/vitejs/vite/blob/main/playground/worker/index.html

Instantiating a worker that references itself via URL, for the .self-reference-url-worker test case.

```javascript
new Worker(new URL('../self-reference-url-worker.js', import.meta.url))
```

--------------------------------

### Test dependency imports and browser field mappings

Source: https://github.com/vitejs/vite/blob/main/playground/optimize-deps/index.html

Demonstrates importing various dependencies, including those with browser field mappings, linked packages, and globbed files.

```javascript
import { astro, okay, scss, tsx, } from '@vitejs/test-dep-cjs-external-package-omit-js-suffix'

text('.external-package-js', okay())
text('.external-package-scss-js', scss())
text('.external-package-astro-js', astro())
text('.external-package-tsx-js', tsx())

function text(el, text) {
  document.querySelector(el).textContent = text
}

// test dep detection in globbed files
const globbed = import.meta.glob('./glob/*.js', { eager: true })

import cjsBrowserFieldBare from '@vitejs/test-dep-cjs-browser-field-bare'
text('.cjs-browser-field-bare', cjsBrowserFieldBare)

import { camelCase } from '@vitejs/test-dep-linked'
text('.deps-linked', camelCase('foo-bar-baz'))

import { msg, VueSFC } from '@vitejs/test-dep-linked-include'
text('.force-include', msg)
text('.externalize-known-non-js', VueSFC.render())

import * as linked from '@vitejs/test-dep-linked-include'
const keys =
```

--------------------------------

### Classic Shared Worker Instantiation

Source: https://github.com/vitejs/vite/blob/main/playground/worker/index.html

Instantiating a classic shared worker, for the .classic-shared-worker test case.

```javascript
new SharedWorker(new URL('./classic-shared-worker.js', import.meta.url), { type: 'classic' })
```

--------------------------------

### Manually instantiate WebAssembly using the ?init query in JavaScript

Source: https://github.com/vitejs/vite/blob/main/docs/guide/features.md

Use `?init` when explicit control over instantiation timing is required. The default export returns a Promise resolving to a `WebAssembly.Instance`.

```js
import 'vite/client'
// ---cut---
import init from './example.wasm?init'

init().then((instance) => {
  instance.exports.test()
})
```

--------------------------------

### Importing a Shared Worker

Source: https://github.com/vitejs/vite/blob/main/playground/worker/index.html

Importing a named shared worker module for the .tick-count test case.

```javascript
import mySharedWorker from '../my-shared-worker?sharedworker&name=shared'
```

--------------------------------

### Glob Import with Base Option

Source: https://github.com/vitejs/vite/blob/main/playground/glob-import/root/index.html

This example uses `import.meta.glob` with the `base` option to specify a different root directory for the glob pattern. It imports raw JSON files eagerly from the specified base path, then parses and displays their content.

```javascript
const baseModules = import.meta.glob('./*.json', {
  query: '?raw',
  eager: true,
  import: 'default',
  base: './dir',
})
const globBase = {}
Object.keys(baseModules).forEach((key) => {
  globBase[key] = JSON.parse(baseModules[key])
})
document.querySelector('.result-base').textContent = JSON.stringify(
  globBase,
  null,
  2,
)
```

--------------------------------

### Raw Glob Import for JSON Files

Source: https://github.com/vitejs/vite/blob/main/playground/glob-import/root/index.html

This example uses `import.meta.glob` with `query: '?raw'` to import JSON files as raw strings. The `eager: true` and `import: 'default'` options ensure immediate loading and direct access to the raw content.

```javascript
const rawModules = import.meta.glob('/dir/*.json', {
  query: '?raw',
  eager: true,
  import: 'default',
})
const globraw = {}
Object.keys(rawModules).forEach((key) => {
  globraw[key] = JSON.parse(rawModules[key])
})
document.querySelector('.globraw').textContent = JSON.stringify(
  globraw,
  null,
  2,
)
```

--------------------------------

### Deep Import Resolution with Babel

Source: https://github.com/vitejs/vite/blob/main/playground/resolve/index.html

Shows resolution of deep paths within external packages using a generator function example.

```javascript
// deep import import slicedToArray from '@babel/runtime/helpers/esm/slicedToArray' const iterable = (function* () { yield 2 yield 4 yield 6 yield 8 })() text('.deep-import', JSON.stringify(slicedToArray(iterable, 2)))
```

--------------------------------

### Overriding NODE_ENV for Development Build

Source: https://github.com/vitejs/vite/blob/main/docs/guide/env-and-mode.md

Example of a .env.testing file to force NODE_ENV to development for a specific mode.

```ini
NODE_ENV=development
```

--------------------------------

### Example of tree-shaking with import.meta.env.DEV

Source: https://github.com/vitejs/vite/blob/main/docs/guide/env-and-mode.md

Code demonstrating how `import.meta.env.DEV` can be used for conditional logic that is tree-shaken in production builds.

```js
if (import.meta.env.DEV) {
  // code inside here will be tree-shaken in production builds
  console.log('Dev mode')
}
```

--------------------------------

### Scaffold a project with specific template

Source: https://github.com/vitejs/vite/blob/main/docs/guide/index.md

Specify the project name and template directly via command line arguments to bypass interactive prompts.

```bash
# npm 7+, extra double-dash is needed:
$ npm create vite@latest my-vue-app -- --template vue
```

```bash
$ yarn create vite my-vue-app --template vue
```

```bash
$ pnpm create vite my-vue-app --template vue
```

```bash
$ bun create vite my-vue-app --template vue
```

```bash
$ deno init --npm vite my-vue-app --template vue
```