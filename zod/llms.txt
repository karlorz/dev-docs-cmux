### Install Zod with various package managers

Source: https://github.com/colinhacks/zod/blob/main/packages/docs-v3/home.md

Install Zod using npm, Deno, Yarn, Bun, or pnpm.

```sh
npm install zod       # npm
deno add npm:zod      # deno
yarn add zod          # yarn
bun add zod           # bun
pnpm add zod          # pnpm
```

--------------------------------

### New transformer syntax

Source: https://github.com/colinhacks/zod/blob/main/packages/docs-v3/MIGRATION.md

Example of the new `.transform()` method available on all Zod schemas.

```typescript
z.string().transform((val) => val.length);
```

--------------------------------

### Inspect URL Normalization Example

Source: https://github.com/colinhacks/zod/blob/main/packages/docs/content/api.mdx

Demonstrates the output of standard WHATWG URL normalization.

```ts
new URL("HTTP://ExAmPle.com:80/./a/../b?X=1#f oo").href
// => "http://example.com/b?X=1#f%20oo"
```

--------------------------------

### Install Zod with npm

Source: https://github.com/colinhacks/zod/blob/main/packages/docs/content/blog/introducing-z-compile.mdx

Installs Zod 4.5.0 or compatible versions. Import zod/compile or call z.compile() after installation to enable schema compilation.

```sh
npm install zod@^4.5.0
```

--------------------------------

### Install latest Zod release using npm

Source: https://github.com/colinhacks/zod/blob/main/packages/docs/content/blog/zod-4-5.mdx

Run this command to upgrade or install the latest release of Zod.

```sh
npm install zod@latest
```

--------------------------------

### Peer Dependency Configuration

Source: https://github.com/colinhacks/zod/blob/main/packages/docs/content/library-authors.mdx

Example of adding Zod as a peer dependency in package.json.

```json
// package.json
{
  // ...
  "peerDependencies": {
    "zod": "^4.0.0"
  }
}
```

--------------------------------

### Install Zod with npm

Source: https://github.com/colinhacks/zod/blob/main/README.md

Add Zod to your project dependencies using npm.

```sh
npm install zod
```

--------------------------------

### Install Zod for Zod Mini

Source: https://github.com/colinhacks/zod/blob/main/packages/docs/content/packages/mini.mdx

Installs Zod v4 to use the built-in `zod/mini` subpath export.

```sh
npm install zod@^4.0.0
```

--------------------------------

### Install Zod with npm

Source: https://github.com/colinhacks/zod/blob/main/packages/docs/content/index.mdx

Install the Zod package from npm into your project dependencies.

```sh
npm install zod
```

--------------------------------

### Install zod and @zod/mini with npm

Source: https://github.com/colinhacks/zod/blob/main/packages/mini/README.md

Requires zod as a peer dependency alongside @zod/mini.

```sh
npm install zod @zod/mini
```

--------------------------------

### Importing Zod 4 Core

Source: https://github.com/colinhacks/zod/blob/main/packages/docs/content/library-authors.mdx

Example of importing the Zod 4 core package using its permalink subpath.

```ts
import * as z4 from "zod/v4/core";
```

--------------------------------

### Dev Server

Source: https://github.com/colinhacks/zod/blob/main/CONTRIBUTING.md

Command to start the development server for Zod documentation.

```sh
pnpm run --filter=@zod/docs dev
```

--------------------------------

### Parse Primitive Schemas for Bundle Comparison

Source: https://github.com/colinhacks/zod/blob/main/packages/docs/content/packages/mini.mdx

A minimal example used to evaluate bundle size reduction differences.

```ts
z.boolean().parse(true)
```

--------------------------------

### Parse Object Schemas for Bundle Comparison

Source: https://github.com/colinhacks/zod/blob/main/packages/docs/content/packages/mini.mdx

A multi-property object schema example used to benchmark bundle size differences.

```ts
const schema = z.object({ a: z.string(), b: z.number(), c: z.boolean() });

schema.parse({
  a: "asdf",
  b: 123,
  c: true,
});
```

--------------------------------

### Peer and Dev Dependency Configuration

Source: https://github.com/colinhacks/zod/blob/main/packages/docs/content/library-authors.mdx

Example of adding Zod as both a peer and dev dependency in package.json for development purposes.

```json
// package.json
{
  "peerDependencies": {
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "zod": "^4.0.0"
  }
}
```

--------------------------------

### Widen peer dependency for Zod 3 and Zod 4

Source: https://github.com/colinhacks/zod/blob/main/packages/docs/content/library-authors.mdx

Example of `package.json` showing how to widen the `peerDependencies` to support both Zod 3 (from `3.25.0`) and Zod 4.

```json
// package.json
{
  // ...
  "peerDependencies": {
    "zod": "^3.25.0 || ^4.0.0"
  }
}
```

--------------------------------

### Creating a ZodError Instance

Source: https://github.com/colinhacks/zod/blob/main/packages/docs-v3/ERROR_HANDLING.md

Example of how to manually create a new instance of ZodError.

```ts
import * as z from "zod";

const myError = new z.ZodError([]);
```

--------------------------------

### Configure runtime preload in nub.jsonc

Source: https://github.com/colinhacks/zod/blob/main/packages/docs/content/compile.mdx

Specify `zod/compile` inside the runtime preload array to enable compilation on application start.

```jsonc
{
  "preload": ["zod/compile"]
}
```

--------------------------------

### Differentiating Zod 3 and Zod 4 schemas at runtime

Source: https://github.com/colinhacks/zod/blob/main/packages/docs/content/library-authors.mdx

Code example showing how to check for the `_zod` property to determine if a schema is Zod 3 or Zod 4.

```ts
import type * as z3 from "zod/v3";
import type * as z4 from "zod/v4/core";

declare const schema: z3.ZodTypeAny | z4.$ZodType;

if ("_zod" in schema) {
  schema._zod.def; // Zod 4 schema
} else {
  schema._def; // Zod 3 schema
}
```

--------------------------------

### Interleaving transformations and refinements

Source: https://github.com/colinhacks/zod/blob/main/packages/docs-v3/MIGRATION.md

Demonstrates how transformations and refinements can be interleaved using the dedicated 'ZodEffects' class.

```typescript
const test = z
    .string()
    .transform((val) => val.length)
    .refine((val) => val > 5, { message: "Input is too short" })
    .transform((val) => val * 2);

test.parse("12characters"); // => 24
```

--------------------------------

### User code demonstrating Zod and Zod Mini usage

Source: https://github.com/colinhacks/zod/blob/main/packages/docs/content/library-authors.mdx

Example of user code calling a library function with schemas from both Zod and Zod Mini, showing simultaneous support.

```ts
// user code
import { acceptObjectSchema } from "your-library";

// Zod 4
import * as z from "zod";
acceptObjectSchema(z.object({ name: z.string() }));

// Zod 4 Mini
import * as zm from "zod/mini";
acceptObjectSchema(zm.object({ name: zm.string() }))
```

--------------------------------

### import "zod/compile"

Source: https://github.com/colinhacks/zod/blob/main/wiki/compile.md

Side-effect import that installs a global post-processor wrapping every newly constructed schema with a one-shot lazy compile shim.

```APIDOC
## Module Import: import "zod/compile"

### Description
Installs a global post-processor that automatically wraps every newly constructed schema with a one-shot lazy compile shim for project-wide opt-in.

### Usage
```ts
import "zod/compile";
```
```

--------------------------------

### Traversing Zod Schemas

Source: https://github.com/colinhacks/zod/blob/main/packages/docs/content/packages/core.mdx

Example of how to traverse Zod schemas using the `_zod.def` property to discriminate between types.

```ts
export function walk(_schema: z.$ZodType) {
  const schema = _schema as z.$ZodTypes;
  const def = schema._zod.def;
  switch (def.type) {
    case "string": {
      // ...
      break;
    }
    case "object": {
      // ...
      break;
    }
  }
}
```

--------------------------------

### Registering, looking up, and removing schemas

Source: https://github.com/colinhacks/zod/blob/main/packages/docs/content/metadata.mdx

Shows how to add, check for, get, remove, and clear schemas in a registry.

```ts
const mySchema = z.string();

myRegistry.add(mySchema, { description: "A cool schema!"});
myRegistry.has(mySchema); // => true
myRegistry.get(mySchema); // => { description: "A cool schema!" }
myRegistry.remove(mySchema);
myRegistry.clear(); // wipe registry
```

--------------------------------

### Library code for Zod and Zod Mini compatibility

Source: https://github.com/colinhacks/zod/blob/main/packages/docs/content/library-authors.mdx

Example of library code importing from `zod/v4/core` to ensure compatibility with both Zod and Zod Mini.

```ts
// library code
import * as z4 from "zod/v4/core";

export function acceptObjectSchema<T extends z4.$ZodObject>(schema: T){
  // parse data
  z4.parse(schema, { /* somedata */});
  // inspect internals
  schema._zod.def.shape;
}
```

--------------------------------

### Constraining the inferred output type of the input schema

Source: https://github.com/colinhacks/zod/blob/main/packages/docs/content/library-authors.mdx

Example of how to constrain the inferred output type of the input schema, ensuring type compatibility.

```ts

import * as z4 from "zod/v4/core";

// only accepts string schemas
function inferSchema<T extends z4.$ZodType<string>>(schema: T) {
  return schema;
}

inferSchema(z.string()); // ✅ 

inferSchema(z.number()); 
// ❌ The types of '_zod.output' are incompatible between these types. 
// // Type 'number' is not assignable to type 'string'
```

--------------------------------

### Iterate on Contributor PR via Git Worktree in Bash

Source: https://github.com/colinhacks/zod/blob/main/AGENTS.md

Fetch an open contributor PR into an isolated worktree, inspect PR head metadata using GitHub CLI, and push updates back to the contributor's fork. Ensures the main working directory remains clean and dependencies are installed against the shared pnpm store.

```bash
# 1. Fetch the PR as a local branch and create a worktree for it
git fetch origin pull/<N>/head:pr-<N>
git worktree add ~/.cursor/worktrees/zod/pr-<N> pr-<N>
cd ~/.cursor/worktrees/zod/pr-<N>
pnpm install --frozen-lockfile   # fast, pnpm store is shared across worktrees

# 2. Look up the PR's head info — you'll need the contributor's fork URL
#    and the head ref name to push back.
gh pr view <N> --repo colinhacks/zod \
  --json headRefName,headRepositoryOwner,maintainerCanModify

# 3. If maintainerCanModify is true, add the fork as a remote and push to
#    the PR's head ref (NOT to your local branch name).
git remote add <contributor> git@github.com:<contributor>/zod.git
git push <contributor> pr-<N>:<headRefName>                 # first push
git push <contributor> pr-<N>:<headRefName> --force-with-lease   # for amends
```

--------------------------------

### `json(schema)` Usage Example

Source: https://github.com/colinhacks/zod/blob/main/packages/docs/content/codecs.mdx

Demonstrates how to use the `jsonCodec` with a specific Zod schema to decode JSON strings into objects and encode objects back into JSON strings, including error handling for invalid input.

```ts
const jsonToObject = jsonCodec(z.object({ name: z.string(), age: z.number() }));

jsonToObject.decode('{"name":"Alice","age":30}');  
// => { name: "Alice", age: 30 }

jsonToObject.encode({ name: "Bob", age: 25 });     
// => '{"name":"Bob","age":25}'

jsonToObject.decode('~~invalid~~'); 
// ZodError: [
//   {
//     "code": "invalid_format",
//     "format": "json",
//     "path": [],
//     "message": "Unexpected token '~', \"~~invalid~~\" is not valid JSON"
//   }
// ]
```

--------------------------------

### Old transformer syntax (not available)

Source: https://github.com/colinhacks/zod/blob/main/packages/docs-v3/MIGRATION.md

Examples of the old transformer syntax that is no longer available in Zod 3.

```typescript
# not available
z.transformer(A, B, func);
A.transform(B, func)
```

--------------------------------

### Preload compilation in runtime configuration

Source: https://github.com/colinhacks/zod/blob/main/packages/docs/content/blog/introducing-z-compile.mdx

Configure preload settings in configuration files such as bunfig.toml or nub.jsonc.

```jsonc
{
  "preload": ["zod/compile"]
}
```

--------------------------------

### Validate UUID-Like GUIDs

Source: https://github.com/colinhacks/zod/blob/main/packages/docs/content/api.mdx

Validates UUID-like identifiers without enforcing RFC 9562/4122 byte constraints.

```ts
z.guid();
```

--------------------------------

### z.prettifyError() usage

Source: https://github.com/colinhacks/zod/blob/main/packages/docs/content/error-formatting.mdx

Shows how to use z.prettifyError() to get a human-readable string representation of the error.

```ts
const pretty = z.prettifyError(result.error);
```

--------------------------------

### Using .register() method

Source: https://github.com/colinhacks/zod/blob/main/packages/docs/content/metadata.mdx

Shows how to use the .register() method on a schema to add it to a registry.

```ts
const mySchema = z.string();

mySchema.register(myRegistry, { description: "A cool schema!" });
// => mySchema
```

--------------------------------

### Discriminating Zod Checks

Source: https://github.com/colinhacks/zod/blob/main/packages/docs/content/packages/core.mdx

Example of using a switch statement on ._zod.def.check to discriminate between different $ZodChecks.

```ts
const check = {} as z.$ZodChecks;
const def = check._zod.def;

switch (def.check) {
  case "less_than":
  case "greater_than":
    // ...
    break;
}
```

--------------------------------

### Async Codec Definition

Source: https://github.com/colinhacks/zod/blob/main/packages/docs/content/codecs.mdx

Example of defining an asynchronous codec with `decode` and `encode` functions that return Promises.

```ts
const asyncCodec = z.codec(z.string(), z.number(), {
  decode: async (str) => Number(str),
  encode: async (num) => num.toString()
});
```

--------------------------------

### Inverting a Codec

Source: https://github.com/colinhacks/zod/blob/main/packages/docs/content/codecs.mdx

Example of using `z.invertCodec()` to create a codec that reverses the input and output schemas and transformations.

```typescript
const dateToString = z.invertCodec(stringToDate);

dateToString.decode(new Date("2024-01-15T10:30:00.000Z"));
// => string

dateToString.encode("2024-01-15T10:30:00.000Z");
// => Date
```

```typescript
const dateToString = z.invertCodec(stringToDate);

z.decode(dateToString, new Date("2024-01-15T10:30:00.000Z"));
// => string

z.encode(dateToString, "2024-01-15T10:30:00.000Z");
// => Date
```

--------------------------------

### Defining a Codec

Source: https://github.com/colinhacks/zod/blob/main/packages/docs/content/codecs.mdx

Example of defining a codec to transform an ISO date string to a Date object and vice-versa.

```typescript
const stringToDate = z.codec(
  z.iso.datetime(),  // input schema: ISO date string
  z.date(),          // output schema: Date object
  {
    decode: (isoString) => new Date(isoString), // ISO string → Date
    encode: (date) => date.toISOString(),       // Date → ISO string
  }
);
```

--------------------------------

### Preloading zod/compile with Node.js CLI flag

Source: https://github.com/colinhacks/zod/blob/main/packages/docs/content/blog/zod-4-5.mdx

Ensures zod/compile runs before any application modules are evaluated in Node.js.

```sh
node --import zod/compile app.js
```

--------------------------------

### Importing Zod 3 and Zod 4 side-by-side

Source: https://github.com/colinhacks/zod/blob/main/packages/docs/content/library-authors.mdx

Demonstrates how to import Zod 3 and Zod 4 from their respective subpaths to use them together in a library.

```ts
import * as z3 from "zod/v3";
import * as z4 from "zod/v4/core";

type Schema = z3.ZodTypeAny | z4.$ZodType;

function acceptUserSchema(schema: z3.ZodTypeAny | z4.$ZodType) {
  // ...
}
```

--------------------------------

### z.flattenError() usage

Source: https://github.com/colinhacks/zod/blob/main/packages/docs/content/error-formatting.mdx

Shows how to use z.flattenError() to get a shallow error object with formErrors and fieldErrors.

```ts
const flattened = z.flattenError(result.error);
// { errors: string[], properties: { [key: string]: string[] } }

{
  formErrors: [ 'Unrecognized key: "extraKey"' ],
  fieldErrors: {
    username: [ 'Invalid input: expected string, received number' ],
    favoriteNumbers: [ 'Invalid input: expected number, received string' ]
  }
}
```

--------------------------------

### .describe() method (Zod Mini)

Source: https://github.com/colinhacks/zod/blob/main/packages/docs/content/metadata.mdx

Demonstrates using `z.describe()` and its equivalence to `z.meta()` for registering a schema description in Zod Mini.

```ts
const emailSchema = z.email().check(z.describe("An email address"));

// equivalent to
z.email().check(z.meta({ description: "An email address" }));
```

--------------------------------

### Parsing data with Zod Core functions

Source: https://github.com/colinhacks/zod/blob/main/packages/docs/content/library-authors.mdx

Demonstrates how to parse data using the top-level `z4.parse` function from Zod Core, as subclass methods are not available.

```ts
function parseData<T extends z4.$ZodType>(data: unknown, schema: T): z4.output<T> {
  return z.parse(schema, data);
}

parseData("sup", z.string());
// => string
```

--------------------------------

### Enable schema compilation in application entrypoint

Source: https://github.com/colinhacks/zod/blob/main/packages/docs/content/blog/introducing-z-compile.mdx

Import at the application entrypoint before any schemas are defined to enable automatic compile-by-default.

```ts
// in your entrypoint (or before any schemas are defined)
import "zod/compile";
```

--------------------------------

### .describe() method (Zod)

Source: https://github.com/colinhacks/zod/blob/main/packages/docs/content/metadata.mdx

Demonstrates using `.describe()` and its equivalence to `.meta()` for registering a schema description in Zod.

```ts
const emailSchema = z.email();
emailSchema.describe("An email address");

// equivalent to
emailSchema.meta({ description: "An email address" });
```

--------------------------------

### Generic registry without metadata type

Source: https://github.com/colinhacks/zod/blob/main/packages/docs/content/metadata.mdx

Example of using a registry as a generic collection when no metadata type is defined.

```ts
const myRegistry = z.registry();

myRegistry.add(z.string());
myRegistry.add(z.number());
```

--------------------------------

### Production Build

Source: https://github.com/colinhacks/zod/blob/main/CONTRIBUTING.md

Commands to build Zod documentation for production, requiring a GITHUB_TOKEN.

```sh
export GITHUB_TOKEN=your_token_here # persists in shell session
pnpm run --filter=@zod/docs build
```

--------------------------------

### Import Standalone @zod/mini Package

Source: https://github.com/colinhacks/zod/blob/main/packages/docs/content/packages/mini.mdx

Imports the functional API from the standalone `@zod/mini` package.

```ts
import * as z from "@zod/mini";
```

--------------------------------

### Discriminating String Format Checks

Source: https://github.com/colinhacks/zod/blob/main/packages/docs/content/packages/core.mdx

Example of using a nested switch statement to discriminate between different string format checks.

```ts
const check = {} as z.$ZodChecks;
const def = check._zod.def;

switch (def.check) {
  case "less_than":
  case "greater_than":
  // ...
  case "string_format":
    {
      const formatCheck = check as z.$ZodStringFormatChecks;
      const formatCheckDef = formatCheck._zod.def;

      switch (formatCheckDef.format) {
        case "email":
        case "url":
          // do stuff
      }
    }
    break;
}
```

--------------------------------

### Constraining input schema to a specific subclass

Source: https://github.com/colinhacks/zod/blob/main/packages/docs/content/library-authors.mdx

Example of how to constrain the accepted schema type to a specific Zod subclass, such as `ZodObject`.

```ts

import * as z4 from "zod/v4/core";

// only accepts object schemas
function inferSchema<T extends z4.$ZodObject>(schema: T) {
  return schema;
}
```

--------------------------------

### Creating a simple registry

Source: https://github.com/colinhacks/zod/blob/main/packages/docs/content/metadata.mdx

Demonstrates how to create a Zod registry with a specific metadata type.

```ts
import * as z from "zod";

const myRegistry = z.registry<{ description: string }>();
```

--------------------------------

### Dispatch manual release workflow for @zod/mini using GitHub CLI

Source: https://github.com/colinhacks/zod/blob/main/AGENTS.md

Triggers the release workflow to publish @zod/mini at an existing zod release version. Dispatch runs one version at a time to avoid concurrent publish failures.

```bash
gh workflow run release.yml -f mini_version=4.5.2
```

--------------------------------

### Augmenting GlobalMeta interface

Source: https://github.com/colinhacks/zod/blob/main/packages/docs/content/metadata.mdx

Example of using declaration merging to add new fields to the GlobalMeta interface for the global registry.

```ts
declare module "zod" {
  interface GlobalMeta {
    // add new fields here
    examples?: unknown[];
  }
}

// forces TypeScript to consider the file a module
export {}
```

--------------------------------

### Enable compilation using Node.js CLI flags

Source: https://github.com/colinhacks/zod/blob/main/packages/docs/content/blog/introducing-z-compile.mdx

Guarantees zod/compile executes before any module defines a schema in ESM or CommonJS environments.

```sh
node --import zod/compile app.js   # ESM
node --require zod/compile app.cjs # CommonJS
```

--------------------------------

### Commands

Source: https://github.com/colinhacks/zod/blob/main/CONTRIBUTING.md

Various `pnpm` commands for development and testing.

```sh
pnpm build
```

```sh
pnpm test
```

```sh
pnpm test:watch
```

```sh
pnpm test <file>
```

```sh
pnpm test --filter <ws> <file>
```

```sh
pnpm dev:play
```

--------------------------------

### Unrepresentable Zod Types in JSON Schema

Source: https://github.com/colinhacks/zod/blob/main/packages/docs/content/json-schema.mdx

Examples of Zod types and checks that have no direct JSON Schema analog and will cause errors unless configured otherwise.

```ts
z.bigint(); // ❌
z.int64(); // ❌
z.symbol(); // ❌
z.undefined(); // ❌
z.void(); // ❌
z.date(); // ❌
z.map(); // ❌
z.set(); // ❌
z.transform(); // ❌
z.nan(); // ❌
z.custom(); // ❌
z.number().multipleOf(0); // ❌ the divisor must be finite and non-zero
```

--------------------------------

### Access enum values with .enum property

Source: https://github.com/colinhacks/zod/blob/main/packages/docs-v3/home.md

Use `.enum` on a Zod enum schema to get autocompletion and access an object mapping each enum key to its string value.

```ts
FishEnum.enum.Salmon; // => autocompletes

FishEnum.enum;
/*
=> {
  Salmon: "Salmon",
  Tuna: "Tuna",
  Trout: "Trout",
}
*/
```

--------------------------------

### Using .meta() to register metadata (Zod Mini)

Source: https://github.com/colinhacks/zod/blob/main/packages/docs/content/metadata.mdx

Conveniently registers metadata in z.globalRegistry using the .meta() method in Zod Mini.

```ts
const emailSchema = z.email().check(
  z.meta({
    id: "email_address",
    title: "Email address",
    description: "Please enter a valid email address",
  })
);
```

--------------------------------

### schema.clone(def)

Source: https://github.com/colinhacks/zod/blob/main/packages/docs/content/packages/mini.mdx

Returns an identical clone of the current schema using the provided definition object.

```APIDOC
## schema.clone(def)

### Description
Creates and returns a new schema clone based on the provided definition object.

### Method Signature
`schema.clone(def: this["_zod"]["def"]): ZodMiniType`

### Parameters
- **def** (`ZodTypeDef`) - The definition object, typically retrieved from `schema._zod.def`.

### Usage Example
```ts
import * as z from "zod/mini";

const mySchema = z.string();

mySchema.clone(mySchema._zod.def);
```
```

--------------------------------

### Configure Docsify Site Settings

Source: https://github.com/colinhacks/zod/blob/main/packages/docs-v3/index.html

This snippet defines the `window.$docsify` object, which configures various aspects of the Docsify documentation site, such as the homepage, navigation levels, repository link, router mode, and lists of external links.

```javascript
window.$docsify = {
  // Vercel no longer serves root-level README.md from static deployments.
  homepage: "home.md",
  subMaxLevel: 1,
  maxLevel: 3,
  auto2top: true,
  repo: "colinhacks/zod",
  routerMode: "history",
  crossOriginLinks: [
    "https://github.com/colinhacks/zod/actions?query=branch%3Amain",
    "https://opensource.org/licenses/MIT",
    "https://www.npmjs.com/package/zod",
    "https://discord.gg/KaSRdyX2vc",
    "https://discord.gg/RcG33DQJdf",
    "https://github.com/colinhacks/zod/issues/new",
    "https://twitter.com/colinhacks",
    "https://trpc.io/",
    "https://zod.dev/",
    "https://deno.land/x/zod",
    "https://zod.dev/blog/clerk-fellowship",
    "https://go.clerk.com/hqN4rp7",
    "https://go.clerk.com/Qgor5NQ",
    "https://go.clerk.com/PKHrcwh"
  ],
  noCompileLinks: [
    "https://github.com/colinhacks/zod/actions?query=branch%3Amain",
    "https://opensource.org/licenses/MIT",
    "https://www.npmjs.com/package/zod",
    "https://discord.gg/KaSRdyX2vc",
    "https://discord.gg/RcG33DQJdf",
    "https://github.com/colinhacks/zod/issues/new",
    "https://twitter.com/colinhacks",
    "https://trpc.io/",
    "https://zod.dev/",
    "https://deno.land/x/zod",
    "https://zod.dev/blog/clerk-fellowship",
    "https://go.clerk.com/hqN4rp7",
    "https://go.clerk.com/Qgor5NQ",
    "https://go.clerk.com/PKHrcwh"
  ]
};
```

--------------------------------

### Example ZodError Issues

Source: https://github.com/colinhacks/zod/blob/main/packages/docs-v3/ERROR_HANDLING.md

Shows the structure of the error issues generated by Zod for the improperly formatted data, including code, expected/received types, path, and message.

```ts
[
  {
    code: "invalid_type",
    expected: "string",
    received: "number",
    path: ["names", 1],
    message: "Invalid input: expected string, received number",
  },
  {
    code: "unrecognized_keys",
    keys: ["extra"],
    path: ["address"],
    message: "Unrecognized key(s) in object: 'extra'",
  },
  {
    code: "too_small",
    minimum: 10000,
    type: "number",
    inclusive: true,
    path: ["address", "zipCode"],
    message: "Value should be greater than or equal to 10000",
  },
];
```

--------------------------------

### Register schema in a registry with .register()

Source: https://github.com/colinhacks/zod/blob/main/packages/docs/content/packages/mini.mdx

Associates metadata with a schema inside a custom registry.

```ts
const myReg = z.registry<{title: string}>();

z.string().register(myReg, { title: "My cool string schema" });
```

--------------------------------

### React Form with Zod Error Display

Source: https://github.com/colinhacks/zod/blob/main/packages/docs-v3/ERROR_HANDLING.md

A React example demonstrating how to integrate Zod's `.format()` output to display validation errors next to form fields.

```tsx
const FormData = z.object({ ... });

function Errors(props: {errors?: string[]}){
  if(!props.errors?.length) return null;
  return <div>{props.errors.map(err => <p>{err}</p>)}</div>
}

function MyForm(){
  const {register, data} = useForm({ ... });

  const result = FormData.safeParse(data);
  const errors = result.success ? {} : result.error.format();

  return <div>
    <label>Name<label>
    <input {...register('name')}>
    <Errors errors={errors?.name?._errors} />
  </div>
}
```

--------------------------------

### Using a Codec for Decoding and Encoding

Source: https://github.com/colinhacks/zod/blob/main/packages/docs/content/codecs.mdx

Demonstrates how `decode` and `encode` behave differently when using a codec that diverges input and output types.

```typescript
stringToDate.decode("2024-01-15T10:30:00.000Z")
// => Date

stringToDate.encode(new Date("2024-01-15T10:30:00.000Z"))
// => string
```

```typescript
z.decode(stringToDate, "2024-01-15T10:30:00.000Z")
// => Date

z.encode(stringToDate, new Date("2024-01-15T10:30:00.000Z"))
// => string
```

--------------------------------

### Clone a schema with .clone()

Source: https://github.com/colinhacks/zod/blob/main/packages/docs/content/packages/mini.mdx

Returns a schema clone using the supplied internal definition object.

```ts
const mySchema = z.string()

mySchema.clone(mySchema._zod.def);
```

--------------------------------

### Regenerate Typia benchmark artifact in a temporary build directory

Source: https://github.com/colinhacks/zod/blob/main/packages/bench/typia-case/README.md

Runs in an isolated temporary directory to avoid patching the shared workspace TypeScript installation. Compiles typia-case with ts-patch and copies the resulting bundle back to build/index.cjs.

```sh
mkdir -p /tmp/typia-build/src && cd /tmp/typia-build
npm init -y
npm install typia@9.7.2 typescript@5.9.3 ts-patch@3.3.0
cp <repo>/packages/bench/typia-case/src/index.ts src/index.ts
cp <repo>/packages/bench/typia-case/tsconfig.json tsconfig.json
npx ts-patch install && npx tsc -p tsconfig.json
cp build/index.js <repo>/packages/bench/typia-case/build/index.cjs
```

--------------------------------

### Enable compilation using Node.js CLI flags

Source: https://github.com/colinhacks/zod/blob/main/packages/docs/content/compile.mdx

Pass `--import` or `--require` flags when launching Node.js to guarantee compilation is enabled prior to module evaluation.

```shell
node --import zod/compile app.js   # ESM
node --require zod/compile app.cjs # CommonJS
```

--------------------------------

### .promise()

Source: https://github.com/colinhacks/zod/blob/main/packages/docs-v3/home.md

Convenience method for creating promise schemas. Equivalent to calling z.promise(schema).

```APIDOC
### .promise()

#### Description
A convenience method for promise types.

#### Signature
`schema.promise()`

#### Usage Example
```ts
const stringPromise = z.string().promise(); // Promise<string>

// equivalent to
z.promise(z.string());
```
```

--------------------------------

### .pick()

Source: https://github.com/colinhacks/zod/blob/main/packages/docs/content/api.mdx

Creates a new schema by picking specific keys from an existing object schema, similar to TypeScript's Pick utility type.

```APIDOC
### `.pick()` / `z.pick()`

#### Description
Picks certain keys from an object schema to create a new narrowed schema.

#### Signature
- **Method**: `schema.pick(keys)`
- **Function (Zod Mini)**: `z.pick(schema, keys)`

#### Parameters
- `keys` (Record<string, true>) - An object mapping keys to `true` indicating which properties to retain.

#### Examples
```ts
const Recipe = z.object({
  title: z.string(),
  description: z.string().optional(),
  ingredients: z.array(z.string()),
});

// Zod
const JustTheTitle = Recipe.pick({ title: true });

// Zod Mini
const JustTheTitle = z.pick(Recipe, { title: true });
```
```

--------------------------------

### Enable global schema compilation via import

Source: https://github.com/colinhacks/zod/blob/main/packages/docs/content/compile.mdx

Import `zod/compile` before defining schemas to lazily compile every schema on its first parse.

```typescript
import "zod/compile"; // must come before modules that define schemas
import * as z from "zod";

const schema = z.object({ name: z.string() });
schema.parse({ name: "ok" }); // compiled on first parse
```

--------------------------------

### z.url(params?) / z.httpUrl()

Source: https://github.com/colinhacks/zod/blob/main/packages/docs/content/api.mdx

Validates WHATWG-compatible URLs with configurable protocol, hostname patterns, or normalization options.

```APIDOC
## z.url([params]) / z.httpUrl()

### Description
Validates URLs using the WHATWG `URL` constructor. Offers parameters to restrict hostnames and protocols, or to validate web URLs specifically.

### Signature
```ts
z.url(params?: { hostname?: RegExp; protocol?: RegExp; normalize?: boolean }): ZodString
z.httpUrl(): ZodString
```

### Parameters
- **hostname** (`RegExp`, optional) - Regular expression to constrain the URL hostname.
- **protocol** (`RegExp`, optional) - Regular expression to constrain the URL protocol.
- **normalize** (`boolean`, optional) - Overwrites the input value with the normalized URL href.

### Usage Example
```ts
const urlSchema = z.url();
urlSchema.parse("https://example.com");

const httpsOnly = z.url({ protocol: /^https$/ });
const domainOnly = z.url({ hostname: /^example\.com$/ });

// Web URLs (restricts protocol to http/https and validates domain hostname)
const webUrl = z.httpUrl();
```
```

--------------------------------

### Implement Dark Theme Toggling with System Preference

Source: https://github.com/colinhacks/zod/blob/main/packages/docs-v3/index.html

This JavaScript code provides functions to check and toggle the dark theme based on the user's system preferences. It applies the 'dark' class to the body and sets a 'data-theme' attribute on the document's root element.

```javascript
localStorage.removeItem("dark-theme");
function checkDark() {
  return (
    // localStorage.getItem("dark-theme") === "true" ??
    window.matchMedia("(prefers-color-scheme: dark)").matches ?? false
  );
}
let isDark = checkDark();
toggleDarkTheme(isDark);
window
  .matchMedia("(prefers-color-scheme: dark)")
  .addEventListener("change", (e) => toggleDarkTheme(e.matches()));
// document.querySelector(".theme-btn").addEventListener("click", () => {
//   const isDark = checkDark();
//   console.log("isDark", isDark);
//   toggleDarkTheme(!isDark);
// });
function toggleDarkTheme(isDark) {
  if (isDark) {
    document.body.classList.add("dark");
  } else {
    document.body.classList.remove("dark");
  }
  document.documentElement.setAttribute(
    "data-theme",
    isDark ? "dark" : "light"
  );
  // localStorage.setItem("dark-theme", isDark ? "true" : "false");
}
```

--------------------------------

### Verify Successful Branch Creation in Git Push Output

Source: https://github.com/colinhacks/zod/blob/main/AGENTS.md

Verify that the command output indicates a new remote branch was created rather than updating main. If the destination ref indicates main, revert or abort immediately.

```text
* [new branch]      <branch> -> <branch>
```

--------------------------------

### Chain schemas into pipes in Zod and Zod Mini

Source: https://github.com/colinhacks/zod/blob/main/packages/docs/content/api.mdx

Chains schemas together using pipes in conjunction with transforms. Use the method syntax in standard Zod or the functional wrapper in Zod Mini.

```ts
const stringToLength = z.string().pipe(z.transform(val => val.length));

stringToLength.parse("hello"); // => 5
```

```ts
const stringToLength = z.pipe(z.string(), z.transform(val => val.length));

z.parse(stringToLength, "hello"); // => 5
```

--------------------------------

### z.compile(schema, options?)

Source: https://github.com/colinhacks/zod/blob/main/wiki/compile.md

Eagerly compiles a schema to an AOT-compiled fast path. Returns a cloned schema that functions as a normal ZodType while falling back to the standard runtime parser on failure.

```APIDOC
## Function: z.compile(schema, options?)

### Description
Eagerly compiles a schema into a cloned schema that runs an AOT-compiled fast path, falling back to the runtime parser on failure. Derivations construct new schemas and do not inherit the fast path, so compile the final schema.

### Parameters
- **schema** (`ZodType`) - Required - The Zod schema to compile.
- **options** (`object`) - Optional - Configuration options for compilation.
  - **strict** (`boolean`) - Optional - When set to `true`, raises refusal errors if the schema cannot be compiled instead of falling back to runtime parsing.

### Return Value
- **Returns** (`ZodType`) - A cloned schema with an AOT-compiled fast path.
```

--------------------------------

### Run benchmark build

Source: https://github.com/colinhacks/zod/blob/main/packages/resolution/README.md

Command to run `tsc` with `extendedDiagnostics` for performance testing.

```shell
npm run build-bench
```

--------------------------------

### z.iso.time([options])

Source: https://github.com/colinhacks/zod/blob/main/packages/docs/content/api.mdx

Validates ISO time strings in HH:MM[:SS[.s+]] format with configurable precision.

```APIDOC
### Function Signature
`z.iso.time(options?: { precision?: number }): ZodType<string>`

### Description
Validates strings in the format `HH:MM[:SS[.s+]]`. Seconds and sub-second decimals are optional by default. Offsets and `Z` specifiers are not allowed.

### Parameters
- `options.precision` (number, optional) - Constrains decimal precision: `-1` for minute precision (`HH:MM`), `0` for second precision (`HH:MM:SS`), or `1` through `3` for decisecond, centisecond, or millisecond precision.

### Example
```ts
const time = z.iso.time();
time.parse("03:15"); // ✅
time.parse("03:15:00"); // ✅
time.parse("03:15:00.9999999"); // ✅

const millisecondTime = z.iso.time({ precision: 3 });
millisecondTime.parse("03:15:00.123"); // ✅
```
```

--------------------------------

### z.file()

Source: https://github.com/colinhacks/zod/blob/main/packages/docs/content/api.mdx

Creates a schema to validate File instances. Supports constraints on file size (bytes) and accepted MIME types using standard Zod methods or Zod Mini checks.

```APIDOC
## z.file()

### Description
Creates a schema to validate `File` instances with optional size limits and MIME type constraints.

### Syntax
```ts
z.file()
```

### Methods & Constraints

#### Standard (Zod)
- **.min(bytes: number)** - Sets minimum `.size` in bytes.
- **.max(bytes: number)** - Sets maximum `.size` in bytes.
- **.mime(mimeType: string | string[])** - Restricts the file to specific MIME type(s).

#### Zod Mini
- **.check(z.minSize(bytes: number))** - Validates minimum `.size` in bytes.
- **.check(z.maxSize(bytes: number))** - Validates maximum `.size` in bytes.
- **.check(z.mime(mimeType: string | string[]))** - Validates MIME type(s).

### Example
```ts
// Zod
const fileSchema = z.file();
fileSchema.min(10_000);
fileSchema.max(1_000_000);
fileSchema.mime("image/png");
fileSchema.mime(["image/png", "image/jpeg"]);

// Zod Mini
const fileSchemaMini = z.file();
fileSchemaMini.check(z.minSize(10_000));
fileSchemaMini.check(z.maxSize(1_000_000));
fileSchemaMini.check(z.mime("image/png"));
fileSchemaMini.check(z.mime(["image/png", "image/jpeg"]));
```
```

--------------------------------

### Evaluate optionality and absence handling in Zod schemas

Source: https://github.com/colinhacks/zod/blob/main/wiki/optionality.md

Shows how Zod processes missing keys versus undefined values across pipelines, coercion, exactOptional, and catch/preprocess divergences.

```typescript
z.object({ a: z.unknown().transform((v) => String(v ?? "X")).pipe(z.string()) }).parse({})
// → THROW  (outer pipe.optin = inner pipe.optin = unknown.optin = undefined — transform is
//   on the OUT side of the inner pipe, which is on the IN side of the outer pipe; the leading
//   `z.unknown()` drives optin and it's not "optional")
//
//   Compare to z.preprocess(fn, T):
//     z.preprocess(fn, T) === pipe(transform(fn), T)         — transform IS def.in of the only pipe
//     z.unknown().transform(fn).pipe(T) === pipe(pipe(unknown, transform), T)  — there's an
//                                                                                inner pipe with
//                                                                                z.unknown() on
//                                                                                the in side
//
//   Preprocess accepts absent because its leading position is the transform itself.
//   The unknown.transform.pipe(T) shape doesn't, because its leading position is z.unknown().
//   If you want preprocess-like absence-handling, use z.preprocess; if you specifically want
//   strict input typing on the leading slot, the unknown.transform.pipe shape gives that.

// === Coerce / unknown / any (intentionally strict on absent) ===

z.object({ a: z.coerce.string() }).parse({})
// → THROW  (coerce.string.optin = undefined; object rejects absent key)

z.object({ a: z.unknown() }).parse({})
// → THROW  (unknown.optin = undefined; soundness fix from 4.4)

z.object({ a: z.any() }).parse({})
// → THROW  (same)

// === exactOptional (delegates instead of short-circuiting) ===

z.object({ a: z.coerce.string().exactOptional() }).parse({})
// → {}  (absent + middle rung: the object drops what coerce made of undefined)

z.object({ a: z.coerce.string().exactOptional() }).parse({ a: undefined })
// → { a: "undefined" }  (present: the inner runs and its answer stands)

z.object({ a: z.string().exactOptional() }).parse({ a: undefined })
// → THROW  (present: string rejects undefined, and the object surfaces it)

z.object({ a: z.string().default("x").exactOptional() }).parse({})
// → { a: "x" }  (top rung substitutes, so the gate doesn't fire)

z.tuple([z.string(), z.coerce.string().exactOptional()]).parse(["x"])
// → ["x"]  (the tuple analog: truncate rather than materialize)

// === Static type vs runtime divergence ===

z.input<typeof z.object({ a: z.string().catch("c") })>
// → { a: string }   — `a` is required at the type level

z.object({ a: z.string().catch("c") }).parse({})
// → { a: "c" }      — but accepts {} at runtime

z.input<typeof z.object({ a: z.preprocess(fn, T) })>
// → { a: <transform's input type> }   — `a` is required at the type level

z.object({ a: z.preprocess(fn, T) }).parse({})
// → { a: fn(undefined) }   — accepts {} at runtime
```

--------------------------------

### Evaluate optionality cases across modifiers in Zod (TypeScript)

Source: https://github.com/colinhacks/zod/blob/main/wiki/optionality.md

Demonstrates runtime evaluation behaviors and optin mechanics when chaining optional, default, catch, prefault, preprocess, and transform. Shows how schema wrapping affects handling of undefined inputs and object keys.

```typescript
// === Catch ===

z.string().catch("c").parse(undefined)
// → "c"  (catch fires on string(undefined) failure; nothing outer to override it)

z.string().catch("c").parse(123)
// → "c"  (catch fires on string(123) failure)

z.string().catch("c").optional().parse(undefined)
// → undefined  (catch.optin = "optional", not "defaulted", so optional yields undefined without running it)

z.string().catch("c").optional().parse("hi")
// → "hi"  (input defined, so optional just runs the inner)

z.string().catch("c").transform((s) => s + "!").optional().parse(undefined)
// → undefined  (pipe.optin = catch.optin = "optional"; optional yields undefined)

z.object({ a: z.string().catch("c") }).parse({})
// → { a: "c" }  (catch.optin = "optional" runtime; obj invokes catch with undefined; catch fires)

z.object({ a: z.string().catch("c") }).parse({ a: undefined })
// → { a: "c" }  (key present with undefined; catch fires on string(undefined))

z.object({ a: z.string().catch("c").optional() }).parse({})
// → {}  (key absent; optional sees optin = "optional", not "defaulted", so yields undefined; obj omits)

// === Default ===

z.string().default("d").parse(undefined)
// → "d"  (default short-circuits on undef input, returns d directly)

z.string().default("d").optional().parse(undefined)
// → "d"  (default.optin = "defaulted"; optional runs it and respects the result)

z.object({ a: z.string().default("d") }).parse({})
// → { a: "d" }  (obj asks optin !== undefined; invokes default; short-circuits to d)

// === Prefault ===

z.string().prefault("p").parse(undefined)
// → "p"  (prefault substitutes, runs inner string("p") which succeeds)

z.string().prefault("p").optional().parse(undefined)
// → "p"  (prefault.optin = "defaulted" — it answers the absence, so optional respects it)

// === Preprocess ===

z.preprocess((v) => v ?? "X", z.string()).parse(undefined)
// → "X"  (preprocess fn produces "X", inner string accepts)

z.preprocess((v) => v ?? "X", z.string()).optional().parse(undefined)
// → undefined  (pipe.optin = transform.optin = "optional"; optional yields undefined, fn never runs)

z.object({ a: z.preprocess((v) => v ?? "X", z.string()) }).parse({})
// → { a: "X" }  (preprocess.optin = "optional" via transform; obj invokes; fn runs)

z.object({ a: z.preprocess((v) => v ?? "X", z.string()).optional() }).parse({})
// → {}  (optional sees "optional", not "defaulted"; yields undefined without running the fn; obj omits)

z.object({ a: z.preprocess((v) => v, z.string().optional()) }).parse({})
// → {}  (inner-optional preprocess; #5917/#5929 path)

// === Default feeding a transform (the #6321 shape) ===

z.string().default("").transform((v) => (v ? v.split(",") : [])).optional().parse(undefined)
// → []  (pipe.optin = def.in.optin = default.optin = "defaulted"; optional runs it and respects)
//   4.4.3-4.4.x: was undefined, because transform flagged every invocation as a fallback

z.object({ a: z.string().default("").transform((v) => v.length) }).partial().parse({})
// → { a: 0 }  (.partial() wraps in optional; the top rung carries through the pipe)

// === Transform ===

z.string().transform((s) => s + "!").parse("hi")
// → "hi!"

z.string().transform((s) => s + "!").parse(undefined)
// → THROW  (string rejects undef before transform runs)

z.transform((v) => v ?? "X").parse(undefined)
// → "X"  (transform fn runs on undef, returns "X")

z.transform((v) => v ?? "X").optional().parse(undefined)
// → undefined  (transform.optin = "optional"; optional yields undefined, fn never runs)

z.object({ a: z.transform((v) => v ?? "X") }).parse({})
// → { a: "X" }  (transform.optin = "optional" runtime; obj invokes transform with undef)

z.object({ a: z.string().transform((s) => s + "!") }).parse({})
// → THROW  (pipe.optin = string.optin = undefined — transform on OUT side doesn't drive optin)
```

--------------------------------

### schema.apply(fn, ...args)

Source: https://github.com/colinhacks/zod/blob/main/packages/docs/content/api.mdx

Incorporates external helper or transformation functions directly into Zod's method chain. Forwards the schema and any additional arguments to the function.

```APIDOC
## schema.apply(fn, ...args)

### Description
Applies an external function into Zod's method chain, passing the current schema as the first argument and any additional arguments to the callback function.

### Signature
`schema.apply<R>(fn: (schema: T, ...args: Args) => R, ...args: Args): R`

### Parameters
- **fn** (`(schema: T, ...args: any[]) => any`) - *Required* - Function that receives the current schema and returns a new or modified schema.
- **...args** (`any[]`) - *Optional* - Additional arguments passed through to `fn`.

### Usage Example
```ts
function withDefault<T extends z.ZodType>(schema: T, value: z.output<T>) {
  return schema.nullish().transform((val) => val ?? value);
}

const schema = z.string().apply(withDefault, "anonymous");

schema.parse(undefined); // => "anonymous"
schema.parse("sandwich"); // => "sandwich"
```
```

--------------------------------

### Inspect default output vs input branding in Zod

Source: https://github.com/colinhacks/zod/blob/main/packages/docs/content/api.mdx

Demonstrates that calling .brand() brands the output type by default while keeping the input type unbranded.

```ts
const USD = z.string().brand<"USD">();

type USDOutput = z.output<typeof USD>; // string & z.$brand<"USD">
type USDInput = z.input<typeof USD>; // string
```