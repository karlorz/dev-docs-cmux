### Install the Stripe SDK

Source: https://github.com/honojs/website/blob/main/examples/stripe-webhook.md

Install the official Stripe Node.js library.

```bash
npm install stripe
```

--------------------------------

### Install project dependencies

Source: https://github.com/honojs/website/blob/main/docs/getting-started/cloudflare-workers-vite.md

Navigate into the project directory and install required dependencies.

```npm
cd my-app
npm i
```

```yarn
cd my-app
yarn
```

```pnpm
cd my-app
pnpm i
```

```bun
cd my-app
bun i
```

--------------------------------

### Install Valibot

Source: https://github.com/honojs/website/blob/main/docs/guides/validation.md

Install the valibot library using your package manager.

```sh
npm i valibot
```

```sh
yarn add valibot
```

```sh
pnpm add valibot
```

```sh
bun add valibot
```

--------------------------------

### Install Apitally SDK

Source: https://github.com/honojs/website/blob/main/examples/apitally.md

Install the Apitally SDK package using npm, yarn, pnpm, or bun.

```bash
# npm
npm install apitally

# yarn
yarn add apitally

# pnpm
pnpm add apitally

# bun
bun add apitally
```

--------------------------------

### Initialize project and install dependencies

Source: https://github.com/honojs/website/blob/main/docs/getting-started/ali-function-compute.md

Initializes a new directory and installs Hono along with the hono-alibaba-cloud-fc3-adapter and build tools using your preferred package manager.

```sh
mkdir my-app
cd my-app
npm i hono hono-alibaba-cloud-fc3-adapter
npm i -D @serverless-devs/s esbuild
mkdir src
touch src/index.ts
```

```sh
mkdir my-app
cd my-app
yarn add hono hono-alibaba-cloud-fc3-adapter
yarn add -D @serverless-devs/s esbuild
mkdir src
touch src/index.ts
```

```sh
mkdir my-app
cd my-app
pnpm add hono hono-alibaba-cloud-fc3-adapter
pnpm add -D @serverless-devs/s esbuild
mkdir src
touch src/index.ts
```

```sh
mkdir my-app
cd my-app
bun add hono hono-alibaba-cloud-fc3-adapter
bun add -D esbuild @serverless-devs/s
mkdir src
touch src/index.ts
```

--------------------------------

### Install @scalar/hono-api-reference

Source: https://github.com/honojs/website/blob/main/examples/scalar.md

Install the Scalar API reference middleware package for Hono.

```bash
npm install @scalar/hono-api-reference
```

--------------------------------

### Install Stytch dependencies with bun

Source: https://github.com/honojs/website/blob/main/examples/stytch-auth.md

Install backend and frontend Stytch dependencies using bun.

```sh
# Backend
bun add @hono/stytch-auth stytch

# Frontend
bun add @stytch/react @stytch/vanilla-js
```

--------------------------------

### Define sample GET and POST routes

Source: https://github.com/honojs/website/blob/main/docs/guides/testing.md

Defines example GET and POST endpoints on a Hono application to validate in test suites.

```ts
app.get('/posts', (c) => {
  return c.text('Many posts')
})

app.post('/posts', (c) => {
  return c.json(
    {
      message: 'Created',
    },
    201,
    {
      'X-Custom': 'Thank you',
    }
  )
})
```

--------------------------------

### Start the development server

Source: https://github.com/honojs/website/blob/main/docs/getting-started/bun.md

Runs the application in development mode, making it accessible at http://localhost:3000.

```sh
bun run dev
```

--------------------------------

### Install @scalar/openapi-to-markdown

Source: https://github.com/honojs/website/blob/main/examples/scalar.md

Install the package used to convert OpenAPI documents into Markdown format.

```bash
npm install @scalar/openapi-to-markdown
```

--------------------------------

### Install Prisma CLI

Source: https://github.com/honojs/website/blob/main/examples/prisma.md

Install Prisma as a development dependency in your Hono project.

```bash
npm i prisma --save-dev
```

--------------------------------

### Install hono-openapi and standard validator

Source: https://github.com/honojs/website/blob/main/examples/hono-openapi.md

Installs the hono-openapi middleware and the @hono/standard-validator package.

```bash
npm install hono-openapi @hono/standard-validator
```

--------------------------------

### Install Better Auth package

Source: https://github.com/honojs/website/blob/main/examples/better-auth.md

Install the framework using your preferred package manager (npm, bun, pnpm, or yarn).

```sh
# npm
npm install better-auth

# bun
bun add better-auth

# pnpm
pnpm add better-auth

# yarn
yarn add better-auth
```

--------------------------------

### Run local development server

Source: https://github.com/honojs/website/blob/main/docs/getting-started/cloudbase.md

Start the local development server listening on port 9000.

```sh
npm run dev
```

--------------------------------

### Install Stytch dependencies with npm

Source: https://github.com/honojs/website/blob/main/examples/stytch-auth.md

Install backend (@hono/stytch-auth, stytch) and frontend (@stytch/react, @stytch/vanilla-js) dependencies using npm.

```sh
# Backend
npm install @hono/stytch-auth stytch

# Frontend
npm install @stytch/react @stytch/vanilla-js
```

--------------------------------

### Install Prisma and Cloudflare D1 adapter dependencies

Source: https://github.com/honojs/website/blob/main/examples/prisma.md

Installs the required Prisma CLI, client, and D1 adapter packages and initializes a Prisma project.

```bash
npm install prisma --save-dev
npx prisma init
npm install @prisma/client
npm install @prisma/adapter-d1
```

--------------------------------

### Install Prisma Accelerate extension

Source: https://github.com/honojs/website/blob/main/examples/prisma.md

Install the Prisma client extension required for Prisma Postgres.

```sh
npm i @prisma/extension-accelerate
```

--------------------------------

### Install ArkType

Source: https://github.com/honojs/website/blob/main/docs/guides/validation.md

Install the arktype validation library using your package manager.

```sh
npm i arktype
```

```sh
yarn add arktype
```

```sh
pnpm add arktype
```

```sh
bun add arktype
```

--------------------------------

### Install InferDI and Hono middleware

Source: https://github.com/honojs/website/blob/main/examples/inferdi.md

Install the core InferDI library alongside the Hono middleware adapter using npm.

```bash
npm install @inferdi/inferdi @inferdi/hono
```

--------------------------------

### Initialize a Hono project with Deno

Source: https://github.com/honojs/website/blob/main/docs/getting-started/deno.md

Initializes a new Hono project using Deno's template starter and enters the project directory. Deno does not require installing Hono explicitly.

```sh
deno init --npm hono --template=deno my-app
```

```sh
cd my-app
```

--------------------------------

### Start development server

Source: https://github.com/honojs/website/blob/main/docs/getting-started/cloudflare-workers-vite.md

Run the local Vite development server, accessible by default at http://localhost:5173.

```npm
npm run dev
```

```yarn
yarn dev
```

```pnpm
pnpm dev
```

```bun
bun run dev
```

--------------------------------

### Install Stytch dependencies with pnpm

Source: https://github.com/honojs/website/blob/main/examples/stytch-auth.md

Install backend and frontend Stytch dependencies using pnpm.

```sh
# Backend
pnpm add @hono/stytch-auth stytch

# Frontend
pnpm add @stytch/react @stytch/vanilla-js
```

--------------------------------

### Install Valibot schema library

Source: https://github.com/honojs/website/blob/main/examples/hono-openapi.md

Installs Valibot and its JSON schema converter for schema definition and OpenAPI generation.

```bash
npm install valibot @valibot/to-json-schema
```

--------------------------------

### Initialize WebAssembly Hono project and install dependencies

Source: https://github.com/honojs/website/blob/main/docs/getting-started/webassembly-wasi.md

Initializes a new project directory and installs Hono alongside Bytecode Alliance WASI tools and Rolldown. Ensure that `type` is set to `"module"` in `package.json`.

```sh
mkdir my-app
cd my-app
npm init
npm i hono
npm i -D @bytecodealliance/jco @bytecodealliance/componentize-js @bytecodealliance/jco-std
npm i -D rolldown
```

```sh
mkdir my-app
cd my-app
npm init
yarn add hono
yarn add -D @bytecodealliance/jco @bytecodealliance/componentize-js @bytecodealliance/jco-std
yarn add -D rolldown
```

```sh
mkdir my-app
cd my-app
pnpm init --init-type module
pnpm add hono
pnpm add -D @bytecodealliance/jco @bytecodealliance/componentize-js @bytecodealliance/jco-std
pnpm add -D rolldown
```

```sh
mkdir my-app
cd my-app
npm init
bun add hono
bun add -D @bytecodealliance/jco @bytecodealliance/componentize-js @bytecodealliance/jco-std
```

--------------------------------

### Install Zod package

Source: https://github.com/honojs/website/blob/main/docs/guides/validation.md

Install Zod using your preferred package manager.

```sh
npm i zod
```

```sh
yarn add zod
```

```sh
pnpm add zod
```

```sh
bun add zod
```

--------------------------------

### Create a basic Hono application in main.ts

Source: https://github.com/honojs/website/blob/main/docs/getting-started/deno.md

Defines a basic root route returning text and starts the server with Deno.serve.

```ts
import { Hono } from 'hono'

const app = new Hono()

app.get('/', (c) => c.text('Hello Deno!'))

Deno.serve(app.fetch)
```

--------------------------------

### Install Stytch dependencies with yarn

Source: https://github.com/honojs/website/blob/main/examples/stytch-auth.md

Install backend and frontend Stytch dependencies using yarn.

```sh
# Backend
yarn add @hono/stytch-auth stytch

# Frontend
yarn add @stytch/react @stytch/vanilla-js
```

--------------------------------

### Install the gcloud CLI using Homebrew

Source: https://github.com/honojs/website/blob/main/docs/getting-started/google-cloud-run.md

Installs the Google Cloud SDK CLI on macOS via Homebrew.

```sh
brew install --cask gcloud-cli
```

--------------------------------

### Initialize project non-interactively with template, package manager, and install

Source: https://github.com/honojs/website/blob/main/docs/guides/create-hono.md

Creates a project with the `vercel` template, sets `npm` as the package manager, installs dependencies automatically, and skips interactive prompts.

```bash
npm create hono@latest my-app -- --template vercel --pm npm --install
```

--------------------------------

### Install Hono and Auth.js dependencies

Source: https://github.com/honojs/website/blob/main/examples/hono-authjs.md

Install the required packages for Hono, Auth.js core, the Hono Auth.js integration, and the Drizzle adapter.

```bash
npm install hono @hono/auth-js @auth/core @auth/drizzle-adapter
```

--------------------------------

### Install Hono and Azure Functions Adapter

Source: https://github.com/honojs/website/blob/main/docs/getting-started/azure-functions.md

Installs @marplex/hono-azurefunc-adapter and hono dependencies using your preferred package manager.

```sh
npm i @marplex/hono-azurefunc-adapter hono
```

```sh
yarn add @marplex/hono-azurefunc-adapter hono
```

```sh
pnpm add @marplex/hono-azurefunc-adapter hono
```

```sh
bun add @marplex/hono-azurefunc-adapter hono
```

--------------------------------

### Example project directory structure for static files

Source: https://github.com/honojs/website/blob/main/docs/getting-started/nodejs.md

Illustrates the directory layout for serving static files such as favicon.ico and static assets.

```sh
./
├── favicon.ico
├── index.ts
└── static
    ├── hello.txt
    └── image.png
```

--------------------------------

### Install dependencies and initialize TypeScript configuration

Source: https://github.com/honojs/website/blob/main/docs/getting-started/webassembly-wasi.md

Installs project packages and generates a `tsconfig.json` configuration file. Ensure `compilerOptions.module` is set to `"nodenext"` in `tsconfig.json`.

```sh
npm i
npx tsc --init
```

```sh
yarn
yarn tsc --init
```

```sh
pnpm i
pnpm exec tsc --init
```

```sh
bun i
```

--------------------------------

### Install @hono/standard-validator

Source: https://github.com/honojs/website/blob/main/docs/guides/validation.md

Install the Standard Schema Validator middleware using your preferred package manager.

```sh
npm i @hono/standard-validator
```

```sh
yarn add @hono/standard-validator
```

```sh
pnpm add @hono/standard-validator
```

```sh
bun add @hono/standard-validator
```

--------------------------------

### Run local development server

Source: https://github.com/honojs/website/blob/main/docs/getting-started/vercel.md

Start the local development server using the Vercel CLI. The app will be accessible at localhost:3000.

```sh
vercel dev
```

--------------------------------

### Install dependencies

Source: https://github.com/honojs/website/blob/main/docs/getting-started/service-worker.md

Installs Hono as a dependency and Vite as a development dependency across various package managers.

```sh
npm i hono
npm i -D vite
```

```sh
yarn add hono
yarn add -D vite
```

```sh
pnpm add hono
pnpm add -D vite
```

```sh
bun add hono
bun add -D vite
```

--------------------------------

### Initialize CDK project and install Hono

Source: https://github.com/honojs/website/blob/main/docs/getting-started/lambda-edge.md

Initializes a TypeScript AWS CDK project and installs the Hono dependency using your preferred package manager.

```sh
mkdir my-app
cd my-app
cdk init app -l typescript
npm i hono
mkdir lambda
```

```sh
mkdir my-app
cd my-app
cdk init app -l typescript
yarn add hono
mkdir lambda
```

```sh
mkdir my-app
cd my-app
cdk init app -l typescript
pnpm add hono
mkdir lambda
```

```sh
mkdir my-app
cd my-app
cdk init app -l typescript
bun add hono
mkdir lambda
```

--------------------------------

### Start the local development server

Source: https://github.com/honojs/website/blob/main/docs/getting-started/fastly.md

Runs the local Fastly development server. Once running, access the app at http://localhost:7676 in a web browser.

```npm
npm run start
```

```yarn
yarn start
```

```pnpm
pnpm run start
```

```bun
bun run start
```

--------------------------------

### Define basic routes and HTTP method handlers

Source: https://github.com/honojs/website/blob/main/docs/api/routing.md

Demonstrates routing by HTTP methods (GET, POST, PUT, DELETE, QUERY), wildcard paths, handling all methods with app.all, custom HTTP methods, and handling multiple methods or paths with app.on.

```typescript
import { Hono } from 'hono'
const app = new Hono()
// ---cut---
// HTTP Methods
app.get('/', (c) => c.text('GET /'))
app.post('/', (c) => c.text('POST /'))
app.put('/', (c) => c.text('PUT /'))
app.delete('/', (c) => c.text('DELETE /'))
app.query('/', (c) => c.text('QUERY /'))

// Wildcard
app.get('/wild/*/card', (c) => {
  return c.text('GET /wild/*/card')
})

// Any HTTP methods
app.all('/hello', (c) => c.text('Any Method /hello'))

// Custom HTTP method
app.on('PURGE', '/cache', (c) => c.text('PURGE Method /cache'))

// Multiple Method
app.on(['PUT', 'DELETE'], '/post', (c) =>
  c.text('PUT or DELETE /post')
)

// Multiple Paths
app.on('GET', ['/hello', '/ja/hello', '/en/hello'], (c) =>
  c.text('Hello')
)
```

--------------------------------

### Initialize project using offline cache

Source: https://github.com/honojs/website/blob/main/docs/guides/create-hono.md

Uses local template cache instead of fetching remote templates, useful for offline environments or deterministic local setups.

```bash
pnpm create hono@latest my-app --template deno --offline
```

--------------------------------

### Create a basic Hono application

Source: https://github.com/honojs/website/blob/main/docs/index.md

Initializes a Hono application instance, registers a GET route on the root path returning text, and exports the app as default.

```ts
import { Hono } from 'hono'
const app = new Hono()

app.get('/', (c) => c.text('Hono!'))

export default app
```

--------------------------------

### Start local development server with netlify dev

Source: https://github.com/honojs/website/blob/main/docs/getting-started/netlify.md

Runs the local Netlify Edge Functions development server. Once started, the application is accessible at http://localhost:8888 in your browser.

```sh
netlify dev
```

--------------------------------

### startTime(c, name)

Source: https://github.com/honojs/website/blob/main/docs/middleware/builtin/timing.md

Starts a timer for measuring the duration of a specific operation.

```APIDOC
### Description
Starts a performance timer under a given metric name for the current request context.

### Signature
`startTime(c, name)`

### Parameters
- **c** (Context) - Required - The Hono request context.
- **name** (string) - Required - Name identifier for the timer.

### Example
```ts
startTime(c, 'db')
const data = await db.findMany(...)
endTime(c, 'db')
```
```

--------------------------------

### Select a starter template

Source: https://github.com/honojs/website/blob/main/docs/getting-started/basic.md

Interactive prompt to choose a target runtime or platform template during project initialization.

```text
? Which template do you want to use?
    aws-lambda
    bun
    cloudflare-pages
❯   cloudflare-workers
    deno
    fastly
    nextjs
    nodejs
    vercel
```

--------------------------------

### Example JSON response from /hello endpoint

Source: https://github.com/honojs/website/blob/main/docs/getting-started/webassembly-wasi.md

Expected JSON payload returned when sending a request to localhost:8000/hello.

```json
{ "message": "Hello from WebAssembly!" }
```

--------------------------------

### Create a Hello World application in src/index.ts

Source: https://github.com/honojs/website/blob/main/docs/getting-started/nodejs.md

Set up a basic Hono web server using the serve function from @hono/node-server.

```ts
import { serve } from '@hono/node-server'
import { Hono } from 'hono'

const app = new Hono()
app.get('/', (c) => c.text('Hello Node.js!'))

serve(app)
```

--------------------------------

### Initialize Prisma with Prisma Postgres

Source: https://github.com/honojs/website/blob/main/examples/prisma.md

Run the initialization command to set up Prisma Postgres. This prompts login via Prisma Data Platform if not already authenticated.

```bash
npx prisma@latest init --db
```

--------------------------------

### Initialize AWS CDK project for Hono

Source: https://github.com/honojs/website/blob/main/docs/getting-started/aws-lambda.md

Initializes a new TypeScript CDK application, installs hono and esbuild, and creates the lambda/index.ts entrypoint file using your preferred package manager.

```sh
mkdir my-app
cd my-app
cdk init app -l typescript
npm i hono
npm i -D esbuild
mkdir lambda
touch lambda/index.ts
```

```sh
mkdir my-app
cd my-app
cdk init app -l typescript
yarn add hono
yarn add -D esbuild
mkdir lambda
touch lambda/index.ts
```

```sh
mkdir my-app
cd my-app
cdk init app -l typescript
pnpm add hono
pnpm add -D esbuild
mkdir lambda
touch lambda/index.ts
```

```sh
mkdir my-app
cd my-app
cdk init app -l typescript
bun add hono
bun add -D esbuild
mkdir lambda
touch lambda/index.ts
```

--------------------------------

### Create a Hello World application with fire()

Source: https://github.com/honojs/website/blob/main/docs/getting-started/fastly.md

Defines a basic Fastly Compute entry point in src/index.ts using fire from @fastly/hono-fastly-compute. Use Hono from 'hono' rather than 'hono/quick' because fire builds router data during initialization.

```ts
// src/index.ts
import { Hono } from 'hono'
import { fire } from '@fastly/hono-fastly-compute'

const app = new Hono()

app.get('/', (c) => c.text('Hello Fastly!'))

fire(app)
```

--------------------------------

### Example jco serve terminal output

Source: https://github.com/honojs/website/blob/main/docs/getting-started/webassembly-wasi.md

Displays the terminal output generated when launching the server with jco serve.

```text
$ npx jco serve dist/component.wasm
Server listening @ localhost:8000...
```

--------------------------------

### Install @hono/zod-validator middleware

Source: https://github.com/honojs/website/blob/main/docs/guides/validation.md

Install the @hono/zod-validator package using your package manager.

```sh
npm i @hono/zod-validator
```

```sh
yarn add @hono/zod-validator
```

```sh
pnpm add @hono/zod-validator
```

```sh
bun add @hono/zod-validator
```

--------------------------------

### Run WebAssembly HTTP server with jco serve

Source: https://github.com/honojs/website/blob/main/docs/getting-started/webassembly-wasi.md

Starts the local development server using jco serve for the compiled WASI component. Intended for development and not recommended for production use.

```sh
npx jco serve dist/component.wasm
```

```sh
yarn jco serve dist/component.wasm
```

```sh
pnpm exec jco serve dist/component.wasm
```

```sh
bun run jco serve dist/component.wasm
```

--------------------------------

### Create a Hono project with deno

Source: https://github.com/honojs/website/blob/main/docs/index.md

Initializes a new Hono project using deno init.

```sh
deno init --npm hono@latest
```

--------------------------------

### Create a new Hono project with Bun

Source: https://github.com/honojs/website/blob/main/docs/getting-started/bun.md

Initializes a new Hono starter project using the create command. Select the bun template when prompted.

```sh
bun create hono@latest my-app
```

--------------------------------

### Serve Supabase Edge Functions locally

Source: https://github.com/honojs/website/blob/main/docs/getting-started/supabase-functions.md

Starts the local Supabase stack and runs the Functions watcher with the `--no-verify-jwt` flag to bypass JWT verification during local development.

```bash
supabase start # start the supabase stack
supabase functions serve --no-verify-jwt # start the Functions watcher
```

--------------------------------

### Create a new Hono project

Source: https://github.com/honojs/website/blob/main/docs/getting-started/basic.md

Initialize a starter template using create-hono with your package manager of choice.

```sh
npm create hono@latest my-app
```

```sh
yarn create hono my-app
```

```sh
pnpm create hono@latest my-app
```

```sh
bun create hono@latest my-app
```

```sh
deno init --npm hono@latest my-app
```

--------------------------------

### Install @cloudflare/workers-types development dependency

Source: https://github.com/honojs/website/blob/main/docs/getting-started/cloudflare-workers.md

Install TypeScript definitions for Cloudflare Workers using your package manager.

```sh
npm i --save-dev @cloudflare/workers-types
```

```sh
yarn add -D @cloudflare/workers-types
```

```sh
pnpm add -D @cloudflare/workers-types
```

```sh
bun add --dev @cloudflare/workers-types
```

--------------------------------

### Initialize a new Supabase project

Source: https://github.com/honojs/website/blob/main/docs/getting-started/supabase-functions.md

Initializes a new Supabase project in the current directory using the Supabase CLI.

```bash
supabase init

```

--------------------------------

### Install typed-htmx dependency

Source: https://github.com/honojs/website/blob/main/examples/htmx.md

Install the typed-htmx development dependency to enable TypeScript definitions for htmx attributes in JSX.

```sh
npm i -D typed-htmx
```

--------------------------------

### Create a Hono project with npm

Source: https://github.com/honojs/website/blob/main/docs/index.md

Initializes a new Hono project using npm create.

```sh
npm create hono@latest
```

--------------------------------

### Build the application

Source: https://github.com/honojs/website/blob/main/docs/getting-started/nodejs.md

Runs the build script using npm, yarn, pnpm, or bun.

```sh
npm run build
```

```sh
yarn run build
```

```sh
pnpm run build
```

```sh
bun run build
```

--------------------------------

### Redirect GET requests with trimTrailingSlash

Source: https://github.com/honojs/website/blob/main/docs/middleware/builtin/trailing-slash.md

Trims trailing slashes from URLs on GET requests when the response status is 404.

```ts
import { Hono } from 'hono'
import { trimTrailingSlash } from 'hono/trailing-slash'

const app = new Hono({ strict: true })

app.use(trimTrailingSlash())
app.get('/about/me', (c) => c.text('Without Trailing Slash'))
```

--------------------------------

### Create a new project with create-hono

Source: https://github.com/honojs/website/blob/main/docs/getting-started/cloudflare-workers-vite.md

Initialize a new project using the cloudflare-workers+vite starter template across different package managers.

```npm
npm create hono@latest my-app
```

```yarn
yarn create hono my-app
```

```pnpm
pnpm create hono my-app
```

```bun
bun create hono@latest my-app
```

```deno
deno init --npm hono my-app
```

--------------------------------

### Install @hono/node-server for Pages Router

Source: https://github.com/honojs/website/blob/main/docs/getting-started/nextjs.md

Installs the Node.js adapter package required when using Hono with Next.js Pages Router.

```sh
npm i @hono/node-server
```

```sh
yarn add @hono/node-server
```

```sh
pnpm add @hono/node-server
```

```sh
bun add @hono/node-server
```

--------------------------------

### Redirect GET requests with appendTrailingSlash

Source: https://github.com/honojs/website/blob/main/docs/middleware/builtin/trailing-slash.md

Appends a trailing slash to URLs when a GET request returns a 404 response status.

```ts
import { Hono } from 'hono'
import { appendTrailingSlash } from 'hono/trailing-slash'

const app = new Hono({ strict: true })

app.use(appendTrailingSlash())
app.get('/about/me/', (c) => c.text('With Trailing Slash'))
```

--------------------------------

### Test GET request with app.request

Source: https://github.com/honojs/website/blob/main/docs/guides/testing.md

Sends a GET request to an endpoint using app.request and asserts the response status and text body.

```ts
describe('Example', () => {
  test('GET /posts', async () => {
    const res = await app.request('/posts')
    expect(res.status).toBe(200)
    expect(await res.text()).toBe('Many posts')
  })
})
```

--------------------------------

### Install Azure Functions Core Tools on macOS

Source: https://github.com/honojs/website/blob/main/docs/getting-started/azure-functions.md

Installs the Azure Functions Core Tools version 4 using Homebrew on macOS.

```sh
brew tap azure/functions
brew install azure-functions-core-tools@4
```

--------------------------------

### Create a basic GET endpoint returning JSON

Source: https://github.com/honojs/website/blob/main/docs/concepts/stacks.md

Sets up a Hono application instance and defines a basic GET endpoint that responds with JSON.

```ts
import { Hono } from 'hono'

const app = new Hono()

app.get('/hello', (c) => {
  return c.json({
    message: `Hello!`,
  })
})
```

--------------------------------

### Pass template argument with bun

Source: https://github.com/honojs/website/blob/main/docs/guides/create-hono.md

Specify a starter template directly using the `--template` flag with bun.

```sh
# "--template cloudflare-workers" selects the Cloudflare Workers template
bun create hono@latest my-app --template cloudflare-workers
```

--------------------------------

### Create a Hono project with yarn

Source: https://github.com/honojs/website/blob/main/docs/index.md

Initializes a new Hono project using yarn create.

```sh
yarn create hono
```

--------------------------------

### Configure Entry Point in wrangler.jsonc

Source: https://github.com/honojs/website/blob/main/docs/getting-started/cloudflare-workers.md

Specifies the application entry point and enables minification in `wrangler.jsonc` after the `compatibility_date` configuration.

```jsonc
"main": "src/index.ts",
"minify": true
```

--------------------------------

### Create a Hono project with pnpm

Source: https://github.com/honojs/website/blob/main/docs/index.md

Initializes a new Hono project using pnpm create.

```sh
pnpm create hono@latest
```

--------------------------------

### endTime(c, name)

Source: https://github.com/honojs/website/blob/main/docs/middleware/builtin/timing.md

Stops a previously started timer and records the duration in the metrics.

```APIDOC
### Description
Stops an active timer identified by name and calculates the elapsed duration for the Server-Timing header.

### Signature
`endTime(c, name)`

### Parameters
- **c** (Context) - Required - The Hono request context.
- **name** (string) - Required - Name identifier for the timer that was started.

### Example
```ts
startTime(c, 'db')
const data = await db.findMany(...)
endTime(c, 'db')
```
```

--------------------------------

### Set up WebSocket with upgradeWebSocket

Source: https://github.com/honojs/website/blob/main/docs/getting-started/nodejs.md

Enable WebSocket support in @hono/node-server using ws and WebSocketServer with noServer set to true.

```ts
import { serve, upgradeWebSocket } from '@hono/node-server'
import { Hono } from 'hono'
import { WebSocketServer } from 'ws'

const app = new Hono()

app.get(
  '/ws',
  upgradeWebSocket(() => ({
    onMessage(event, ws) {
      ws.send(event.data)
    },
  }))
)

const wss = new WebSocketServer({ noServer: true })

serve({
  fetch: app.fetch,
  websocket: { server: wss },
})
```

--------------------------------

### Get all matched routes with matchedRoutes()

Source: https://github.com/honojs/website/blob/main/docs/helpers/route.md

Returns an array of all routes and middleware matching the current request.

```ts
app.all('/api/*', (c, next) => {
  console.log('API middleware')
  return next()
})

app.get('/api/users/:id', (c) => {
  const routes = matchedRoutes(c)
  // Returns: [
  //   { method: 'ALL', path: '/api/*', handler: [Function] },
  //   { method: 'GET', path: '/api/users/:id', handler: [Function] }
  // ]
  return c.json({ routes: routes.length })
})
```

--------------------------------

### Generate static site on Bun with hono/bun adapter

Source: https://github.com/honojs/website/blob/main/docs/helpers/ssg.md

Uses the Bun-specific toSSG adapter, which automatically provides Bun's native filesystem module.

```ts
import { toSSG } from 'hono/bun'

toSSG(app) // The second argument is an option typed `ToSSGOptions`.
```

--------------------------------

### Directory layout for static files

Source: https://github.com/honojs/website/blob/main/docs/getting-started/bun.md

Illustrates the directory and file structure that works with the static file serving configuration.

```text
./
├── favicon.ico
├── src
└── static
    ├── demo
    │   └── index.html
    ├── fallback.txt
    ├── hello.txt
    └── images
        └── dinotocat.png
```

--------------------------------

### Get current route pattern with routePath()

Source: https://github.com/honojs/website/blob/main/docs/helpers/route.md

Returns the registered route path pattern for the current handler.

```ts
app.get('/posts/:id', (c) => {
  console.log(routePath(c)) // '/posts/:id'
  return c.text('Post details')
})
```

--------------------------------

### Generate a static site manually with toSSG in Node.js

Source: https://github.com/honojs/website/blob/main/docs/helpers/ssg.md

Demonstrates setting up a Hono app with JSX rendering, generating static files using toSSG with Node's fs/promises module, and inspecting the generated static directory.

```tsx
// index.tsx
const app = new Hono()

app.get('/', (c) => c.html('Hello, World!'))

app.use('/about', async (c, next) => {
  c.setRenderer((content) => {
    return c.html(
      <html>
        <head />
        <body>
          <p>{content}</p>
        </body>
      </html>
    )
  })
  await next()
})

app.get('/about', (c) => {
  return c.render(
    <>
      <title>Hono SSG Page</title>Hello!
    </>
  )
})

export default app
```

```ts
// build.ts
import app from './index'
import { toSSG } from 'hono/ssg'
import fs from 'fs/promises'

toSSG(app, fs)
```

```bash
ls ./static
about.html  index.html
```

--------------------------------

### Getting Router Name with getRouterName()

Source: https://github.com/honojs/website/blob/main/docs/helpers/dev.md

Returns the name of the router currently being used by the Hono application instance.

```ts
const app = new Hono()

// ...

console.log(getRouterName(app))
```

--------------------------------

### Generate static site on Deno with hono/deno adapter

Source: https://github.com/honojs/website/blob/main/docs/helpers/ssg.md

Uses the Deno-specific toSSG adapter, which automatically provides Deno's native filesystem module.

```ts
import { toSSG } from 'hono/deno'

toSSG(app) // The second argument is an option typed `ToSSGOptions`.
```

--------------------------------

### Install Hono into an existing Bun project

Source: https://github.com/honojs/website/blob/main/docs/getting-started/bun.md

Adds the hono dependency to the root directory of an existing Bun application.

```sh
bun add hono
```

--------------------------------

### Create and configure scf_bootstrap script

Source: https://github.com/honojs/website/blob/main/docs/getting-started/cloudbase.md

Create scf_bootstrap in the project root with LF line endings and make it executable. Ensure the node path matches the target runtime (/var/lang/node20/bin/node for Node.js 20.19).

```sh
#!/bin/bash
/var/lang/node20/bin/node dist/index.js
```

```sh
chmod +x scf_bootstrap
```

--------------------------------

### Get request header values with c.req.header()

Source: https://github.com/honojs/website/blob/main/docs/api/request.md

Retrieve specific HTTP request headers such as User-Agent using c.req.header().

```typescript
import { Hono } from 'hono'
const app = new Hono()
// ---cut---
app.get('/', (c) => {
  const userAgent = c.req.header('User-Agent')
  //      ^?
  return c.text(`Your user agent is ${userAgent}`)
})
```

--------------------------------

### Get basic route information

Source: https://github.com/honojs/website/blob/main/docs/helpers/route.md

Access the current route pattern and all matched routes within a request handler.

```ts
const app = new Hono()

app.get('/posts/:id', (c) => {
  const currentPath = routePath(c) // '/posts/:id'
  const routes = matchedRoutes(c) // Array of matched routes

  return c.json({
    path: currentPath,
    totalRoutes: routes.length,
  })
})
```

--------------------------------

### Create auth API handler in routes/auth.ts

Source: https://github.com/honojs/website/blob/main/examples/better-auth.md

Serves all POST and GET requests under /auth/* by delegating to auth.handler with the raw request.

```ts
import { Hono } from 'hono'
import { auth } from '../lib/auth'
import type { AuthType } from '../lib/auth'

const router = new Hono<{ Bindings: AuthType }>({
  strict: false,
})

router.on(['POST', 'GET'], '/auth/*', (c) => {
  return auth.handler(c.req.raw)
})

export default router
```

--------------------------------

### Get multiple query parameters with c.req.queries()

Source: https://github.com/honojs/website/blob/main/docs/api/request.md

Retrieve multiple values for duplicate query parameter keys as an array of strings.

```typescript
import { Hono } from 'hono'
const app = new Hono()
// ---cut---
app.get('/search', async (c) => {
  // tags will be string[]
  const tags = c.req.queries('tags')
  //     ^?
  // ...
})
```

--------------------------------

### Build client and server bundles

Source: https://github.com/honojs/website/blob/main/docs/getting-started/cloudflare-pages.md

Runs Vite build sequentially in client mode and server mode.

```sh
vite build --mode client && vite build
```

--------------------------------

### Using defaultPlugin with toSSG

Source: https://github.com/honojs/website/blob/main/docs/helpers/ssg.md

Demonstrates how `defaultPlugin` is applied automatically when no plugins are specified to skip non-200 responses.

```ts
import { toSSG, defaultPlugin } from 'hono/ssg'

// defaultPlugin is automatically applied when no plugins specified
toSSG(app, fs)

// Equivalent to:
toSSG(app, fs, { plugins: [defaultPlugin] })
```

--------------------------------

### Get path parameter values with c.req.param()

Source: https://github.com/honojs/website/blob/main/docs/api/request.md

Extract individual path parameters by name or retrieve all captured parameters at once as an object.

```typescript
import { Hono } from 'hono'
const app = new Hono()
// ---cut---
// Captured params
app.get('/entry/:id', async (c) => {
  const id = c.req.param('id')
  //    ^?
  // ...
})

// Get all params at once
app.get('/entry/:id/comment/:commentId', async (c) => {
  const { id, commentId } = c.req.param()
  //      ^?
})
```

--------------------------------

### Filter GET Requests with beforeRequestHook

Source: https://github.com/honojs/website/blob/main/docs/helpers/ssg.md

Custom plugin using `beforeRequestHook` to skip non-GET routes during generation by returning `false`.

```ts
const getOnlyPlugin: SSGPlugin = {
  beforeRequestHook: (req) => {
    if (req.method === 'GET') {
      return req
    }
    return false
  },
}
```

--------------------------------

### Get resolved base path with basePath()

Source: https://github.com/honojs/website/blob/main/docs/helpers/route.md

Returns the base path including resolved parameter values from the actual request.

```ts
const subApp = new Hono()
subApp.get('/posts/:id', (c) => {
  return c.text(basePath(c)) // '/api' (for request to '/api/posts/123')
})

app.route('/:sub', subApp)
```

--------------------------------

### Get base route pattern with baseRoutePath()

Source: https://github.com/honojs/website/blob/main/docs/helpers/route.md

Returns the base path pattern of the current route as defined during sub-app routing.

```ts
const subApp = new Hono()
subApp.get('/posts/:id', (c) => {
  return c.text(baseRoutePath(c)) // '/:sub'
})

app.route('/:sub', subApp)
```

--------------------------------

### Get route path at index with routePath()

Source: https://github.com/honojs/website/blob/main/docs/helpers/route.md

Pass an index parameter to routePath() to retrieve the route path at a specific position.

```ts
app.all('/api/*', (c, next) => {
  return next()
})

app.get('/api/users/:id', (c) => {
  console.log(routePath(c, 0)) // '/api/*' (first matched route)
  console.log(routePath(c, -1)) // '/api/users/:id' (last matched route)
  return c.text('User details')
})
```

--------------------------------

### Create a Hello World Hono application for Bun

Source: https://github.com/honojs/website/blob/main/docs/getting-started/bun.md

Defines a basic Hono web server that responds with plain text on the root route. For existing projects, place this code in src/index.ts.

```ts
import { Hono } from 'hono'

const app = new Hono()
app.get('/', (c) => c.text('Hello Bun!'))

export default app
```

--------------------------------

### Pass template argument with pnpm

Source: https://github.com/honojs/website/blob/main/docs/guides/create-hono.md

Specify a starter template directly using the `--template` flag with pnpm.

```sh
# "--template cloudflare-workers" selects the Cloudflare Workers template
pnpm create hono@latest my-app --template cloudflare-workers
```

--------------------------------

### Return JSON response with c.json

Source: https://github.com/honojs/website/blob/main/docs/getting-started/basic.md

Handle a GET request and return an application/json response using the context json method.

```ts
app.get('/api/hello', (c) => {
  return c.json({
    ok: true,
    message: 'Hello Hono!',
  })
})
```

--------------------------------

### Defining a Redirect Route for redirectPlugin

Source: https://github.com/honojs/website/blob/main/docs/helpers/ssg.md

Example route returning a redirect response that will be converted into an HTML redirect page by `redirectPlugin`.

```ts
app.get('/old', (c) => c.redirect('/new'))
```

--------------------------------

### Use Server-Timing middleware with metrics and timers

Source: https://github.com/honojs/website/blob/main/docs/middleware/builtin/timing.md

Demonstrates applying the `timing` middleware to a Hono app, recording string and numeric metrics with `setMetric`, and measuring execution durations with `startTime`/`endTime` or `wrapTime`.

```js
// Specify the variable types to infer the `c.get('metric')`:
type Variables = TimingVariables

const app = new Hono<{ Variables: Variables }>()

// add the middleware to your router
app.use(timing());

app.get('/', async (c) => {

  // add custom metrics
  setMetric(c, 'region', 'europe-west3')

  // add custom metrics with timing, must be in milliseconds
  setMetric(c, 'custom', 23.8, 'My custom Metric')

  // start a new timer
  startTime(c, 'db');
  const data = await db.findMany(...);

  // end the timer
  endTime(c, 'db');

  // ...or you can also just wrap a Promise using this function:
  const data = await wrapTime(c, 'db', db.findMany(...));

  return c.json({ response: data });
});
```

--------------------------------

### c.req.method

Source: https://github.com/honojs/website/blob/main/docs/api/request.md

Retrieves the HTTP method name of the current incoming request. Returns strings such as GET, POST, PUT, DELETE, etc.

```APIDOC
### Property
`c.req.method`

### Description
The method name of the request.

### Type
`string`

### Example
```ts
app.get('/about/me', async (c) => {
  const method = c.req.method // `GET`
})
```
```

--------------------------------

### showRoutes(app, options?)

Source: https://github.com/honojs/website/blob/main/docs/helpers/dev.md

Prints all registered application routes to the console. Supports optional configuration for verbose output and color formatting.

```APIDOC
## showRoutes(app, options?)

### Description
Displays the registered routes in the console when the application starts or runs.

### Syntax
`showRoutes(app, options?)`

### Parameters
- **app** (`Hono`) - Required - The Hono application instance containing registered routes.
- **options** (`object`) - Optional - Configuration options for route display:
  - **verbose** (`boolean`) - Optional - When set to `true`, displays verbose information.
  - **colorize** (`boolean`) - Optional - When set to `false`, the output will not be colored.

### Example
```ts
import { Hono } from 'hono'
import { showRoutes } from 'hono/dev'

const app = new Hono().basePath('/v1')

app.get('/posts', (c) => {})
app.get('/posts/:id', (c) => {})
app.post('/posts', (c) => {})

showRoutes(app, {
  verbose: true,
})
```
```

--------------------------------

### Get query string parameters with c.req.query()

Source: https://github.com/honojs/website/blob/main/docs/api/request.md

Retrieve specific query parameters by key or retrieve all query string parameters as an object.

```typescript
import { Hono } from 'hono'
const app = new Hono()
// ---cut---
// Query params
app.get('/search', async (c) => {
  const query = c.req.query('q')
  //     ^?
})

// Get all params at once
app.get('/search', async (c) => {
  const { q, limit, offset } = c.req.query()
  //      ^?
})
```