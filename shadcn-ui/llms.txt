### my-custom-starter-template.json

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/registry/examples.mdx

Example of a universal registry item that installs multiple files, demonstrating how to define multiple file targets.

```json
{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "my-custom-starter-template",
  "type": "registry:item",
  "dependencies": ["better-auth"],
  "files": [
    {
      "path": "/path/to/file-01.json",
      "type": "registry:file",
      "target": "~/file-01.json",
      "content": "..."
    },
    {
      "path": "/path/to/file-02.vue",
      "type": "registry:file",
      "target": "~/pages/file-02.vue",
      "content": "..."
    }
  ]
}
```

--------------------------------

### Install Menubar via CLI or Manual Setup

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/components/radix/menubar.mdx

Use the CLI for automated installation or install the Radix UI primitive manually as a dependency.

```bash
npx shadcn@latest add menubar
```

```bash
npm install radix-ui
```

--------------------------------

### Install Menubar via CLI or Manual Setup

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/components/base/menubar.mdx

Use the shadcn CLI to add the component automatically or install the Base UI dependency manually.

```bash
npx shadcn@latest add menubar
```

```bash
npm install @base-ui/react
```

--------------------------------

### Install Dialog using CLI or npm

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/components/aria/dialog.mdx

Choose between the automated CLI installation or manual dependency setup. The CLI method is recommended for most projects.

```bash
npx shadcn@latest add dialog
```

```bash
npm install react-aria-components
```

--------------------------------

### Install Multiple Namespaced Resources

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/registry/namespace.mdx

Use this command to install several resources from different namespaces simultaneously, streamlining the setup process.

```bash
npx shadcn@latest add @acme/header @lib/auth-utils @ai/chatbot-rules
```

--------------------------------

### Installation

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/components/aria/select.mdx

Install the Select component using the CLI or manually add the required dependencies.

```bash
npx shadcn@latest add select
```

```bash
npm install react-aria-components
```

--------------------------------

### Install from a community registry

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/(root)/new.mdx

Install components from any registry URL using the CLI.

```bash
npx shadcn@latest add @[registry]/[name]
```

--------------------------------

### Example: Add alert-dialog component

Source: https://github.com/shadcn-ui/ui/blob/main/packages/shadcn/README.md

Example of using the add command to install the 'alert-dialog' component.

```bash
npx shadcn add alert-dialog
```

--------------------------------

### Install Vitest Setup Registry Item

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/registry/github.mdx

Command to install the "vitest-setup" registry item from the "acme/toolkit" repository.

```bash
npx shadcn@latest add acme/toolkit/vitest-setup
```

--------------------------------

### Example Registry File Structure

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/registry/github.mdx

Illustrates the directory structure for a registry item that includes configuration, documentation, and setup files.

```txt
registry.json
config
└── vitest.config.ts
docs
└── testing.md
test
└── setup.ts
```

--------------------------------

### example-item.json

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/registry/examples.mdx

Example demonstrating the use of the 'devDependencies' field to install packages as development dependencies for a registry item.

```json
{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "custom-item",
  "type": "registry:item",
  "devDependencies": ["@types/mdx"],
  "files": [
    {
      "path": "lib/mdx.ts",
      "content": "...",
      "type": "registry:lib"
    }
  ]
}
```

--------------------------------

### example-style.json

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/registry/examples.mdx

JSON configuration to install a block from the shadcn/ui registry and override its primitives with custom ones.

```json
{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "custom-login",
  "type": "registry:block",
  "registryDependencies": [
    "login-01",
    "https://example.com/r/button.json",
    "https://example.com/r/input.json",
    "https://example.com/r/label.json"
  ]
}
```

--------------------------------

### .eslintrc.json

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/registry/examples.mdx

Example of a universal registry item for installing a custom ESLint configuration file.

```json
{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "my-eslint-config",
  "type": "registry:item",
  "files": [
    {
      "path": "/path/to/your/registry/default/custom-eslint.json",
      "type": "registry:file",
      "target": "~/.eslintrc.json",
      "content": "..."
    }
  ]
}
```

--------------------------------

### Install Hover Card via CLI or npm

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/components/radix/hover-card.mdx

Use the CLI for automated setup or install the radix-ui dependency manually.

```bash
npx shadcn@latest add hover-card
```

```bash
npm install radix-ui
```

--------------------------------

### Installation Commands

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/components/aria/context-menu.mdx

Install the component via CLI or add the required dependencies manually.

```bash
npx shadcn@latest add context-menu
```

```bash
npm install react-aria-components
```

--------------------------------

### Install Context Menu

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/components/base/context-menu.mdx

Install the component using the CLI or manually add the Base UI dependency.

```bash
npx shadcn@latest add context-menu
```

```bash
npm install @base-ui/react
```

--------------------------------

### Install Item from GitHub Registry

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/changelog/2026-06-github-registries.mdx

Use this command to install an item from any public GitHub repository configured as a shadcn registry. The generic form shows the required path structure, followed by a concrete example.

```bash
npx shadcn@latest add <username>/<repo>/<item>
```

```bash
npx shadcn@latest add acme/toolkit/project-conventions
```

--------------------------------

### Install Button Group via CLI or Manual

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/components/radix/button-group.mdx

Use the CLI to add the component automatically or install the radix-ui dependency for manual setup.

```bash
npx shadcn@latest add button-group
```

```bash
npm install radix-ui
```

--------------------------------

### Installation

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/components/radix/alert-dialog.mdx

Install the component via the shadcn CLI or manually add the Radix UI primitive dependency.

```bash
npx shadcn@latest add alert-dialog
```

```bash
npm install radix-ui
```

--------------------------------

### Install Input OTP

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/components/base/input-otp.mdx

Install the component using the CLI or manually via npm.

```bash
npx shadcn@latest add input-otp
```

```bash
npm install input-otp
```

--------------------------------

### Using Component Preview

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/examples/README.md

How to use the example in documentation by referencing its name (filename without .tsx).

```tsx
<ComponentPreview name="button-loading" />
```

--------------------------------

### Install Tooltip component

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/components/aria/tooltip.mdx

Install the component using the shadcn CLI or manually via npm.

```bash
npx shadcn@latest add tooltip
```

```bash
npm install react-aria-components
```

--------------------------------

### Initial setup

Source: https://github.com/shadcn-ui/ui/blob/main/packages/shadcn/test/fixtures/frameworks/remix-indie-stack/README.md

Command to run the initial setup script for the project.

```sh
npm run setup
```

--------------------------------

### Install Alert Dialog

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/components/aria/alert-dialog.mdx

Install the component using the shadcn CLI or manually by adding the required dependencies.

```bash
npx shadcn@latest add alert-dialog
```

```bash
npm install react-aria-components
```

--------------------------------

### Install Aspect Ratio

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/components/base/aspect-ratio.mdx

Install the component using the CLI or manually by adding the required dependencies.

```bash
npx shadcn@latest add aspect-ratio
```

```bash
npm install @base-ui/react
```

--------------------------------

### example.json

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/changelog/2026-05-package-imports-target-aliases.mdx

Example of a registry item using target aliases in files[].target to install files under configured shadcn directories.

```json
{
  "files": [
    {
      "path": "registry/default/ai/prompt-input.tsx",
      "type": "registry:ui",
      "target": "@ui/ai/prompt-input.tsx"
    }
  ]
}
```

--------------------------------

### Install React Aria dependencies

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/components/aria/accordion.mdx

Install the required react-aria-components package for manual setup.

```bash
npm install react-aria-components
```

--------------------------------

### Install Progress component

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/components/aria/progress.mdx

Install the component using the shadcn CLI or manually via npm.

```bash
npx shadcn@latest add progress
```

```bash
npm install react-aria-components
```

--------------------------------

### Start the development server

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/(root)/_blocks.mdx

Launches the documentation site locally to preview your block changes.

```bash
pnpm www:dev
```

--------------------------------

### Install Avatar component

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/components/radix/avatar.mdx

Use the shadcn CLI for automatic installation or manually install the Radix UI primitive.

```bash
npx shadcn@latest add avatar
```

```bash
npm install radix-ui
```

--------------------------------

### Install Font

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/rtl/start.mdx

Install the Noto Sans Arabic font using Fontsource.

```bash
npm install @fontsource-variable/noto-sans-arabic
```

--------------------------------

### Installation

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/components/base/navigation-menu.mdx

Install the Navigation Menu component using the shadcn/ui CLI.

```bash
npx shadcn@latest add navigation-menu
```

--------------------------------

### Installation - Command Line Interface

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/components/base/label.mdx

Install the Label component using the shadcn/ui CLI.

```bash
npx shadcn@latest add label
```

--------------------------------

### Initialize or create a project

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/(root)/cli.mdx

Use the init command or its alias create to set up project configurations, install dependencies, and configure CSS variables.

```bash
npx shadcn@latest init
```

```bash
npx shadcn@latest create
```

--------------------------------

### Add all available components

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/(root)/new.mdx

Add all available components to your project.

```bash
npx shadcn@latest add --all
```

--------------------------------

### Connect the MCP Server

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/(root)/new.mdx

Initialize the shadcn MCP server for your AI assistant.

```bash
npx shadcn@latest mcp init
```

--------------------------------

### Install Resizable component

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/components/radix/resizable.mdx

Add the resizable component to your project using the CLI or manual installation.

```bash
npx shadcn@latest add resizable
```

```bash
npm install react-resizable-panels
```

--------------------------------

### Manual Installation Dependencies

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/components/aria/input-otp.mdx

Install the core input-otp package manually using npm.

```bash
npm install input-otp
```

--------------------------------

### Initialize project using shadcn CLI

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/changelog/2023-06-new-cli.mdx

Use the init command to set up your project and generate the initial components.json configuration.

```bash
npx shadcn@latest init
```

--------------------------------

### Install Radix UI dependency

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/components/radix/accordion.mdx

Install the core Radix UI primitive required for the manual setup of the Accordion component.

```bash
npm install radix-ui
```

--------------------------------

### example-style.json

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/registry/examples.mdx

Custom style from scratch

```json
{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "extends": "none",
  "name": "new-style",
  "type": "registry:style",
  "dependencies": ["tailwind-merge", "clsx"],
  "registryDependencies": [
    "utils",
    "https://example.com/r/button.json",
    "https://example.com/r/input.json",
    "https://example.com/r/label.json",
    "https://example.com/r/select.json"
  ],
  "cssVars": {
    "theme": {
      "font-sans": "Inter, sans-serif"
    },
    "light": {
      "main": "#88aaee",
      "bg": "#dfe5f2",
      "border": "#000",
      "text": "#000",
      "ring": "#000"
    },
    "dark": {
      "main": "#88aaee",
      "bg": "#272933",
      "border": "#000",
      "text": "#e6e6e6",
      "ring": "#fff"
    }
  }
}
```

--------------------------------

### .cursor/rules/custom-python.mdc

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/registry/examples.mdx

Example of a universal registry item that installs custom Cursor rules for Python, demonstrating an explicit target for files.

```json
{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "python-rules",
  "type": "registry:item",
  "files": [
    {
      "path": "/path/to/your/registry/default/custom-python.mdc",
      "type": "registry:file",
      "target": "~/.cursor/rules/custom-python.mdc",
      "content": "..."
    }
  ]
}
```

--------------------------------

### Install Select component

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/components/base/select.mdx

Install via the shadcn CLI or manually add the Base UI peer dependency.

```bash
npx shadcn@latest add select
```

```bash
npm install @base-ui/react
```

--------------------------------

### Install Sonner via CLI or Package Manager

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/components/radix/sonner.mdx

Use the shadcn CLI for automated setup or install the required dependencies manually.

```bash
npx shadcn@latest add sonner
```

```bash
npm install sonner next-themes
```

--------------------------------

### get(count?)

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/helpers/ai-sdk.mdx

Retrieves cloned messages from the start of the conversation.

```APIDOC
## get(count?)

### Description
Return cloned messages from the start of the conversation. Throws an error if count is negative or not an integer.

### Parameters
#### Arguments
- **count** (number) - Optional - The number of messages to retrieve.

### Returns
- **UIMessage[]** - An array of cloned message objects.
```

--------------------------------

### Add multiple components

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/(root)/new.mdx

Add multiple components at once using the CLI.

```bash
npx shadcn@latest add button card input label
```

--------------------------------

### Example Secure Registry Setup for Operators

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/registry/namespace.mdx

This configuration illustrates a secure registry setup for operators, including HTTPS and authorization headers with versioning.

```json
{
  "@company": {
    "url": "https://registry.company.com/v1/{name}.json",
    "headers": {
      "Authorization": "Bearer ${COMPANY_TOKEN}",
      "X-Registry-Version": "1.0"
    }
  }
}
```

--------------------------------

### Install via CLI

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/components/aria/input.mdx

Use the shadcn CLI to add the input component to your project.

```bash
npx shadcn@latest add input
```

--------------------------------

### Install Context Menu

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/components/radix/context-menu.mdx

Install the component using the shadcn CLI or manually add the Radix UI dependency.

```bash
npx shadcn@latest add context-menu
```

```bash
npm install radix-ui
```

--------------------------------

### Install Avatar component using CLI or npm

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/components/base/avatar.mdx

Use the CLI for automated setup or manually install the Base UI React package as a dependency.

```bash
npx shadcn@latest add avatar
```

```bash
npm install @base-ui/react
```

--------------------------------

### Import and Use Card Component (shadcn/create)

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/installation/tanstack.mdx

Example of importing and using the Card component in a TanStack Start route file after using shadcn/create.

```tsx
import { createFileRoute } from "@tanstack/react-router"

import {
  Card,
  CardContent,
  CardDescription,
  CardHeader,
  CardTitle,
} from "@/components/ui/card"

export const Route = createFileRoute("/")({
  component: App,
})

function App() {
  return (
    <Card className="max-w-sm">
      <CardHeader>
        <CardTitle>Project Overview</CardTitle>
        <CardDescription>
          Track progress and recent activity for your TanStack Start app.
        </CardDescription>
      </CardHeader>
      <CardContent>
        Your design system is ready. Start building your next component.
      </CardContent>
    </Card>
  )
}
```

--------------------------------

### Install via CLI

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/components/aria/switch.mdx

Use the shadcn CLI to add the switch component to your project.

```bash
npx shadcn@latest add switch
```

--------------------------------

### Get List of Installed shadcn/ui Components

Source: https://github.com/shadcn-ui/ui/blob/main/skills/shadcn/cli.md

Run this command to retrieve a list of currently installed shadcn/ui components in your project. This is useful for the smart merge workflow when updating components.

```bash
npx shadcn@latest info
```

--------------------------------

### View Specific Component Details Examples

Source: https://github.com/shadcn-ui/ui/blob/main/skills/shadcn/cli.md

Examples demonstrating how to view details for a shadcn component using its name or a full path.

```bash
npx shadcn@latest view @shadcn/button
```

```bash
npx shadcn@latest view owner/repo/item
```

--------------------------------

### Install Multiple Resources with Override

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/registry/namespace.mdx

When installing multiple resources, the last resolved resource with conflicting files will override previous ones. In this example, `login-form.ts` from `@custom/login-form` will override the one from `@acme/auth`.

```bash
npx shadcn@latest add @acme/auth @custom/login-form
```

--------------------------------

### alias-parent.json

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/registry/examples.mdx

Shows how registry dependencies can also utilize target placeholders, with a parent item installing an app component and a hook, and a child item installing a UI component and a helper.

```json
{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "alias-parent",
  "type": "registry:item",
  "registryDependencies": ["https://example.com/r/alias-child.json"],
  "files": [
    {
      "path": "registry/new-york/alias/target-alias-panel.tsx",
      "type": "registry:component",
      "target": "@components/target-alias-panel.tsx",
      "content": "..."
    },
    {
      "path": "registry/new-york/alias/use-target-alias.ts",
      "type": "registry:hook",
      "target": "@hooks/use-target-alias.ts",
      "content": "..."
    }
  ]
}
```

--------------------------------

### example-theme.json

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/registry/examples.mdx

Custom theme

```json
{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "custom-theme",
  "type": "registry:theme",
  "cssVars": {
    "light": {
      "background": "oklch(1 0 0)",
      "foreground": "oklch(0.141 0.005 285.823)",
      "primary": "oklch(0.546 0.245 262.881)",
      "primary-foreground": "oklch(0.97 0.014 254.604)",
      "ring": "oklch(0.746 0.16 232.661)",
      "sidebar-primary": "oklch(0.546 0.245 262.881)",
      "sidebar-primary-foreground": "oklch(0.97 0.014 254.604)",
      "sidebar-ring": "oklch(0.746 0.16 232.661)"
    },
    "dark": {
      "background": "oklch(1 0 0)",
      "foreground": "oklch(0.141 0.005 285.823)",
      "primary": "oklch(0.707 0.165 254.624)",
      "primary-foreground": "oklch(0.97 0.014 254.604)",
      "ring": "oklch(0.707 0.165 254.624)",
      "sidebar-primary": "oklch(0.707 0.165 254.624)",
      "sidebar-primary-foreground": "oklch(0.97 0.014 254.604)",
      "sidebar-ring": "oklch(0.707 0.165 254.624)"
    }
  }
}
```

--------------------------------

### Initialize shadcn/ui Monorepo Project

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/(root)/monorepo.mdx

Use this command to create a new monorepo project with shadcn/ui, setting up `web` and `ui` workspaces with Turborepo.

```bash
npx shadcn@latest init --monorepo
```

--------------------------------

### example-multiple-plugins.json

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/registry/examples.mdx

Illustrates adding multiple plugins, noting their automatic grouping and deduplication, along with their npm dependencies.

```json
{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "multiple-plugins",
  "type": "registry:item",
  "dependencies": [
    "@tailwindcss/typography",
    "@tailwindcss/forms",
    "tw-animate-css"
  ],
  "css": {
    "@plugin \"@tailwindcss/typography\"": {},
    "@plugin \"@tailwindcss/forms\"": {},
    "@plugin \"tw-animate-css\"": {}
  }
}
```

--------------------------------

### Use useDirection Hook

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/components/base/direction.mdx

Example of using the useDirection hook to get the current text direction.

```tsx
import { useDirection } from "@/components/ui/direction"

function MyComponent() {
  const direction = useDirection()
  return <div>Current direction: {direction}</div>
}
```

--------------------------------

### alias-child.json

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/registry/examples.mdx

Demonstrates using `files[].target` placeholders to install files under user-configured shadcn directories, resolving from `components.json`.

```json
{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "alias-child",
  "type": "registry:item",
  "files": [
    {
      "path": "registry/new-york/alias/target-alias-button.tsx",
      "type": "registry:ui",
      "target": "@ui/target-alias-button.tsx",
      "content": "..."
    },
    {
      "path": "registry/new-york/alias/target-alias-helper.ts",
      "type": "registry:lib",
      "target": "@lib/target-alias-helper.ts",
      "content": "..."
    },
    {
      "path": "registry/new-york/alias/prompt-input.tsx",
      "type": "registry:ui",
      "target": "@ui/ai/prompt-input.tsx",
      "content": "..."
    }
  ]
}
```

--------------------------------

### Install via CLI

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/components/aria/table.mdx

Use the shadcn CLI to add the table component to your project.

```bash
npx shadcn@latest add table
```

--------------------------------

### Install via CLI

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/components/aria/resizable.mdx

Add the resizable component to your project using the shadcn CLI.

```bash
npx shadcn@latest add resizable
```

--------------------------------

### components.json

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/(root)/components-json.mdx

Example configuration demonstrating multiple registry setups including public, private with authentication, and team-specific with versioning.

```json
{
  "registries": {
    "@shadcn": "https://ui.shadcn.com/r/{name}.json",
    "@company-ui": {
      "url": "https://registry.company.com/ui/{name}.json",
      "headers": {
        "Authorization": "Bearer ${COMPANY_TOKEN}"
      }
    },
    "@team": {
      "url": "https://team.company.com/{name}.json",
      "params": {
        "team": "frontend",
        "version": "${REGISTRY_VERSION}"
      }
    }
  }
}
```

--------------------------------

### Run the Website Workspace

Source: https://github.com/shadcn-ui/ui/blob/main/CONTRIBUTING.md

Starts the development server for the ui.shadcn.com website.

```bash
pnpm --filter=v4 dev
```

--------------------------------

### CLI TypeScript Opt-out Prompt

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/changelog/2023-07-javascript.mdx

Example of the CLI prompt when opting out of TypeScript during project setup.

```txt
Would you like to use TypeScript (recommended)? no
```

--------------------------------

### Get component documentation

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/changelog/2026-03-cli-v4.mdx

Command to retrieve documentation, code, and examples for a UI component.

```bash
npx shadcn@latest docs combobox
```

--------------------------------

### Generated Project Creation Command

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/installation/tanstack.mdx

Example of the `shadcn@latest init` command generated by shadcn/create, including a preset and template.

```bash
npx shadcn@latest init --preset [CODE] --template start
```

--------------------------------

### Dark Mode Setup with next-themes

Source: https://github.com/shadcn-ui/ui/blob/main/skills/shadcn/customization.md

Example of setting up class-based dark mode toggle using `next-themes` in a Next.js application.

```tsx
import { ThemeProvider } from "next-themes"

<ThemeProvider attribute="class" defaultTheme="system" enableSystem>
  {children}
</ThemeProvider>
```

--------------------------------

### example-media-import.json

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/registry/examples.mdx

Illustrates importing CSS conditionally using media queries for print and mobile styles.

```json
{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "responsive-import",
  "type": "registry:item",
  "css": {
    "@import \"print-styles.css\" print": {},
    "@import url(\"mobile.css\") screen and (max-width: 768px)": {}
  }
}
```

--------------------------------

### Get Project Context with shadcn info

Source: https://github.com/shadcn-ui/ui/blob/main/skills/shadcn/SKILL.md

Use this command to refresh or retrieve the current project context, including configuration and installed components.

```bash
npx shadcn@latest info
```

--------------------------------

### Scaffold a new project with CLI

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/installation/index.mdx

Command to initialize a new project using the shadcn CLI with a specified framework template.

```bash
npx shadcn@latest init -t [framework]
```

--------------------------------

### Retrieve messages using chat.get

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/helpers/tanstack-ai.mdx

Use get() to return cloned UIMessage values from the start of the conversation without modifying the chat state.

```ts
chat.get() // Every message.
chat.get(2) // The first two messages.
chat.get(0) // An empty initial conversation.
```

--------------------------------

### Popover Usage Example

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/changelog/2026-01-inline-side-styles.mdx

Demonstrates how to use the `side="inline-start"` prop with `PopoverContent`.

```tsx
<Popover>
  <PopoverTrigger>Open</PopoverTrigger>
  <PopoverContent side="inline-start">
    {/* Opens on the left in LTR, right in RTL */}
  </PopoverContent>
</Popover>
```

--------------------------------

### Import and Use Button Component

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/installation/tanstack.mdx

Example of importing and using the Button component within a TanStack React Router file after it has been added to the project.

```tsx
import { createFileRoute } from "@tanstack/react-router"

import { Button } from "@/components/ui/button"

export const Route = createFileRoute("/")({
  component: App,
})

function App() {
  return (
    <div className="flex min-h-svh items-center justify-center p-6">
      <Button>Click me</Button>
    </div>
  )
}
```

--------------------------------

### shadcn init command usage and options

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/(root)/cli.mdx

Reference for arguments and flags available when running the init command, including template selection and monorepo support.

```bash
Usage: shadcn init [options] [components...]

initialize your project and install dependencies

Arguments:
  components                 names, url or local path to component

Options:
  -t, --template <template>  the template to use. (next, vite, start, react-router, laravel, astro)
  -b, --base <base>          the component library to use. (base, radix, aria)
  -p, --preset [name]        use a preset configuration
  -y, --yes                  skip confirmation prompt. (default: true)
  -d, --defaults             use default configuration: --template=next --preset=nova (default: false)
  -f, --force                force overwrite of existing configuration. (default: false)
  -c, --cwd <cwd>            the working directory. defaults to the current directory.
  -n, --name <name>          the name for the new project.
  -s, --silent               mute output. (default: false)
  --css-variables            use css variables for theming. (default: true)
  --no-css-variables         do not use css variables for theming.
  --monorepo                 scaffold a monorepo project.
  --no-monorepo              skip the monorepo prompt.
  --rtl                      enable RTL support.
  --no-rtl                   disable RTL support.
  --pointer                  enable pointer cursor for buttons.
  --no-pointer               disable pointer cursor for buttons.
  --reinstall                re-install existing UI components.
  --no-reinstall             do not re-install existing UI components.
  -h, --help                 display help for command
```

--------------------------------

### Define Vitest Setup Registry Item in JSON

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/registry/github.mdx

Defines a registry item named "vitest-setup" in registry.json, specifying multiple files with their paths and target destinations for installation.

```json
{
  "$schema": "https://ui.shadcn.com/schema/registry.json",
  "name": "acme-toolkit",
  "homepage": "https://github.com/acme/toolkit",
  "items": [
    {
      "name": "vitest-setup",
      "type": "registry:item",
      "title": "Vitest Setup",
      "description": "A Vitest setup with project defaults and docs.",
      "files": [
        {
          "path": "config/vitest.config.ts",
          "type": "registry:file",
          "target": "~/vitest.config.ts"
        },
        {
          "path": "test/setup.ts",
          "type": "registry:file",
          "target": "~/test/setup.ts"
        },
        {          "path": "docs/testing.md",
          "type": "registry:file",
          "target": "~/docs/testing.md"
        }
      ]
    }
  ]
}
```

--------------------------------

### Accessing Message Scroller Scroll State (TypeScript)

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/components/radix/message-scroller.mdx

Destructure `useMessageScrollerScrollable` to get `start` and `end` boolean flags, indicating if the scroller can still scroll towards the beginning or end of the list.

```tsx
const { start, end } = useMessageScrollerScrollable()
```

--------------------------------

### Anchoring a User Message in Message Scroller Item (TSX)

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/components/radix/message-scroller.mdx

Use the `scrollAnchor` prop on a `MessageScrollerItem` to designate a specific row as the start of a new conversation turn. This example anchors the item if the message role is 'user'.

```tsx
// This tells the scroller to anchor the user's message for the next turn.
<MessageScrollerItem
  messageId={message.id}
  scrollAnchor={message.role === "user"}
/>
```

--------------------------------

### CLI initialization prompts

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/changelog/2023-06-new-cli.mdx

The CLI asks a series of questions to configure styles, colors, paths, and React Server Components support.

```txt
Which style would you like to use? › Default
Which color would you like to use as base color? › Slate
Where is your global CSS file? › › app/globals.css
Do you want to use CSS variables for colors? › no / yes
Where is your tailwind.config.js located? › tailwind.config.js
Configure the import alias for components: › @/components
Configure the import alias for utils: › @/lib/utils
Are you using React Server Components? › no / yes
```

--------------------------------

### example-import.json

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/registry/examples.mdx

Demonstrates a basic CSS import using "@import \"tailwindcss\"" and a local CSS file.

```json
{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "custom-import",
  "type": "registry:component",
  "css": {
    "@import \"tailwindcss\"": {},
    "@import \"./styles/base.css\"": {}
  }
}
```

--------------------------------

### Get shadcn/ui Project Context

Source: https://github.com/shadcn-ui/ui/blob/main/skills/shadcn/SKILL.md

Use this command to retrieve the current shadcn/ui project configuration and installed components. The output is in JSON format. Remember to use the appropriate package runner (npx, pnpm dlx, or bunx) for your project.

```shell
!`npx shadcn@latest info --json`
```

--------------------------------

### Install Recharts Dependency

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/components/aria/chart.mdx

Manually install the recharts library if you are not using the CLI for installation.

```bash
npm install recharts
```

--------------------------------

### Install via CLI

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/components/aria/combobox.mdx

Add the combobox component to your project using the shadcn CLI.

```bash
npx shadcn@latest add combobox
```

--------------------------------

### Install via CLI

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/components/aria/textarea.mdx

Use the shadcn CLI to add the textarea component to your project.

```bash
npx shadcn@latest add textarea
```

--------------------------------

### Install Embla Carousel Dependency

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/components/aria/carousel.mdx

Manually install the embla-carousel-react package when not using the CLI for installation.

```bash
npm install embla-carousel-react
```

--------------------------------

### New Example File Content

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/examples/README.md

Example content for a new .tsx file in examples/base or examples/radix.

```tsx
// examples/base/button-loading.tsx
import { Button } from "@/styles/base-nova/ui/button"

export function ButtonLoading() {
  return <Button disabled>Loading...</Button>
}
```

--------------------------------

### Initialize New shadcn/ui Project

Source: https://github.com/shadcn-ui/ui/blob/main/skills/shadcn/SKILL.md

Commands to create a new shadcn/ui project, specifying project name, preset, and template.

```bash
npx shadcn@latest init --name my-app --preset base-nova
npx shadcn@latest init --name my-app --preset a2r6bw --template vite
```

--------------------------------

### Add a single component

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/(root)/new.mdx

Use the CLI to add a single component to your project.

```bash
npx shadcn@latest add button
```

--------------------------------

### Install Base UI Dependency

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/components/aria/drawer.mdx

Install the core Base UI package required for the drawer component when performing a manual installation.

```bash
npm install @base-ui/react
```

--------------------------------

### Initialize Existing shadcn/ui Project

Source: https://github.com/shadcn-ui/ui/blob/main/skills/shadcn/SKILL.md

Commands to initialize an existing project with shadcn/ui, applying a preset or using default settings.

```bash
npx shadcn@latest init --preset base-nova
npx shadcn@latest init --defaults  # shortcut: --template=next --preset=nova (base style implied)
```

--------------------------------

### example-style.json

Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/registry/examples.mdx

Custom colors

```json
{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "custom-style",
  "type": "registry:style",
  "cssVars": {
    "light": {
      "brand": "oklch(0.99 0.00 0)"
    },
    "dark": {
      "brand": "oklch(0.14 0.00 286)"
    }
  }
}
```