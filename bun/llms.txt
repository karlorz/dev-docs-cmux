### View development server startup output

Source: https://github.com/oven-sh/bun/blob/main/docs/bundler/html-static.mdx

Example output when starting the Bun dev server with an HTML entry point.

```text
Bun v1.3.3
ready in 6.62ms
→ http://localhost:3000/
Press h + Enter to show shortcuts
```

--------------------------------

### Qwik project creation CLI prompts

Source: https://github.com/oven-sh/bun/blob/main/docs/guides/ecosystem/qwik.mdx

Example interactive terminal prompts and output during Qwik project setup.

```txt
      ............
    .::: :--------:.
   .::::  .:-------:.
  .:::::.   .:-------.
  ::::::.     .:------.
 ::::::.        :-----:
 ::::::.       .:-----.
  :::::::.     .-----.
   ::::::::..   ---:.
    .:::::::::. :-:.
     ..::::::::::::
             ...::::


┌  Let's create a  Qwik App  ✨ (v1.20.0)
│
◇  Where would you like to create your new project? (Use '.' or './' for current directory)
│  ./my-app
│
●  Creating new project in  /path/to/my-app  ... 🐇
│
◇  Select a starter
│  Playground App (Qwik City + Qwik)
│
◇  Would you like to install bun dependencies?
│  Yes
│
◇  Initialize a new git repository?
│  No
│
◇  Finishing the install. Wanna hear a joke?
│  Yes
│
○  ────────────────────────────────────────────────────────╮
│                                                          │
│  How do you know if there’s an elephant under your bed?  │
│  Your head hits the ceiling!                             │
│                                                          │
├──────────────────────────────────────────────────────────╯
│
◇  App Created 🐰
│
◇  Installed bun dependencies 📋
│
○  Result ─────────────────────────────────────────────╮
│                                                      │
│  Success!  Project created in my-app directory       │
│                                                      │
│  Integrations? Add Netlify, Cloudflare, Tailwind...  │
│  bun qwik add                                        │
│                                                      │
│  Relevant docs:                                      │
│  https://qwik.dev/docs/getting-started/              │
│                                                      │
│  Questions? Start the conversation at:               │
│  https://qwik.dev/chat                               │
│  https://twitter.com/QwikDev                         │
│                                                      │
│  Presentations, Podcasts and Videos:                 │
│  https://qwik.dev/media/                             │
│                                                      │
│  Next steps:                                         │
│  cd my-app                                           │
│  bun start                                           │
│                                                      │
│                                                      │
├──────────────────────────────────────────────────────╯
│
└  Happy coding! 🎉

```

--------------------------------

### Setup commands

Source: https://github.com/oven-sh/bun/blob/main/packages/bun-lambda/README.md

Commands to clone the repository, set up sparse checkout, install dependencies, and publish the layer.

```sh
git clone --filter=blob:none --sparse https://github.com/oven-sh/bun.git
git -C bun sparse-checkout set packages/bun-lambda
cd bun/packages/bun-lambda
bun install
bun run publish-layer
```

--------------------------------

### Manual CI Setup - Install packages

Source: https://github.com/oven-sh/bun/blob/main/test/js/bun/secrets-ci-setup.md

Commands to manually install required packages and then run the tests.

```bash
apt-get update && apt-get install -y libsecret-1-dev gnome-keyring dbus-x11

bun test test/js/bun/secrets.test.ts
```

--------------------------------

### Example text output for bun pm licenses

Source: https://github.com/oven-sh/bun/blob/main/docs/pm/cli/pm.mdx

Sample output showing installed dependencies grouped under MIT and Unknown licenses, with dev dependencies marked.

```txt
MIT (2)
├── path-parse@1.0.6
└── resolve@1.9.0

Unknown (4)
├── a-dep@1.0.1 (dev)
├── no-deps@1.0.0
├── no-deps@1.0.1
└── one-dep@1.0.0
```

--------------------------------

### Navigate to directory and install dependencies

Source: https://github.com/oven-sh/bun/blob/main/docs/guides/ecosystem/hono.mdx

Change into the created project directory and install project dependencies.

```sh
cd myapp
bun install
```

--------------------------------

### Install dependencies with Bun

Source: https://github.com/oven-sh/bun/blob/main/docs/guides/ecosystem/solidstart.mdx

Navigates into the newly created project directory and installs all dependencies.

```sh
cd my-app
bun install
```

--------------------------------

### Configuring setup lifecycle hooks in package.json for Bun templates

Source: https://github.com/oven-sh/bun/blob/main/docs/runtime/templating/create.mdx

Configures preinstall, postinstall, and start scripts within the `bun-create` configuration block. Bun strips this section after scaffolding.

```json
{
  "name": "@bun-examples/simplereact",
  "version": "0.0.1",
  "main": "index.js",
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  },
  "bun-create": {
    "preinstall": "echo 'Installing...'", // a single command
    "postinstall": ["echo 'Done!'"], // an array of commands
    "start": "bun run echo 'Hello world!'"
  }
}
```

--------------------------------

### Install dependencies with Bun

Source: https://github.com/oven-sh/bun/blob/main/docs/guides/ecosystem/vite.mdx

Navigate into the created project directory and install all required packages using Bun.

```bash
cd my-app
bun install
```

--------------------------------

### View gcloud initialization output

Source: https://github.com/oven-sh/bun/blob/main/docs/guides/deployment/google-cloud-run.mdx

Example terminal output and prompts during gcloud project creation and setup.

```txt
Welcome! This command will take you through the configuration of gcloud.

You must sign in to continue. Would you like to sign in (Y/n)? Y
You are signed in as [email@example.com].

Pick cloud project to use:
 [1] existing-bun-app-1234
 [2] Enter a project ID
 [3] Create a new project
Please enter numeric choice or text value (must exactly match list item): 3

Enter a Project ID. my-bun-app
Your current project has been set to: [my-bun-app]

The Google Cloud CLI is configured and ready to use!
```

--------------------------------

### Install Bun using the install script

Source: https://github.com/oven-sh/bun/blob/main/README.md

This is the recommended method for installing Bun on Linux and macOS systems.

```sh
# with install script (recommended)
curl -fsSL https://bun.com/install | bash
```

--------------------------------

### Install dependencies with Bun

Source: https://github.com/oven-sh/bun/blob/main/docs/guides/ecosystem/remix.mdx

Navigate into the project directory and install required dependencies.

```sh
cd my-remix-app
bun install
```

--------------------------------

### Install

Source: https://github.com/oven-sh/bun/blob/main/test/js/node/test/fixtures/postject-copy/node_modules/postject/README.markdown

Command to install postject globally.

```sh
npm i -g postject
```

--------------------------------

### Putting it all together

Source: https://github.com/oven-sh/bun/blob/main/packages/bun-uws/misc/READMORE.md

A complete example demonstrating route definition, listening on a port, and running the event loop with `App.run`.

```c++
int main() {
    uWS::App().get("/*", [](auto *res, auto *req) {
        res->end("Hello World!");
    }).listen(9001, [](auto *listenSocket) {
        if (listenSocket) {
            std::cout << "Listening for connections..." << std::endl;
        }
    }).run();

    std::cout << "Shoot! We failed to listen and the App fell through, exiting now!" << std::endl;
}
```

--------------------------------

### GitHub Actions (Explicit Package Installation)

Source: https://github.com/oven-sh/bun/blob/main/test/js/bun/secrets-ci-setup.md

Example of running Bun secrets tests in GitHub Actions with explicit installation of keyring packages.

```yaml
- name: Install keyring packages  
  run: |
    sudo apt-get update
    sudo apt-get install -y libsecret-1-dev gnome-keyring dbus-x11

- name: Run secrets tests
  run: bun test test/js/bun/secrets.test.ts
```

--------------------------------

### Running npm starter packages with bun create and bunx

Source: https://github.com/oven-sh/bun/blob/main/docs/runtime/templating/create.mdx

Demonstrates equivalent commands for invoking npm starter packages such as `create-remix`.

```sh
bun create remix
bunx create-remix
```

--------------------------------

### Required Packages Installation

Source: https://github.com/oven-sh/bun/blob/main/test/js/bun/secrets-ci-setup.md

Command to install necessary packages on Ubuntu/Debian systems.

```bash
apt-get install -y \
  libsecret-1-dev \
  gnome-keyring \
  dbus-x11
```

--------------------------------

### Define a start script in package.json

Source: https://github.com/oven-sh/bun/blob/main/docs/quickstart.mdx

Add a start script entry to package.json to execute files with Bun.

```json
{
  "name": "my-app",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": { // [!code ++]
    "start": "bun run index.ts" // [!code ++]
  }, // [!code ++]
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^7"
  }
}
```

--------------------------------

### GitHub Actions (Auto-Setup)

Source: https://github.com/oven-sh/bun/blob/main/test/js/bun/secrets-ci-setup.md

Example of running Bun secrets tests in GitHub Actions with automatic setup.

```yaml
- name: Run secrets tests (auto-setup)
  run: bun test test/js/bun/secrets.test.ts
```

--------------------------------

### Initialize project and install Neon driver

Source: https://github.com/oven-sh/bun/blob/main/docs/guides/ecosystem/neon-serverless-postgres.mdx

Create a new project directory, initialize Bun configuration, and install the Neon serverless driver dependency.

```sh
mkdir bun-neon-postgres
cd bun-neon-postgres
bun init -y
bun add @neondatabase/serverless
```

--------------------------------

### Initialize a new project with Bun

Source: https://github.com/oven-sh/bun/blob/main/docs/guides/ecosystem/prisma.mdx

Create a project directory and initialize it with standard Bun files.

```bash
mkdir prisma-app
cd prisma-app
bun init
```

--------------------------------

### BuildKite

Source: https://github.com/oven-sh/bun/blob/main/test/js/bun/secrets-ci-setup.md

Example of running Bun secrets tests in BuildKite.

```yaml
steps:
  - command: bun test test/js/bun/secrets.test.ts
    label: "🔐 Secrets API Tests"
```

--------------------------------

### Initialize project and install dependencies

Source: https://github.com/oven-sh/bun/blob/main/docs/guides/ecosystem/neon-drizzle.mdx

Sets up a new project directory with Bun and installs Drizzle ORM, the Neon serverless driver, and Drizzle Kit development tools.

```sh
mkdir bun-drizzle-neon
cd bun-drizzle-neon
bun init -y
bun add drizzle-orm @neondatabase/serverless
bun add -D drizzle-kit
```

--------------------------------

### Docker

Source: https://github.com/oven-sh/bun/blob/main/test/js/bun/secrets-ci-setup.md

Example of a Dockerfile for running Bun secrets tests, including optional pre-installation of packages.

```dockerfile
# Optional: pre-install packages for faster test startup
RUN apt-get update && apt-get install -y \
    libsecret-1-dev \
    gnome-keyring \
    dbus-x11

# Run test normally - setup is automatic
RUN bun test test/js/bun/secrets.test.ts
```

--------------------------------

### Start development server

Source: https://github.com/oven-sh/bun/blob/main/src/runtime/cli/init/README2.default.md

Command to start the development server for the project.

```bash
bun dev
```

--------------------------------

### Set credential with unrestricted access for CI

Source: https://github.com/oven-sh/bun/blob/main/test/js/bun/secrets-ci-setup.md

Example of using allowUnrestrictedAccess: true for Bun.secrets.set in CI environments on macOS.

```ts
// For CI environments where user interaction is not possible
await Bun.secrets.set({
  service: "ci-deployment", 
  name: "api-key",
  value: process.env.API_KEY,
  allowUnrestrictedAccess: true // Bypass macOS keychain user prompts
});
```

--------------------------------

### Initialize a new project with Bun

Source: https://github.com/oven-sh/bun/blob/main/docs/quickstart.mdx

Scaffolds a new project directory and prompts to select a template.

```bash
bun init my-app
```

--------------------------------

### Initialize a new Bun project

Source: https://github.com/oven-sh/bun/blob/main/docs/guides/ecosystem/discordjs.mdx

Sets up a new project directory and initializes default configuration files using Bun.

```sh
mkdir my-bot
cd my-bot
bun init
```

--------------------------------

### Delete credential with empty string

Source: https://github.com/oven-sh/bun/blob/main/test/js/bun/secrets-ci-setup.md

Examples showing how to delete a credential using Bun.secrets.delete or by setting an empty string with Bun.secrets.set.

```ts
// These are equivalent:
await Bun.secrets.delete({ service: "myapp", name: "token" });
await Bun.secrets.set({ service: "myapp", name: "token", value: "" });
```

--------------------------------

### Force Keyring Setup

Source: https://github.com/oven-sh/bun/blob/main/test/js/bun/secrets-ci-setup.md

Command to force the automatic keyring setup using an environment variable.

```bash
FORCE_KEYRING_SETUP=1 bun test test/js/bun/secrets.test.ts
```

--------------------------------

### Install Dependencies

Source: https://github.com/oven-sh/bun/blob/main/bench/README.md

Installs project dependencies using npm.

```bash
npm install
```

--------------------------------

### Preload global setup script via CLI

Source: https://github.com/oven-sh/bun/blob/main/docs/test/lifecycle.mdx

Executes the specified setup file before running any test files.

```bash
bun test --preload ./setup.ts
```

--------------------------------

### Run initial index.ts with Bun

Source: https://github.com/oven-sh/bun/blob/main/docs/quickstart.mdx

Navigates into the project directory and executes the default starter script.

```bash
cd my-app
bun run index.ts
```

```txt
Hello via Bun!
```

--------------------------------

### Automatic CI Setup

Source: https://github.com/oven-sh/bun/blob/main/test/js/bun/secrets-ci-setup.md

Command to run the secrets test, which automatically handles setup in CI environments.

```bash
bun test test/js/bun/secrets.test.ts
```

--------------------------------

### Set Up and Run Bun Site Benchmarks

Source: https://github.com/oven-sh/bun/blob/main/bench/site/README.md

Sets up required toolchains and dependencies, then runs the full benchmark suite or targeted subsets. Requires Linux x64 and external dependencies like hyperfine, bombardier, and PostgreSQL on PATH.

```sh
bench/site/setup-toolchains.sh                      # node, deno, pnpm/yarn/npm, bun release + canary -> $SITEBENCH_HOME/toolchains
bench/site/setup-repos.sh                           # rolldown/benchmarks clone + deps of bench/express, bench/websocket-server, bench/postgres
BUN=/abs/path/to/bun bench/site/run-all.sh          # everything: bundler http ws postgres install (~30 min with RUNS=3)
BUN=/abs/path/to/bun bench/site/run-all.sh http ws  # a subset
RUNS=1 BUN=... bench/site/run-all.sh                # quick smoke run
```

--------------------------------

### Ordered list start number beginning with zero

Source: https://github.com/oven-sh/bun/blob/main/test/js/bun/md/spec.txt

Example of an ordered list with a start number beginning with 0.

```markdown
0. ok
```

--------------------------------

### Ordered list start number with leading zeros

Source: https://github.com/oven-sh/bun/blob/main/test/js/bun/md/spec.txt

Example of an ordered list with a start number containing leading zeros.

```markdown
003. ok
```

--------------------------------

### Set up development environment using Nix

Source: https://github.com/oven-sh/bun/blob/main/docs/project/contributing.mdx

Enters an isolated Nix shell containing all required dependencies and builds Bun.

```bash
nix develop
bun bd
```

--------------------------------

### Valid ordered list start number

Source: https://github.com/oven-sh/bun/blob/main/test/js/bun/md/spec.txt

Example of an ordered list with a valid start number (nine digits or less).

```markdown
123456789. ok
```

--------------------------------

### Start production server with Bun

Source: https://github.com/oven-sh/bun/blob/main/docs/guides/ecosystem/sveltekit.mdx

Run the compiled production server entrypoint using Bun.

```sh
bun ./build/index.js
```

--------------------------------

### Invalid ordered list start number (negative)

Source: https://github.com/oven-sh/bun/blob/main/test/js/bun/md/spec.txt

Example of an ordered list with an invalid negative start number, which is not parsed as a list.

```markdown
-1. not ok
```

--------------------------------

### Example project directory structure before build

Source: https://github.com/oven-sh/bun/blob/main/docs/bundler/index.mdx

Visualizes source directory structure with entrypoint files located inside a subfolder.

```text
.
└── pages
  └── index.tsx
  └── settings.tsx
```

--------------------------------

### Defining a GET route

Source: https://github.com/oven-sh/bun/blob/main/packages/bun-uws/misc/READMORE.md

Example of attaching behavior to a URL route using the `get` method with `uWS::App`.

```c++
uWS::App().get("/hello", [](auto *res, auto *req) {
    res->end("Hello World!");
});
```

--------------------------------

### Install a security scanner using Bun CLI

Source: https://github.com/oven-sh/bun/blob/main/docs/pm/security-scanner-api.mdx

Installs the scanner package as a development dependency. Replace the example package with the specific scanner package you want to use.

```bash
bun add -d @oven/bun-security-scanner

```

--------------------------------

### Invalid ordered list start number (too many digits)

Source: https://github.com/oven-sh/bun/blob/main/test/js/bun/md/spec.txt

Example of an ordered list with an invalid start number (more than nine digits), which is not parsed as a list.

```markdown
1234567890. not ok
```

--------------------------------

### Interactive Vite scaffolding prompts

Source: https://github.com/oven-sh/bun/blob/main/docs/guides/ecosystem/vite.mdx

Example terminal selection prompts displayed during Vite project setup.

```txt
◇  Select a framework:
│  React
│
◇  Select a variant:
│  TypeScript
│
◇  Which linter to use?
│  Oxlint
│
◇  Install with bun and start now?
│  No
│
◇  Scaffolding project in /path/to/my-app...
```

--------------------------------

### Run HTML file with Bun

Source: https://github.com/oven-sh/bun/blob/main/docs/bundler/html-static.mdx

Start the development server and bundle the specified HTML entry point directly using the Bun CLI.

```bash
bun ./index.html
```

--------------------------------

### Initialize project directory with Bun

Source: https://github.com/oven-sh/bun/blob/main/docs/guides/ecosystem/prisma-postgres.mdx

Create a new directory and initialize a Bun project with bun init.

```bash
mkdir prisma-postgres-app
cd prisma-postgres-app
bun init
```

--------------------------------

### Script Profiling

Source: https://github.com/oven-sh/bun/blob/main/packages/bun-inspector-protocol/README.md

Example demonstrating how to start and stop script profiling and handle profiling updates and completion.

```typescript
// Start script profiling with sampling
await inspector.send("ScriptProfiler.startTracking", { includeSamples: true });

// Listen for profiling updates
inspector.on("ScriptProfiler.trackingUpdate", event => {
  console.log("Profiling event:", event);
});

// Stop profiling to get complete data
inspector.on("ScriptProfiler.trackingComplete", data => {
  if (data.samples) {
    // Process stack traces
    console.log(`Collected ${data.samples.stackTraces.length} stack traces`);
  }
});

await inspector.send("ScriptProfiler.stopTracking");
```

--------------------------------

### View create-solid interactive setup output

Source: https://github.com/oven-sh/bun/blob/main/docs/guides/ecosystem/solidstart.mdx

Shows the interactive CLI prompt selections and output when creating a SolidStart project.

```txt
┌
 Create-Solid v0.9.0
│
◇  Which version of SolidStart?
│  2 (Stable)
│
◇  Which template would you like to use?
│  basic
│
◇  Project created 🎉
│
◇  To get started, run: ─╮
│                        │
│  cd my-app             │
│  bun install           │
│  bun dev               │
│                        │
├────────────────────────╯
```

--------------------------------

### Memory Profiling

Source: https://github.com/oven-sh/bun/blob/main/packages/bun-inspector-protocol/README.md

Example demonstrating how to start and stop heap tracking and listen for garbage collection events.

```typescript
// Start heap tracking
await inspector.send("Heap.enable");
await inspector.send("Heap.startTracking");

// Listen for GC events
inspector.on("Heap.garbageCollected", ({ collection }) => {
  console.log(
    `GC completed: ${collection.type} (${collection.endTime - collection.startTime}ms)`,
  );
});

// ... perform operations to analyze ...

// Get heap snapshot
const { snapshotData } = await inspector.send("Heap.stopTracking");
// Process snapshotData to find memory leaks
```

--------------------------------

### Start the development server with Bun

Source: https://github.com/oven-sh/bun/blob/main/docs/guides/ecosystem/tanstack-start.mdx

Navigates into the project directory and starts the Vite development server using Bun.

```sh
cd my-tanstack-app
bun --bun run dev
```

--------------------------------

### Initialize a project using Bun CLI

Source: https://github.com/oven-sh/bun/blob/main/docs/snippets/cli/init.mdx

Scaffolds a new project interactively in the current or specified target directory.

```bash
bun init <folder?>
```

--------------------------------

### Benchmark against a local Bun H3 server

Source: https://github.com/oven-sh/bun/blob/main/packages/h3blast/README.md

Example of starting a Bun test server and then using h3blast to benchmark it.

```sh
bun test-server.js 3443 &
./h3blast -t 2 -c 4 -m 32 -d 10 https://127.0.0.1:3443/
```

--------------------------------

### Bring Up a Host

Source: https://github.com/oven-sh/bun/blob/main/scripts/darwin-ci/README.md

Commands to copy the provisioning scripts to a new host and initiate the setup process.

```sh
scp -r scripts/darwin-ci <admin>@<host>:
ssh <admin>@<host> 'darwin-ci/host.sh <hostname> tart --tags <tailscale tags>'
```

--------------------------------

### build.onStart

Source: https://github.com/oven-sh/bun/blob/main/docs/runtime/plugins.mdx

Registers a callback that executes when the bundler initializes and starts a new bundle.

```APIDOC
### Signature
```ts
build.onStart(callback: () => void | Promise<void>): void
```

### Description
Registers a callback that runs when the bundler starts a new bundle. The bundler waits until all `onStart()` callbacks have completed before continuing with the bundle process.

### Parameters
- **callback** (`() => void | Promise<void>`) - Required. Function to execute when bundling begins. Can return a Promise.

### Return Value
- `void`
```

--------------------------------

### Get elapsed nanoseconds with Bun.nanoseconds()

Source: https://github.com/oven-sh/bun/blob/main/docs/runtime/utils.mdx

Returns the number of nanoseconds elapsed since the current Bun process started as a number.

```typescript
Bun.nanoseconds();
// => 7288958
```

--------------------------------

### WWW Autolink Example

Source: https://github.com/oven-sh/bun/blob/main/test/js/bun/md/spec-permissive-autolinks.txt

Demonstrates a permissive WWW autolink, which starts with 'www.' and is implicitly converted to an HTTP URL.

```markdown
www.google.com/search?q=Markdown
```

```html
<p><a href="http://www.google.com/search?q=Markdown">www.google.com/search?q=Markdown</a></p>
```

--------------------------------

### Start development server with Bun

Source: https://github.com/oven-sh/bun/blob/main/docs/guides/ecosystem/sveltekit.mdx

Navigate into the project directory and start the Vite dev server using Bun's runtime.

```sh
cd my-app
bun --bun run dev
```

--------------------------------

### Listening on a port

Source: https://github.com/oven-sh/bun/blob/main/packages/bun-uws/misc/READMORE.md

Example of how to start listening for new connections on a specified port using `App.listen`.

```c++
App.listen(port, [](auto *listenSocket) {
    /* listenSocket is either nullptr or us_listen_socket */
})
```

--------------------------------

### Output of all installed dependencies command

Source: https://github.com/oven-sh/bun/blob/main/docs/pm/cli/pm.mdx

Sample output displaying the entire dependency tree.

```txt
/path/to/project node_modules (135)
├── @eslint-community/eslint-utils@4.4.0
├── @eslint-community/regexpp@4.5.0
├── @eslint/eslintrc@2.0.2
├── @eslint/js@8.38.0
├── @nodelib/fs.scandir@2.1.5
├── @nodelib/fs.stat@2.0.5
├── @nodelib/fs.walk@1.2.8
├── acorn@8.8.2
├── acorn-jsx@5.3.2
├── ajv@6.12.6
├── ansi-regex@5.0.1
├── ...
```

--------------------------------

### Bun.serve(options)

Source: https://github.com/oven-sh/bun/blob/main/docs/runtime/http/server.mdx

Configures and starts an HTTP server in Bun. Supports HTTP/3 over QUIC with TLS and customizable idle connection timeouts.

```APIDOC
## Bun.serve(options)

### Description
Starts a Bun HTTP server with support for HTTP/3 over QUIC, TLS configurations, and custom connection idle timeouts.

### Syntax
```ts
Bun.serve(options)
```

### Parameters
- **options** (object) - Configuration object for the HTTP server.
  - **fetch** (Function) - Request handler callback accepting a `req` parameter and returning a `Response`.
  - **http3** (boolean) - Optional. When `true`, enables listening for HTTP/3 over QUIC on UDP. Requires `tls` configuration.
  - **http1** (boolean) - Optional. When set to `false` with `http3: true`, disables TCP listening to serve HTTP/3 only.
  - **tls** (object) - Required if `http3` is enabled. Contains TLS credentials.
    - **key** (BunFile) - Private key file reference.
    - **cert** (BunFile) - Certificate file reference.
  - **idleTimeout** (number) - Optional. Inactivity timeout in seconds before closing connections (default is `10`, maximum is `255`, `0` disables).

### Examples

#### HTTP/3 Server
```ts
Bun.serve({
  tls: {
    key: Bun.file("./key.pem"),
    cert: Bun.file("./cert.pem"),
  },
  http3: true,
  fetch(req) {
    return new Response("Hello over HTTP/3!");
  },
});
```

#### Custom Idle Timeout
```ts
Bun.serve({
  idleTimeout: 30,
  fetch(req) {
    return new Response("Bun!");
  },
});
```
```

--------------------------------

### Define an HTTP route and start server in Elysia

Source: https://github.com/oven-sh/bun/blob/main/docs/guides/ecosystem/elysia.mdx

Sets up a basic GET route and listens for incoming requests on port 8080.

```ts
import { Elysia } from "elysia";

const app = new Elysia().get("/", () => "Hello Elysia").listen(8080);

console.log(`🦊 Elysia is running on port ${app.server?.port}...`);
```

--------------------------------

### Initialize and run a React app with Bun

Source: https://github.com/oven-sh/bun/blob/main/docs/guides/ecosystem/react.mdx

Scaffold a new React project and run commands for development, production builds, and server execution.

```bash
# Create a new React app
bun init --react

# Run the app in development mode
bun dev

# Build as a static site for production
bun run build

# Run the server in production
bun start
```

--------------------------------

### Example usage of onGC

Source: https://github.com/oven-sh/bun/blob/main/test/napi/node-napi-tests/test/common/README.md

Demonstrates how to install a garbage collection listener using the onGC function.

```js
require('../common');
const { onGC } = require('../common/gc');

onGC({}, { ongc() { console.log('collected'); } });
```

--------------------------------

### Initialize a new Bun project

Source: https://github.com/oven-sh/bun/blob/main/docs/guides/ecosystem/mongoose.mdx

Creates a new project directory and initializes it with Bun.

```sh
mkdir mongoose-app
cd mongoose-app
bun init
```

--------------------------------

### Basic HTML block of type 6

Source: https://github.com/oven-sh/bun/blob/main/test/js/bun/md/spec.txt

A simple example of an HTML block starting with <table> (type 6) and its parsed output.

```markdown+html
<table>
  <tr>
    <td>
           hi
    </td>
  </tr>
</table>

okay.
```

```markdown+html
<table>
  <tr>
    <td>
           hi
    </td>
  </tr>
</table>
<p>okay.</p>
```

--------------------------------

### bunx Command Examples

Source: https://github.com/oven-sh/bun/blob/main/docs/snippets/cli/bunx.mdx

Illustrates common use cases for `bunx`, including running migrations, formatting files, specifying package versions, handling differing binary/package names, and forcing Bun execution.

```bash
# Run Prisma migrations
bunx prisma migrate
```

```bash
# Format a file with Prettier
bunx prettier foo.js
```

```bash
# Run a specific version of a package
bunx uglify-js@3.14.0 app.js
```

```bash
# Use --package when binary name differs from package name
bunx -p @angular/cli ng new my-app
```

```bash
# Force running with Bun instead of Node.js, even if the executable contains a Node shebang
bunx --bun vite dev foo.js
```

--------------------------------

### Start HTTP server with Bun

Source: https://github.com/oven-sh/bun/blob/main/docs/quickstart.mdx

Runs the server script and logs the local listening URL.

```bash
bun run index.ts
```

```txt
Listening on http://localhost:3000/
```

--------------------------------

### Content not forming a list item due to indentation

Source: https://github.com/oven-sh/bun/blob/main/test/js/bun/md/spec.txt

Example where content starting with three spaces of indentation does not form a list item when a list marker is prepended.

```markdown
   foo

bar
```

--------------------------------

### Initialize a new Bun project

Source: https://github.com/oven-sh/bun/blob/main/docs/guides/ecosystem/gel.mdx

Create a new project directory and initialize it with Bun defaults.

```sh
mkdir my-gel-app
cd my-gel-app
bun init -y
```

--------------------------------

### Filter dependency types for update in Bun

Source: https://github.com/oven-sh/bun/blob/main/docs/pm/cli/update.mdx

Use `--dev` or `--prod` to target specific dependency groups in `package.json`. These flags only limit what gets updated, while remaining dependencies are still installed.

```sh
bun update --dev
bun update --prod --latest
bun update -D '@types/*'
bun update -i --prod
```

--------------------------------

### Initialize a new Bun project

Source: https://github.com/oven-sh/bun/blob/main/docs/guides/ecosystem/upstash.mdx

Creates a new Bun project directory and navigates into it using `bun init`.

```sh
bun init bun-upstash-redis
cd bun-upstash-redis
```

--------------------------------

### Bun.serve(params)

Source: https://github.com/oven-sh/bun/blob/main/docs/runtime/http/websockets.mdx

Starts an HTTP and WebSocket server instance using the provided configuration and WebSocket event handlers.

```APIDOC
### Function
`Bun.serve(params: object): Server`

### Parameters
- `params.fetch` (`(req: Request, server: Server) => Response | Promise<Response>`) - Handler for HTTP requests and WebSocket upgrade initiation.
- `params.websocket` (`object`, optional) - WebSocket server configuration and event handlers:
  - `message` (`(ws: ServerWebSocket, message: string | ArrayBuffer | Uint8Array) => void`) - Handler invoked when a message is received.
  - `open` (`(ws: ServerWebSocket) => void`, optional) - Handler invoked when a WebSocket connection is opened.
  - `close` (`(ws: ServerWebSocket, code: number, reason: string) => void`, optional) - Handler invoked when a WebSocket connection is closed.
  - `error` (`(ws: ServerWebSocket, error: Error) => void`, optional) - Handler invoked when an error occurs on a WebSocket.
  - `drain` (`(ws: ServerWebSocket) => void`, optional) - Handler invoked when the socket is ready to accept more data after backpressure.
  - `maxPayloadLength` (`number`, optional) - Maximum message size in bytes (default: `16777216` / 16 MB).
  - `idleTimeout` (`number`, optional) - Connection timeout in seconds (default: `120`).
  - `backpressureLimit` (`number`, optional) - Maximum backpressure in bytes before applying limit behavior (default: `16777216` / 16 MB).
  - `closeOnBackpressureLimit` (`boolean`, optional) - Close socket if backpressure limit is reached (default: `false`).
  - `sendPings` (`boolean`, optional) - Automatically send WebSocket pings (default: `true`).
  - `publishToSelf` (`boolean`, optional) - Receive messages published to subscribed topics by the same socket (default: `false`).
  - `perMessageDeflate` (`boolean | object`, optional) - Compression configuration options.

### Returns
- `Server` - The running server instance.
```

--------------------------------

### HTML block with div and nested markdown

Source: https://github.com/oven-sh/bun/blob/main/test/js/bun/md/spec.txt

An example of an HTML block starting with <div> (type 6) containing markdown content, demonstrating that the content within the block is treated as raw HTML.

```markdown+html
 <div>
  *hello*
         <foo><a>
```

```markdown+html
 <div>
  *hello*
         <foo><a>
```

--------------------------------

### HTTP/2 GOAWAY Handling with Stream Retries and Non-Replayable Bodies in Node.js

Source: https://github.com/oven-sh/bun/blob/main/test/js/web/fetch/H2_TEST_PORT_PLAN.md

This complex example shows how a server sends a GOAWAY frame with a `lastStreamID` that causes some client requests to be retried on a new connection (e.g., GET, POST with Buffer), while others with non-replayable bodies (e.g., POST with ReadableStream) are rejected.

```javascript
let sess = 0; server.on('session', s => { sess++; });
server.on('stream', (stream, h) => {
  if (sess === 1 && stream.id > 1) return; // let goaway kill them
  let body=[]; stream.on('data',c=>body.push(c)); stream.on('end',()=>{}
    stream.respond({':status':200}); stream.end(String(stream.id));
  });
  if (sess === 1 && stream.id === 1) setTimeout(()=>stream.session.goaway(0,1), 30);
});
```

--------------------------------

### Initialize a Qwik project with Bun

Source: https://github.com/oven-sh/bun/blob/main/docs/guides/ecosystem/qwik.mdx

Scaffolds a new Qwik application using Bun. Automatically detects Bun to handle project dependency installation.

```sh
bun create qwik
```

--------------------------------

### builder.onStart(callback)

Source: https://github.com/oven-sh/bun/blob/main/docs/bundler/esbuild.mdx

Registers a lifecycle callback invoked when the bundling process starts.

```APIDOC
## builder.onStart(callback)

### Description
Called when the bundling process begins.

### Method
`builder.onStart(callback)`

### Parameters
- **callback** (`() => void | Promise<void>`) - Function executed at the start of bundling.

### Example
```ts
builder.onStart(() => {
  console.log("Bundling started");
});
```
```

--------------------------------

### Install Dependencies

Source: https://github.com/oven-sh/bun/blob/main/test/js/third_party/pnpm/install_fixture/README.md

Command to install project dependencies using npm, pnpm, or yarn.

```bash
$ npm install # or pnpm install or yarn install
```

--------------------------------

### Initialize project with default options

Source: https://github.com/oven-sh/bun/blob/main/docs/snippets/cli/init.mdx

Accepts all default prompts automatically without interactive questions.

```bash
bun init -y
```

--------------------------------

### Configure development dependencies installation in TOML

Source: https://github.com/oven-sh/bun/blob/main/docs/runtime/bunfig.mdx

Specifies whether devDependencies are installed by bun install. Defaults to true.

```toml
[install]
dev = true
```

--------------------------------

### Perform a dry run installation

Source: https://github.com/oven-sh/bun/blob/main/docs/pm/cli/install.mdx

Simulates package installation without writing files to disk or installing packages.

```bash
bun install --dry-run
```

--------------------------------

### Install Prisma CLI and Postgres adapter

Source: https://github.com/oven-sh/bun/blob/main/docs/guides/ecosystem/prisma-postgres.mdx

Install Prisma development tools alongside the client and PostgreSQL adapter packages.

```bash
bun add -d prisma
bun add @prisma/client @prisma/adapter-pg
```

--------------------------------

### Install specific Bun version

Source: https://github.com/oven-sh/bun/blob/main/docs/guides/util/upgrade.mdx

Use the official installation script with a version parameter to install or pin a specific version of Bun.

```bash
curl -fsSL https://bun.sh/install | bash -s "bun-v1.3.3"
```

```powershell
iex "& {$(irm https://bun.sh/install.ps1)} -Version 1.3.3"
```

--------------------------------

### Install a specific Bun version

Source: https://github.com/oven-sh/bun/blob/main/docs/installation.mdx

Re-run the installation script with a target version tag or number to install older or pinned versions of Bun.

```bash
curl -fsSL https://bun.com/install | bash -s "bun-v1.3.3"
```

```powershell
iex "& {$(irm https://bun.com/install.ps1)} -Version 1.3.3"
```

--------------------------------

### Configure start script in package.json

Source: https://github.com/oven-sh/bun/blob/main/docs/guides/ecosystem/tanstack-start.mdx

Adds a start script to package.json to run the custom server script with Bun.

```json
{
  "scripts": {
    "build": "bun --bun vite build",
    "start": "bun run server.ts" // [!code ++]
  }
}
```

--------------------------------

### Bun.serve(options)

Source: https://github.com/oven-sh/bun/blob/main/docs/runtime/http/routing.mdx

Configures and starts an HTTP server with defined routes and request handlers. Supports asynchronous handlers, parameterized routes, static response objects, and file serving.

```APIDOC
## Bun.serve(options)

### Description
Configures and starts an HTTP server with declarative routing, supporting async/await handlers, route precedence, type-safe path parameters, and zero-allocation static responses.

### Syntax
```ts
Bun.serve(options)
serve(options)
```

### Parameters
- **options** (object) - Configuration options for the HTTP server.
  - **port** (number, optional) - The port number for the server to listen on.
  - **routes** (Record<string, Function | Response>, optional) - A map of URL path patterns to handler functions or static `Response` instances.

### Route Handler Specifications
- **Handler Function**: `(req: BunRequest) => Response | Promise<Response>` - A synchronous or asynchronous function returning a `Response` or `Promise<Response>`.
  - **req.params** (Record<string, string>) - Object containing automatically percent-decoded route parameter values.
- **Static Response**: `Response` - Direct `Response` object cached for zero-allocation dispatch without executing a handler function.

### Route Precedence Rules
Routes match in order of specificity:
1. Exact routes (`/users/all`)
2. Parameter routes (`/users/:id`)
3. Wildcard routes (`/users/*`)
4. Global catch-all (`/*`)

### Examples

#### Async Route Handlers
```ts
import { sql, serve } from "bun";

serve({
  port: 3001,
  routes: {
    "/api/version": async () => {
      const [version] = await sql`SELECT version()`;
      return Response.json(version);
    },
  },
});
```

#### Type-safe Route Parameters
```ts
import type { BunRequest } from "bun";

Bun.serve({
  routes: {
    "/orgs/:orgId/repos/:repoId": (req: BunRequest<"/orgs/:orgId/repos/:repoId">) => {
      const { orgId, repoId } = req.params;
      return Response.json({ orgId, repoId });
    },
  },
});
```

#### Static and File Responses
```ts
Bun.serve({
  routes: {
    "/health": new Response("OK"),
    "/logo.png": new Response(await Bun.file("./logo.png").bytes()),
    "/download.zip": new Response(Bun.file("./download.zip")),
  },
});
```
```

--------------------------------

### Configure dry run installation in TOML

Source: https://github.com/oven-sh/bun/blob/main/docs/runtime/bunfig.mdx

Resolves dependencies without installing them when set to true. Equivalent to passing --dry-run to bun install.

```toml
[install]
dryRun = false
```

--------------------------------

### Run fullstack development server using Bun CLI

Source: https://github.com/oven-sh/bun/blob/main/docs/bundler/fullstack.mdx

Start the development server using the Bun runtime.

```bash
bun run app.ts
```

--------------------------------

### Installation

Source: https://github.com/oven-sh/bun/blob/main/test/js/node/test/fixtures/postject-copy/node_modules/commander/Readme.md

Command to install Commander.js via npm.

```sh
npm install commander
```

--------------------------------

### Installation

Source: https://github.com/oven-sh/bun/blob/main/packages/bun-plugin-yaml/README.md

Command to install the bun-plugin-yaml package as a development dependency.

```sh
bun add bun-plugin-yaml -d
```

--------------------------------

### bun init <folder?>

Source: https://github.com/oven-sh/bun/blob/main/docs/snippets/cli/init.mdx

Initializes a new Bun project in the specified directory or the current directory with optional templates and preset configurations.

```APIDOC
## bun init <folder?>

### Description
Initializes project files and configuration for the chosen options. Exact files vary by template.

### Usage
```bash
bun init <folder?> [options]
```

### Arguments
- **folder** (string, optional) - Target directory to initialize the project in.

### Options
- **--yes**, **-y** (boolean) - Accept all default prompts without asking questions.
- **--minimal**, **-m** (boolean) - Only initialize type definitions (skip app scaffolding).
- **--react** (string | boolean) - Scaffold a React project. When used without a value, creates a baseline React app. Accepts preset values: `tailwind` (React app preconfigured with Tailwind CSS), `shadcn` (React app with `@shadcn/ui` and Tailwind CSS).
- **--help**, **-h** (boolean) - Print this help menu.

### Examples

**Accept all defaults**
```bash
bun init -y
```

**React**
```bash
bun init --react
```

**React + Tailwind CSS**
```bash
bun init --react=tailwind
```

**React + @shadcn/ui**
```bash
bun init --react=shadcn
```
```

--------------------------------

### Install Mongoose with Bun

Source: https://github.com/oven-sh/bun/blob/main/docs/guides/ecosystem/mongoose.mdx

Installs the Mongoose ODM library as a dependency.

```sh
bun add mongoose
```

--------------------------------

### Run project

Source: https://github.com/oven-sh/bun/blob/main/src/runtime/cli/init/README.default.md

Command to run the project's entry point.

```bash
bun run {[entryPoint]s}
```

--------------------------------

### Install discord.js package

Source: https://github.com/oven-sh/bun/blob/main/docs/guides/ecosystem/discordjs.mdx

Installs the discord.js library as a project dependency.

```sh
bun add discord.js
```

--------------------------------

### Create a TanStack Start app with Bun

Source: https://github.com/oven-sh/bun/blob/main/docs/guides/ecosystem/tanstack-start.mdx

Initializes a new TanStack Start application using the interactive TanStack CLI.

```sh
bunx @tanstack/cli create my-tanstack-app
```

--------------------------------

### Initialize Gel project instance

Source: https://github.com/oven-sh/bun/blob/main/docs/guides/ecosystem/gel.mdx

Initialize a new Gel database instance and create a gel.toml configuration file in the project root.

```sh
gel project init
```

--------------------------------

### Simple example with backticks

Source: https://github.com/oven-sh/bun/blob/main/test/js/bun/md/spec.txt

Here is a simple example with backticks:

```markdown
```
<
 >
```
```

--------------------------------

### Migrating from npm install to bun install

Source: https://github.com/oven-sh/bun/blob/main/docs/guides/install/from-npm-install-to-bun-install.mdx

Use `bun i` to install dependencies, `bun i -d` to add devDependencies, and `bun rm` to remove dependencies, replacing their `npm` equivalents.

```bash
# It only takes one command to migrate
bun i

# To add dependencies:
bun i @types/bun

# To add devDependencies:
bun i -d @types/bun

# To remove a dependency:
bun rm @types/bun
```

--------------------------------

### Bun.serve(options)

Source: https://github.com/oven-sh/bun/blob/main/docs/runtime/http/routing.mdx

Initializes and runs an HTTP server in Bun with route-based request dispatching and fallback handlers.

```APIDOC
## Bun.serve(options)

### Description
Starts an HTTP server that routes incoming requests using path patterns or falls back to a general `fetch` handler. Route handlers receive a `BunRequest` object and return a standard `Response` or `Promise<Response>`.

### Signature
```ts
Bun.serve(options: {
  routes?: Record<string, (req: BunRequest) => Response | Promise<Response>>;
  fetch?: (req: Request) => Response | Promise<Response>;
})
```

### Parameters
- **options** (object) - Server configuration object.
  - **routes** (Record<string, Function>) - Optional. Key-value mapping of path strings (static paths, parameters, wildcards) to handler functions.
  - **fetch** (Function) - Optional. Handler function invoked when an incoming request does not match any route defined in `routes`.

### Types
#### BunRequest
```ts
interface BunRequest<T extends string = string> extends Request {
  params: Record<T, string>;
  readonly cookies: CookieMap;
}
```

### Example
```ts
Bun.serve({
  routes: {
    "/": () => new Response("Home"),
    "/api": () => Response.json({ success: true }),
    "/users": async () => Response.json({ users: [] }),
  },
  fetch() {
    return new Response("Unmatched route");
  },
});
```
```