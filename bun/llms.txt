### Setup project and run tests

Source: https://github.com/oven-sh/bun/blob/main/packages/bun-plugin-yaml/README.md

Install dependencies and execute the test suite when contributing to the plugin.

```bash
$ bun install # project setup
$ bun test # run tests
```

--------------------------------

### Configure local template setup logic in `package.json`

Source: https://github.com/oven-sh/bun/blob/main/docs/runtime/templating/create.mdx

Defines `preinstall`, `postinstall`, and `start` scripts in the `bun-create` section of a local template's `package.json`.

```json
{
  "name": "@bun-examples/simplereact",
  "version": "0.0.1",
  "main": "index.js",
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  },
  "bun-create": {
    "preinstall": "echo 'Installing...'", // a single command
    "postinstall": ["echo 'Done!'"], // an array of commands
    "start": "bun run echo 'Hello world!'"
  }
}
```

--------------------------------

### Install project dependencies

Source: https://github.com/oven-sh/bun/blob/main/docs/guides/ecosystem/remix.mdx

Navigate into the project directory and install dependencies with bun install.

```sh
cd my-remix-app
bun install
```

--------------------------------

### Install project dependencies

Source: https://github.com/oven-sh/bun/blob/main/docs/guides/ecosystem/solidstart.mdx

Navigate into the newly created project directory and install dependencies with Bun.

```sh
cd my-app
bun install
```

--------------------------------

### Install a package using bun install

Source: https://github.com/oven-sh/bun/blob/main/docs/snippets/cli/install.mdx

Install a package by specifying its package name and target version.

```sh
bun install <name>@<version>
```

--------------------------------

### Install Bun in GitHub Actions with setup-bun

Source: https://github.com/oven-sh/bun/blob/main/docs/guides/install/cicd.mdx

Use the official oven-sh/setup-bun action in a GitHub Actions runner step to install Bun and execute commands like bun install.

```yaml
name: my-workflow
jobs:
  my-job:
    name: my-job
    runs-on: ubuntu-latest
    steps:
      # ...
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2 # [!code ++]

      # run any `bun` or `bunx` command
      - run: bun install # [!code ++]
```

--------------------------------

### Execute start script with bun run

Source: https://github.com/oven-sh/bun/blob/main/docs/quickstart.mdx

Runs the configured start script using bun run, starting the server with lower overhead than npm run.

```bash
bun run start
```

```txt
Listening on http://localhost:3000/
```

--------------------------------

### Run for production with bun start

Source: https://github.com/oven-sh/bun/blob/main/src/runtime/cli/init/README2.default.md

Run this command to start the application in production mode.

```bash
bun start
```

--------------------------------

### Explain package installation with bun why

Source: https://github.com/oven-sh/bun/blob/main/docs/pm/cli/why.mdx

Basic command syntax to show the dependency chain that caused a package to be installed.

```bash
bun why <package>
```

--------------------------------

### Set the installation strategy using the --linker flag

Source: https://github.com/oven-sh/bun/blob/main/docs/pm/isolated-installs.mdx

Pass `--linker isolated` or `--linker hoisted` to `bun install` to choose between strict dependency isolation and traditional flattened installs.

```bash
# Use isolated installs
bun install --linker isolated

# Use traditional hoisted installs
bun install --linker hoisted
```

--------------------------------

### Install dependencies with Bun

Source: https://github.com/oven-sh/bun/blob/main/bench/expect-to-equal/README.md

Install the benchmark dependencies using Bun package manager.

```bash
bun install
```

--------------------------------

### Install postject globally with npm

Source: https://github.com/oven-sh/bun/blob/main/test/js/node/test/fixtures/postject-copy/node_modules/postject/README.markdown

Install the postject command-line interface globally using npm.

```sh
npm i -g postject
```

--------------------------------

### Install Prisma CLI, Client, and LibSQL adapter

Source: https://github.com/oven-sh/bun/blob/main/docs/guides/ecosystem/prisma.mdx

Installs prisma as a development dependency alongside @prisma/client and @prisma/adapter-libsql.

```bash
bun add -d prisma
bun add @prisma/client @prisma/adapter-libsql
```

--------------------------------

### Install express with bun add

Source: https://github.com/oven-sh/bun/blob/main/docs/guides/deployment/render.mdx

Install the Express library into the project using Bun package manager.

```sh
bun add express
```

--------------------------------

### Run secrets tests with automatic CI setup

Source: https://github.com/oven-sh/bun/blob/main/test/js/bun/secrets-ci-setup.md

Runs secrets API tests in CI with automatic detection, package installation, keyring setup, and service initialization.

```bash
# Just run the test normally - setup happens automatically!
bun test test/js/bun/secrets.test.ts
```

--------------------------------

### Set up toolchains and run homepage benchmarks

Source: https://github.com/oven-sh/bun/blob/main/bench/site/README.md

Sets up required toolchains and repositories, then executes the benchmark suite using a specified Bun binary under test. Supports running full suites, specific sub-benchmarks, or quick smoke tests via environment variables.

```sh
bench/site/setup-toolchains.sh                      # node, deno, pnpm/yarn/npm, bun release + canary -> $SITEBENCH_HOME/toolchains
bench/site/setup-repos.sh                           # rolldown/benchmarks clone + deps of bench/express, bench/websocket-server, bench/postgres
BUN=/abs/path/to/bun bench/site/run-all.sh          # everything: bundler http ws postgres install (~30 min with RUNS=3)
BUN=/abs/path/to/bun bench/site/run-all.sh http ws  # a subset
RUNS=1 BUN=... bench/site/run-all.sh                # quick smoke run
```

--------------------------------

### Autolink URLs with fragments

Source: https://github.com/oven-sh/bun/blob/main/test/js/bun/md/spec-permissive-autolinks.txt

Parses optional URL fragments starting with '#' appended to the domain or path.

```example
https://example.com#fragment
.
<p><a href="https://example.com#fragment">https://example.com#fragment</a></p>
.
--fpermissive-url-autolinks
```

--------------------------------

### Reset to a blank starter project

Source: https://github.com/oven-sh/bun/blob/main/test/integration/expo-app/README.md

Moves starter code into the app-example directory and creates a clean app directory for development.

```bash
npm run reset-project
```

--------------------------------

### Initialize project and install @neondatabase/serverless

Source: https://github.com/oven-sh/bun/blob/main/docs/guides/ecosystem/neon-serverless-postgres.mdx

Creates a new project directory, initializes a Bun project with defaults, and adds Neon's serverless driver dependency.

```sh
mkdir bun-neon-postgres
cd bun-neon-postgres
bun init -y
bun add @neondatabase/serverless
```

--------------------------------

### Initialize project and install Drizzle and Neon dependencies

Source: https://github.com/oven-sh/bun/blob/main/docs/guides/ecosystem/neon-drizzle.mdx

Create a new project directory, initialize it with Bun, and install drizzle-orm, the Neon serverless driver, and drizzle-kit.

```sh
mkdir bun-drizzle-neon
cd bun-drizzle-neon
bun init -y
bun add drizzle-orm @neondatabase/serverless
bun add -D drizzle-kit
```

--------------------------------

### Configure security scanner in bunfig.toml

Source: https://github.com/oven-sh/bun/blob/main/docs/pm/security-scanner-api.mdx

Configures Bun to scan packages before installation. Replace the example package name with your scanner's actual package name.

```toml
[install.security]
scanner = "@oven/bun-security-scanner" # example name, replace with your scanner's package
```

--------------------------------

### Escape block quote marker for literal setext heading

Source: https://github.com/oven-sh/bun/blob/main/test/js/bun/md/spec.txt

Use backslash escapes when literal text starting with a block quote marker is intended as a setext heading.

```example
\> foo
------
.
<h2>&gt; foo</h2>
```

--------------------------------

### Configure and start an HTTP server with Bun.serve in index.ts

Source: https://github.com/oven-sh/bun/blob/main/docs/runtime/http/server.mdx

Demonstrates setting up static, dynamic parameter, per-HTTP method, wildcard, redirect, and static file routes using `Bun.serve`. The `routes` option requires Bun v1.2.3+, and `fetch` acts as an optional fallback handler for unmatched routes.

```ts
const server = Bun.serve({
  // `routes` requires Bun v1.2.3+
  routes: {
    // Static routes
    "/api/status": new Response("OK"),

    // Dynamic routes
    "/users/:id": req => {
      return new Response(`Hello User ${req.params.id}!`);
    },

    // Per-HTTP method handlers
    "/api/posts": {
      GET: () => new Response("List posts"),
      POST: async req => {
        const body = await req.json();
        return Response.json({ created: true, ...body });
      },
    },

    // Wildcard route for all routes that start with "/api/" and aren't otherwise matched
    "/api/*": Response.json({ message: "Not found" }, { status: 404 }),

    // Redirect from /blog/hello to /blog/hello/world
    "/blog/hello": Response.redirect("/blog/hello/world"),

    // Serve a file by lazily loading it into memory
    "/favicon.ico": Bun.file("./favicon.ico"),
  },

  // (optional) fallback for unmatched routes:
  // Required if Bun's version < 1.2.3
  fetch(req) {
    return new Response("Not Found", { status: 404 });
  },
});

console.log(`Server running at ${server.url}`);
```

--------------------------------

### Autolink URLs with paths

Source: https://github.com/oven-sh/bun/blob/main/test/js/bun/md/spec-permissive-autolinks.txt

Matches optional URL paths starting with '/' and delimited by slashes. Non-alphanumerical characters (., -, _) in path components must be bounded by alphanumerical characters.

```example
https://example.com/images/branding/logo_272x92.png
.
<p><a href="https://example.com/images/branding/logo_272x92.png">https://example.com/images/branding/logo_272x92.png</a></p>
.
--fpermissive-url-autolinks
```

--------------------------------

### Get current file name with import.meta.file

Source: https://github.com/oven-sh/bun/blob/main/docs/guides/util/import-meta-file.mdx

Use `import.meta.file` to retrieve the filename of the currently executing module (for example, evaluating inside `/a/b/c.ts` returns `"c.ts"`).

```ts
import.meta.file; // => "c.ts"
```

--------------------------------

### Initialize Prisma with SQLite datasource provider

Source: https://github.com/oven-sh/bun/blob/main/docs/guides/ecosystem/prisma.mdx

Uses bunx to run prisma init, generating the schema file, migration directory, and writing DATABASE_URL to .env.

```bash
bunx --bun prisma init --datasource-provider sqlite
```

--------------------------------

### Define an HTTP route and start an Elysia server in server.ts

Source: https://github.com/oven-sh/bun/blob/main/docs/guides/ecosystem/elysia.mdx

Define a GET route at root using Elysia and listen on port 8080. Logs the running server port using the app.server instance.

```ts
import { Elysia } from "elysia";

const app = new Elysia().get("/", () => "Hello Elysia").listen(8080);

console.log(`🦊 Elysia is running on port ${app.server?.port}...`);
```

--------------------------------

### Create and initialize project directory

Source: https://github.com/oven-sh/bun/blob/main/docs/guides/ecosystem/prisma.mdx

Creates a new project directory named prisma-app and initializes it with Bun.

```bash
mkdir prisma-app
cd prisma-app
bun init
```

--------------------------------

### Initialize a new project with bun init

Source: https://github.com/oven-sh/bun/blob/main/docs/guides/ecosystem/upstash.mdx

Creates a new Bun project directory and navigates into the project root.

```sh
bun init bun-upstash-redis
cd bun-upstash-redis
```

--------------------------------

### Scaffold a baseline React app with --react

Source: https://github.com/oven-sh/bun/blob/main/docs/snippets/cli/init.mdx

Creates a baseline React project setup.

```bash
bun init --react
```

--------------------------------

### Install and configure install.security.scanner

Source: https://github.com/oven-sh/bun/blob/main/docs/runtime/bunfig.mdx

Installs a vulnerability scanner package and registers it under [install.security] in bunfig.toml to scan packages before installation.

```bash
bun add -d @oven/bun-security-scanner
```

```toml
[install.security]
scanner = "@oven/bun-security-scanner" # example name, replace with your scanner's package
```

--------------------------------

### Install a specific version of Bun on Windows

Source: https://github.com/oven-sh/bun/blob/main/docs/guides/util/upgrade.mdx

Installs a specific version of Bun via the PowerShell installation script on Windows.

```powershell
iex "& {$(irm https://bun.sh/install.ps1)} -Version 1.3.3"
```

--------------------------------

### Set up a Rust native plugin project

Source: https://github.com/oven-sh/bun/blob/main/docs/runtime/plugins.mdx

Initialize a new NAPI-RS project template and add the bun-native-plugin crate dependency.

```bash
bun add -g @napi-rs/cli
napi new
```

```bash
cargo add bun-native-plugin
```

--------------------------------

### Install LLVM 23 on Ubuntu/Debian

Source: https://github.com/oven-sh/bun/blob/main/docs/project/contributing.mdx

Runs the official LLVM installation script to install LLVM 23 on compatible Ubuntu and Debian systems.

```bash
# LLVM has an automatic installation script that is compatible with all versions of Ubuntu
wget https://apt.llvm.org/llvm.sh -O - | sudo bash -s -- 23 all
```

--------------------------------

### Download and install LLVM on Windows ARM64

Source: https://github.com/oven-sh/bun/blob/main/docs/project/building-windows.mdx

Downloads LLVM 23.1.1 installer directly from GitHub releases and installs it silently on Windows ARM64.

```powershell
# Download and install LLVM for ARM64
Invoke-WebRequest -Uri "https://github.com/llvm/llvm-project/releases/download/llvmorg-23.1.1/LLVM-23.1.1-woa64.msi" -OutFile "$env:TEMP\LLVM-23.1.1-woa64.msi"
Start-Process msiexec.exe -ArgumentList "/i `"$env:TEMP\LLVM-23.1.1-woa64.msi`" /qn" -Wait
```

--------------------------------

### Example project file structure before bundling

Source: https://github.com/oven-sh/bun/blob/main/docs/bundler/index.mdx

Represents the initial file structure with entrypoints residing under the pages directory.

```text
.
└── pages
  └── index.tsx
  └── settings.tsx
```

--------------------------------

### Global test setup with beforeAll and afterAll hooks

Source: https://github.com/oven-sh/bun/blob/main/docs/test/configuration.mdx

Define database setup and teardown handlers using `bun:test` hooks in a preload script.

```typescript
// Global test setup
import { beforeAll, afterAll } from "bun:test";

beforeAll(() => {
  // Set up test database
  setupTestDatabase();
});

afterAll(() => {
  // Clean up
  cleanupTestDatabase();
});
```

--------------------------------

### Install specific packages or versions using bun install

Source: https://github.com/oven-sh/bun/blob/main/docs/pm/cli/install.mdx

Install individual packages by name, specific semver version, or tag into a project.

```bash
bun install react
bun install react@19.1.1 # specific version
bun install react@latest # specific tag
```

--------------------------------

### Install LLVM 23 on Ubuntu and Debian

Source: https://github.com/oven-sh/bun/blob/main/CONTRIBUTING.md

Runs the official LLVM installation script to install LLVM 23 on Ubuntu and Debian distributions. Older Ubuntu versions (<= 20.04) may require installing the C++ standard library separately.

```bash
$ # LLVM has an automatic installation script that is compatible with all versions of Ubuntu
$ wget https://apt.llvm.org/llvm.sh -O - | sudo bash -s -- 23 all
```

--------------------------------

### Run an npm template using equivalent `bun create` and `bunx` commands

Source: https://github.com/oven-sh/bun/blob/main/docs/runtime/templating/create.mdx

Shows equivalent commands for running npm package initializers such as `create-remix`.

```sh
bun create remix
bunx create-remix
```

--------------------------------

### Basic Pub/Sub workflow setup with publisher and subscriber

Source: https://github.com/oven-sh/bun/blob/main/docs/runtime/redis.mdx

Demonstrates how to create and run a Redis Pub/Sub setup using separate publisher and subscriber files in Bun. Note that Redis Pub/Sub is experimental.

```typescript
import { RedisClient } from "bun";

const writer = new RedisClient("redis://localhost:6379");
await writer.connect();

await writer.publish("general", "Hello everyone!");

writer.close();
```

```typescript
import { RedisClient } from "bun";

const listener = new RedisClient("redis://localhost:6379");
await listener.connect();

await listener.subscribe("general", (message, channel) => {
  console.log(`Received: ${message}`);
});
```

```bash
bun run subscriber.ts
```

```bash
bun run publisher.ts
```

--------------------------------

### Install a specific version of Bun on macOS and Linux

Source: https://github.com/oven-sh/bun/blob/main/docs/guides/util/upgrade.mdx

Installs a specific version tag of Bun via the bash install script on macOS and Linux.

```bash
curl -fsSL https://bun.sh/install | bash -s "bun-v1.3.3"
```

--------------------------------

### Parse ordered list start number with leading zeros

Source: https://github.com/oven-sh/bun/blob/main/test/js/bun/md/spec.txt

Demonstrates that leading zeros in start numbers are parsed to their integer value for the list start attribute.

```markdown
003. ok
.
<ol start="3">
<li>ok</li>
</ol>
```

--------------------------------

### Set up development environment using Nix

Source: https://github.com/oven-sh/bun/blob/main/docs/project/contributing.mdx

Enters an isolated Nix development shell with all dependencies preconfigured and builds Bun using `bun bd` without requiring root permissions.

```bash
nix develop
bun bd
```

--------------------------------

### Initialize a new project with bun init

Source: https://github.com/oven-sh/bun/blob/main/docs/quickstart.mdx

Initializes a new Bun project in a directory named my-app.

```bash
bun init my-app
```

--------------------------------

### Install Bun across platforms

Source: https://github.com/oven-sh/bun/blob/main/README.md

Installs Bun using various package managers and platform-specific methods including the install script, PowerShell, npm, Homebrew, and Docker.

```sh
# with install script (recommended)
curl -fsSL https://bun.com/install | bash

# on windows
powershell -c "irm bun.sh/install.ps1 | iex"

# with npm
npm install -g bun

# with Homebrew
brew tap oven-sh/bun
brew install bun

# with Docker
docker pull oven/bun
docker run --rm --init --ulimit memlock=-1:-1 oven/bun
```

--------------------------------

### Install dependencies using hardlink backend

Source: https://github.com/oven-sh/bun/blob/main/docs/pm/cli/install.mdx

Force the hardlink backend with bun install. This is the default and fastest backend on Linux and Windows.

```bash
rm -rf node_modules
bun install --backend hardlink
```

--------------------------------

### Install dependencies

Source: https://github.com/oven-sh/bun/blob/main/bench/postgres/README.md

Installs the required package dependencies for the benchmark suite.

```bash
bun install
```

--------------------------------

### Scaffold a new Bun project with bun init

Source: https://github.com/oven-sh/bun/blob/main/docs/runtime/templating/init.mdx

Runs the interactive project setup to scaffold a new project directory with template choices and default configuration files. Press enter to accept defaults or use the -y flag to skip interactive prompts.

```bash
bun init my-app
```

```txt
? Select a project template - Press return to submit.
❯ Blank
  React
  Library

✓ Select a project template: Blank

 + .gitignore
 + CLAUDE.md
 + .cursor/rules/use-bun-instead-of-node-vite-npm-pnpm.mdc -> CLAUDE.md
 + index.ts
 + tsconfig.json (for editor autocomplete)
 + README.md
```

--------------------------------

### Create a SvelteKit project with Bun

Source: https://github.com/oven-sh/bun/blob/main/docs/guides/ecosystem/sveltekit.mdx

Initialize a new SvelteKit project using the Svelte CLI via `bunx sv create` and select Bun as the package manager during prompt setup.

```sh
bunx sv create my-app
```

```txt
┌  Welcome to the Svelte CLI! (v0.5.7)
│
◇  Which template would you like?
│  SvelteKit demo
│
◇  Add type checking with Typescript?
│  Yes, using Typescript syntax
│
◆  Project created
│
◇  What would you like to add to your project?
│  none
│
◇  Which package manager do you want to install dependencies with?
│  bun
│
◇  Successfully installed dependencies
│
◇  Project next steps ─────────────────────────────────────────────────────╮
│                                                                          │
│  1: cd my-app                                                            │
│  2: git init && git add -A && git commit -m "Initial commit" (optional)  │
│  3: bun run dev -- --open                                                │
│                                                                          │
│  To close the dev server, hit Ctrl-C                                     │
│                                                                          │
│  Stuck? Visit us at https://svelte.dev/chat                              │
│                                                                          │
├──────────────────────────────────────────────────────────────────────────╯
│
└  You're all set!
```

--------------------------------

### Install specific workspace dependencies with bun install --filter

Source: https://github.com/oven-sh/bun/blob/main/docs/pm/workspaces.mdx

Installs dependencies selectively for specific workspace packages by name pattern or directory path while excluding others.

```bash
# Install dependencies for all workspaces starting with `pkg-` except for `pkg-c`
bun install --filter "pkg-*" --filter "!pkg-c"

# Paths can also be used. This is equivalent to the command above.
bun install --filter "./packages/pkg-*" --filter "!pkg-c" # or --filter "!./packages/pkg-c"
```

--------------------------------

### Install commander via npm

Source: https://github.com/oven-sh/bun/blob/main/test/js/node/test/fixtures/postject-copy/node_modules/commander/Readme.md

Installs the Commander package as a dependency in your project.

```sh
npm install commander
```

--------------------------------

### Examples of bun info queries

Source: https://github.com/oven-sh/bun/blob/main/docs/pm/cli/info.mdx

Illustrates common `bun info` commands including querying package information, versions, dependencies, homepages, and JSON output.

```bash
# View basic package information
bun info is-number

# View a specific version
bun info is-number@7.0.0

# View all available versions
bun info is-number versions

# View package dependencies
bun info express dependencies

# View package homepage
bun info lodash homepage

# Get JSON output
bun info react --json
```

--------------------------------

### Install the Railway CLI

Source: https://github.com/oven-sh/bun/blob/main/docs/guides/deployment/railway.mdx

Installs the Railway CLI globally using Bun.

```bash
bun install -g @railway/cli
```

--------------------------------

### Example pages directory structure

Source: https://github.com/oven-sh/bun/blob/main/docs/runtime/file-system-router.mdx

Sample file tree illustrating a Next.js-style pages directory with index, nested dynamic routes, and catch-all routes.

```txt
pages
├── index.tsx
├── settings.tsx
├── blog
│   ├── [slug].tsx
│   └── index.tsx
└── [[...catchall]].tsx
```

--------------------------------

### Initialize a new Bun project directory

Source: https://github.com/oven-sh/bun/blob/main/docs/guides/ecosystem/prisma-postgres.mdx

Creates a new project directory and initializes it with default configuration using bun init.

```bash
mkdir prisma-postgres-app
cd prisma-postgres-app
bun init
```

--------------------------------

### Specify global package installation path with install.globalDir in bunfig.toml

Source: https://github.com/oven-sh/bun/blob/main/docs/runtime/bunfig.mdx

Defines the target directory where packages installed with bun install --global are placed. Can also be set with the BUN_INSTALL_GLOBAL_DIR environment variable.

```toml
[install]
# where `bun install --global` installs packages
globalDir = "~/.bun/install/global"
```

--------------------------------

### Start the SvelteKit production server

Source: https://github.com/oven-sh/bun/blob/main/docs/guides/ecosystem/sveltekit.mdx

Start the built application server with Bun. The server listens on port 3000 by default unless configured using the `PORT` environment variable.

```sh
bun ./build/index.js
```

```txt
Listening on http://0.0.0.0:3000/
```

--------------------------------

### Install LLVM on Fedora

Source: https://github.com/oven-sh/bun/blob/main/docs/project/contributing.mdx

Installs llvm, clang, and lld-devel using DNF on Fedora.

```bash
sudo dnf install llvm clang lld-devel
```

--------------------------------

### Start the SvelteKit development server with Bun

Source: https://github.com/oven-sh/bun/blob/main/docs/guides/ecosystem/sveltekit.mdx

Run the development server using Bun with the `--bun` flag. Omit `--bun` if you want to run the dev server with Node.js instead.

```sh
cd my-app
bun --bun run dev
```

```txt
  $ vite dev
  Forced re-optimization of dependencies

    VITE v5.4.10  ready in 424 ms

    ➜  Local:   http://localhost:5173/
    ➜  Network: use --host to expose
    ➜  press h + enter to show help
```

--------------------------------

### Install @happy-dom/global-registrator

Source: https://github.com/oven-sh/bun/blob/main/docs/guides/test/happy-dom.mdx

Install the Happy DOM global registrator package as a development dependency.

```sh
bun add -d @happy-dom/global-registrator
```

--------------------------------

### Install react and react-dom

Source: https://github.com/oven-sh/bun/blob/main/docs/guides/ecosystem/ssr-react.mdx

Install React and React DOM dependencies using Bun.

```sh
# Any package manager can be used
bun add react react-dom
```

--------------------------------

### Install the @sentry/bun SDK

Source: https://github.com/oven-sh/bun/blob/main/docs/guides/ecosystem/sentry.mdx

Install the official Sentry SDK for Bun into your project dependencies.

```sh
bun add @sentry/bun
```

--------------------------------

### Start benchmark server using Node.js

Source: https://github.com/oven-sh/bun/blob/main/bench/stream-file-upload-client/README.md

Runs the destination HTTP web server to receive file uploads.

```bash
node server-node.mjs

```

--------------------------------

### Start a simple HTTP server with Bun.serve

Source: https://github.com/oven-sh/bun/blob/main/docs/guides/http/simple.mdx

Starts an HTTP server listening on port 3000 that responds to every request with status 200 and "Welcome to Bun!".

```typescript
const server = Bun.serve({
  port: 3000,
  fetch(request) {
    return new Response("Welcome to Bun!");
  },
});

console.log(`Listening on ${server.url}`);
```

--------------------------------

### Start service immediately

Source: https://github.com/oven-sh/bun/blob/main/docs/guides/ecosystem/systemd.mdx

Starts the systemd service manually without needing to reboot.

```bash
systemctl start my-app
```

--------------------------------

### Install Prisma CLI, Client, and Postgres adapter

Source: https://github.com/oven-sh/bun/blob/main/docs/guides/ecosystem/prisma-postgres.mdx

Installs the prisma CLI as a development dependency alongside @prisma/client and @prisma/adapter-pg.

```bash
bun add -d prisma
bun add @prisma/client @prisma/adapter-pg
```

--------------------------------

### Start the Fullstack Dev Server

Source: https://github.com/oven-sh/bun/blob/main/docs/bundler/fullstack.mdx

Runs the application server script using Bun CLI.

```bash
bun run app.ts
```

--------------------------------

### Inheriting install settings in bunfig.toml

Source: https://github.com/oven-sh/bun/blob/main/docs/test/configuration.mdx

Configure [install] settings like registry, exact, and prefer alongside [test] options in bunfig.toml. bun test inherits network and package installation settings during test execution.

```toml
[install]
# These settings are inherited by bun test
registry = "https://npm.company.com/"
exact = true
prefer = "offline"

[test]
# Test-specific configuration
coverage = true
```

--------------------------------

### Configure bunfig.toml and run Fullstack Dev Server

Source: https://github.com/oven-sh/bun/blob/main/packages/bun-plugin-svelte/README.md

Register bun-plugin-svelte under serve.static plugins in bunfig.toml, then run the dev server with an HTML entrypoint.

```toml
[serve.static]
plugins = ["bun-plugin-svelte"]

```

```sh
$ bun index.html

```

--------------------------------

### Run initial Prisma database migration

Source: https://github.com/oven-sh/bun/blob/main/docs/guides/ecosystem/prisma-postgres.mdx

Creates and applies the initial SQL migration to the PostgreSQL database. Pass --env-file=.env because Bun does not load .env automatically when executing CLIs with --bun.

```bash
bun run --bun --env-file=.env prisma migrate dev --name init
```

```txt
Loaded Prisma config from prisma.config.ts.

Prisma schema loaded from prisma/schema.prisma.
Datasource "db": PostgreSQL database "mydb", schema "public" at "localhost:5432"

Applying migration `20250114141233_init`

The following migration(s) have been created and applied from new schema changes:

prisma/migrations/
  └─ 20250114141233_init/
    └─ migration.sql

Your database is now in sync with your schema.
```